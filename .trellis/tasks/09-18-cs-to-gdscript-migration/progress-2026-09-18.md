# 2026-09-18 迁移进度

## 本批边界

- 将运行中的局外长按时长换算迁移到 `core/constants/world_interaction_timing.gd`。
- 将无项目调用方的 `WeatherType` 枚举迁移到 `core/constants/weather_type.gd`。
- 保留 `WorldInteractionTiming.cs` 作为尚未迁移的 C# 资源层兼容实现，避免采集资源和 C# 测试出现半迁移引用。

## 验证

- GodotAI MCP 会话 `cusga@e91d97bd8fc818bd` 可用，Godot `4.7.1`。
- `game_eval` 验证长按时长 `0/10/20/-5` 行动值分别为 `0/1/2/0` 秒。
- `game_eval` 验证 `WeatherType.Rain` 与 `WeatherType.RAIN` 均为 `0`。
- 主场景重新运行后 helper 正常上线，当前运行日志无本批新增错误。
- `env CI=true dotnet build CUSGA.sln --no-restore` 受本机缺失 `Godot.NET.Sdk/4.7.1` 和 NuGet 网络限制阻断，尚不能作为编译通过证据。

## 后续

- 迁移 `ReusableGatheringInteraction` 时先保留旧 C# 资源类和 C# 测试作为行为对照；下一阶段再在确认无调用方后移除 `WorldInteractionTiming.cs` 兼容引用。
- 继续迁移纯值对象和 Resource 数据类，逐批更新 `.tres`/场景引用后再删除对应 C# 文件。

## 2026-09-18 21:19：可重复采集 Resource 迁移

### 本批边界

- 新增 `resources/interaction/reusable_gathering_interaction.gd`，保留 `TimeCost`、`GatheringTag`、`DropTable`、次数刷新、最低耗时、工具槽位减免和有序操作描述。
- 将 `resources/map/terrain/reusable_wood_terrain.tres` 与森林场景中的可重复采集子资源切换到 GDScript Resource。
- 将 `TerrainCardData.InteractionBehavior` 放宽为 `Resource`，`TerrainInteractionExecutor` 增加 GDScript `Array[Dictionary]` 操作描述桥接；旧 C# `TerrainInteraction` 继续走原有 `TerrainOp` 路径。
- 将 `WorldInteractionCoordinator` 与 `RoomBoardPresenter` 的可重复采集检测、冷却刷新和有效耗时读取改为兼容 C# / GDScript 两套 API。
- 新增 `tests/godot/test_reusable_gathering_interaction.gd` 及 MCP 根目录转发入口，覆盖工具槽位、最低耗时、次数刷新、操作顺序、快照耗时和实际 `.tres` 脚本引用。

### 验证

- GodotAI `script_manage.find_symbols`：新资源与测试脚本解析成功，无本文件诊断。
- GodotAI `test_run(suite="reusable_gathering")`：5/5 通过，23 个断言，无失败。
- GodotAI `resource_manage.load` 与测试套件实际加载 `reusable_wood_terrain.tres`：交互脚本路径为 `res://resources/interaction/reusable_gathering_interaction.gd`。
- `$env:CI='true'; dotnet build CUSGA.sln --no-restore`：0 警告、0 错误。
- 主场景运行仍被既有 `player_manager.gd` placeholder 错误阻断；该错误与本批资源脚本无关，需后续单独处理。

## 2026-09-18 21:38：遭遇倍率范围 Resource 迁移

### 本批边界

- 新增 `resources/encounters/monster_stat_multiplier_range.gd`，保留十二个倍率字段、1.0 默认值及上下界快照方法。
- 将 7 个地形 `.tres` 和 7 个地图 `.tscn` 的倍率范围子资源切换到 GDScript 脚本路径；不改变任何倍率数值。
- 更新一次性地形配置生成脚本，避免重新生成时恢复旧 C# 倍率资源路径。
- 将 `RoomTerrainProfile.EncounterVarianceRange` 放宽为通用 `Resource`，让 `RoomTerrainLayoutGenerator` 按稳定字段名读取 Variant，同时保留旧 C# Resource 类型供兼容测试使用。
- 更新 `gameplay-system-patterns.md`，记录 C#/GDScript Resource 并存期间的操作描述桥接和倍率范围契约。

### 验证

- GodotAI `script_manage.find_symbols`：倍率范围脚本和新增测试脚本解析成功。
- GodotAI `test_run(suite="reusable_gathering")`：6/6 通过，新增倍率范围断言 25 个。
- GodotAI `resource_manage.load`：`reusable_wood_terrain.tres` 能够加载，7 个地形资产的嵌套倍率资源均由回归测试确认指向 GDScript。
- `$env:CI='true'; dotnet build CUSGA.sln --no-restore`：0 警告、0 错误。
- 地图场景 MCP 运行未上线 helper；返回的 `current_run_errors` 为空，提示中的 `reusable_gathering` 解析信息属于编辑器保留旧日志，另有既有 `player_manager.gd` placeholder 问题。

## 2026-09-18 22:40：通道驻守概率修正 Resource 迁移

### 本批边界

- 新增 `resources/map/passage_guard_probability_modifier.gd`，保留 `RequiredTag`、
  `AdditiveChance`、`Multiplier` 三个序列化字段和原默认值。
- 将 `default_passage_guard_settings.tres` 的 `ProbabilityModifiers` 接入一个中性
  GDScript 修正子资源；中性值保证基础概率与旧配置一致。
- 保留 `PassageGuardSettings.cs`、`PassageGuardProbabilityModifier.cs` 和提供器的
  C# 兼容边界，避免当前驻守控制器及既有运行时测试发生半迁移断裂。
- 将资源字段回归断言并入已注册的 `reusable_gathering` MCP 套件，验证默认字段、嵌套
  `.tres` 资源脚本路径和中性数值。

### 验证

- GodotAI `resource_manage.load`：默认驻守配置成功加载，嵌套概率修正资源存在。
- GodotAI `test_run(suite="reusable_gathering")`：8/8 通过，0 失败；本批新增默认字段
  与嵌套配置断言均通过。
- GodotAI `project_run(mode="current")`：当前场景无新增运行错误；helper 在停止前未保持
  live，游戏日志只有已知 helper 注册信息。
- GodotAI 编辑器测试发现器仍显示一次已删除独立测试文件的缓存 `load_errors`；该路径已从
  磁盘删除，需重启编辑器后清理预加载缓存，不作为本批脚本失败证据。

## 2026-09-18 22:55：采集遭遇规则 Resource 迁移

### 本批边界

- 新增 `resources/encounters/gathering_encounter_rule.gd`，保留采集标签、怪物数组、
  提示语和遭遇倍率四个序列化字段。
- 将 `scenes/Main.tscn` 中三个采集遭遇子资源切换到 GDScript 脚本，并移除旧 C# 自定义类型
  UID；规则数值和怪物资源引用保持不变。
- 将 `EncounterManager.GatheringRules` 放宽为 `Array<Resource>`，在读取边界动态转换
  `StringName`、倍率、提示文本和 `MonsterData` 数组；昼夜倍率、装备倍率、随机判定和
  结果构造逻辑保持原样。
- 保留 `GatheringEncounterRule.cs` 作为兼容类型，避免现有 C# 测试和未迁移调用方断裂。

### 验证

- `$env:CI='true'; dotnet build CUSGA.sln --no-restore`：0 警告、0 错误。
- GodotAI `script_manage.find_symbols`：新规则脚本解析成功，4 个导出字段全部存在。
- GodotAI `test_run(suite="reusable_gathering")`：9/9 通过，新增采集遭遇规则默认值断言。
- GodotAI `scene_open(res://scenes/Main.tscn)`：主场景成功打开；随后主场景运行
  `current_run_errors=[]`，没有本批新增脚本错误。
- UTF-8 roundtrip：新脚本、C# 管理器、主场景和测试文件通过。

## 2026-09-18 23:20：驻守 Encounter 数据与 Settings Resource 迁移

### 本批边界

- 新增 `resources/map/passage_guard_encounter_data.gd`，保留 `Monsters` 数组字段。
- 新增 `resources/map/passage_guard_settings.gd`，迁移基础概率、home 保护标签、概率修正
  和默认 encounter 池四个设置字段。
- 将默认驻守 `.tres`、地图属性 `guard_encounter_pool` 和驻守控制器切换到通用 GDScript
  Resource 边界；六个未配置 encounter 的群系资源移除旧 C# encounter 外部脚本引用。
- `PassageGuardMonsterResolver` 新增 `ResolveResources`，保留旧强类型 `Resolve` 重载；
  `PassageGuardProbabilityProvider` 接受通用 Settings Resource，旧 C# 类型仍可兼容输入。
- 修复一次真实序列化问题：仅迁移 encounter 而保留 C# Settings 时，默认池在编辑器中
  反序列化为空；将 Settings 一并迁移后恢复两个默认 encounter，未改变怪物或概率数值。

### 验证

- `$env:CI='true'; dotnet build CUSGA.sln --no-restore`：0 警告、0 错误。
- GodotAI `script_manage.find_symbols`：Settings 4 个导出字段、Encounter 1 个导出字段，
  驻守控制器解析成功。
- GodotAI `resource_manage.load(default_passage_guard_settings.tres)`：Settings 脚本为
  `passage_guard_settings.gd`，默认池包含两个 encounter 子资源。
- GodotAI `test_run(suite="reusable_gathering")`：10/10 通过，0 失败。
- `scene_open(res://scenes/map_scenes/map_control.tscn)` 成功；旧 C# Settings/Encounter
  路径在场景和资源扫描中无残留。
- UTF-8 roundtrip：本批 GDScript、C#、`.tres`、地图属性和测试文件通过。

## 2026-09-18 23:22：怪物技能条目与集合 Resource 迁移

### 本批边界

- 新增 `resources/monster/monster_skill_entry_data.gd` 与
  `resources/monster/monster_skill_set_data.gd`，保留技能字段、默认值、技能说明回退和
  `Skills` 数组顺序。
- 将全部项目怪物 `.tres` 与 `scenes/monster_scenes/monster.tscn` 的技能条目/集合脚本
  引用切换到 GDScript，并清理旧 C# 自定义类型元数据；怪物技能资源内容未重排或补填。
- 将 `MonsterData.SkillSet` 与 `MonsterSkillComponent` 放宽为通用 Resource；组件在边界
  动态读取 C#/GDScript 的 `Skills`、`Skill`、`VisibleInPreview` 和预览说明，同时保留
  `Initialize(MonsterSkillSetData)` 兼容重载。
- 将预览回退、默认集合和代表性怪物资产断言并入已注册的 `reusable_gathering` MCP 套件。

### 验证

- GodotAI `script_manage.find_symbols`：两个新 Resource 和测试脚本解析成功，导出字段与
  `GetPreviewDescription` 均可发现。
- GodotAI `test_run(suite="reusable_gathering")`：12/12 通过，新增预览与资产断言 37 个；
  `test_monster_2.tres` 的原始空技能集合已确认迁移后仍为空。
- 已扫描项目资源和场景，旧 `MonsterSkillEntryData.cs` / `MonsterSkillSetData.cs` 路径
  仅保留在兼容 C# 文件及其 `.uid` 旁车文件中，无运行时资源引用残留。
- `$env:CI='true'; dotnet build CUSGA.sln --no-restore`：0 警告、0 错误，Resource 类型边界
  和 `MonsterSkillComponent` 动态读取 API 编译通过。
- UTF-8 roundtrip：本批脚本、C#、63 个怪物资源、场景与文档均通过。

### 迁移后续验证补充

- 已执行 `$env:CI='true'; dotnet build CUSGA.sln --no-restore`，0 警告、0 错误。
- GodotAI `test_run(suite="reusable_gathering")` 已复跑，12/12 通过。
- `scene_open(res://scenes/monster_scenes/monster.tscn)` 与主场景打开成功；主场景运行后
  helper 延迟上线，最终 `session_active=true`，游戏日志无新增脚本错误。
- 发现并修复 `card_table/export_current_cards.py` 中两个旧技能资源路径，防止后续导出回滚。
- 全项目旧技能资源路径搜索仅保留兼容 C# 文件和其 `.uid` 旁车文件。

## 2026-09-18 23:25：掉落条目 Resource 迁移

### 本批边界

- 新增 `resources/loot/loot_drop.gd`，保留物品资源、掉落概率和数量范围四个字段。
- 将全部怪物、地形、地图场景和地形生成脚本中的 `LootDrop` 子资源切换到 GDScript，
  不改变掉落数值和表格顺序。
- 将 `LootTable.Drops` 放宽为 `Array<Resource>`，集中读取 C#/GDScript 条目的稳定字段；
  随机抽样和 `ItemStack` 创建仍由 C# `LootTable` 负责，旧 `LootDrop.cs` 继续作为兼容输入。
- 扩展 GodotAI 回归测试，覆盖默认字段、代表性怪物资产和 GDScript 掉落条目引用。

### 验证

- GodotAI `script_manage.find_symbols`：掉落条目脚本解析成功，4 个导出字段存在。
- GodotAI `test_run(suite="reusable_gathering")`：13/13 通过。
- GodotAI `game_eval` 在主场景运行中加载 `test_monster_1.tres`，确认 1 个 GDScript 掉落条目
  经 C# `LootTable.RollLoot(0)` 生成 1 个 `ItemStack`。
- `$env:CI='true'; dotnet build CUSGA.sln --no-restore`：0 警告、0 错误。
- 旧 `LootDrop.cs` 资源路径搜索无运行时残留，仅保留兼容 C# 文件和 `.uid` 文件。

## 2026-09-19 00:18：地形池条目 Resource 迁移

### 本批边界

- 新增 `core/map/room_terrain_pool_entry.gd`，保留 `TerrainData` 与 `Weight` 两个序列化字段及 `1.0` 默认权重。
- 将 7 个地形 `.tres`、7 个地图 `.tscn` 和地形配置生成脚本切换到 GDScript 条目路径，并移除森林场景残留的旧 C# 类型元数据。
- 将 `RoomTerrainProfile.TerrainPool` 放宽为 `Array<Resource>`；`RoomTerrainLayoutGenerator` 通过稳定字段读取地形卡与权重，保留旧 C# 条目的兼容输入和原轮盘抽样行为。
- 扩展 `reusable_gathering` 套件，验证条目默认值、真实资源脚本路径和嵌套地形池可加载性。

### 验证

- GodotAI `filesystem_manage(op="scan")` 完成编辑器扫描。
- GodotAI `script_manage.find_symbols`：新 Resource 解析成功，包含 2 个导出字段；测试脚本解析成功。
- GodotAI `test_run(suite="reusable_gathering")`：14/14 通过，0 失败。
- GodotAI `resource_manage.load` 与 `game_eval`：`reusable_wood_terrain.tres` 的池条目脚本为 `res://core/map/room_terrain_pool_entry.gd`，权重为 `1.0`。
- GodotAI 打开并运行 `map_desert.tscn`，随后主场景运行 helper 上线；当前运行日志无本批新增错误。
- 旧 `RoomTerrainPoolEntry.cs` 仅保留兼容 C# 文件及 `.uid`，运行时资源和场景无旧路径残留。

## 2026-09-19 00:45：地形布局配置 Resource 迁移

### 本批边界

- 新增 `core/map/room_terrain_profile.gd`，保留地形池、数量范围、网格尺寸、摆放范围和遭遇倍率范围字段。
- 将 7 个地形 `.tres`、7 个地图 `.tscn` 和地形配置生成脚本的 Profile 引用切换到 GDScript；不改变已序列化的配置值。
- 将 `RoomTerrainLayoutGenerator` 增加通用 `Resource` 入口和稳定字段读取，保留旧 `Generate(RoomTerrainProfile)` 重载；`RoomBoardPresenter` 与地图脚本使用通用 Resource 边界。
- 保留 `RoomTerrainProfile.cs` 及其 C# 测试调用作为兼容垫片；StartingStats、物品链、Crafting 和 Autoload 不在本批范围。

### 验证

- GodotAI `filesystem_manage(op="scan")` 完成编辑器扫描。
- GodotAI `script_manage.find_symbols`：Profile 脚本包含 8 个导出字段，测试脚本解析成功。
- GodotAI `resource_manage.load(res://resources/map/terrain/reusable_wood_terrain.tres)`：顶层脚本为 `res://core/map/room_terrain_profile.gd`，字段值和嵌套池条目保持不变。
- GodotAI `test_run(suite="reusable_gathering")`：15/15 通过，包含 Profile 默认值、真实资产脚本路径和嵌套资源加载断言。
- GodotAI 打开并运行 `map_desert.tscn` 后，helper 上线且游戏日志无本批新增错误；停止运行后编辑器保持可用。
- 旧 Profile C# 路径在运行时资源和场景中无残留，仅保留兼容 C# 文件、测试引用及 `.uid` 旁车文件。

### 当前总进度判断

`cs迁移gds要求.md` 的全量迁移尚未完成。当前已完成局外长按、多个低风险 Resource 和地形配置链的分阶段迁移；项目仍保留 C#、`.csproj`、`.sln` 及 C# Autoload，后续需按依赖顺序继续处理剩余模块并在最终阶段清理。

### 下一步建议

1. 继续盘点并迁移剩余低风险 Resource，逐批更新资产引用和 GodotAI 回归测试。
2. 在独立设计兼容边界后再处理 StartingStats、物品链、Crafting 和 Autoload。
3. 每批完成后执行旧路径扫描、UTF-8 检查、聚焦测试和主场景冒烟；全量完成前保留所有 C# 兼容垫片与工程文件。

### 后续尝试记录

- 曾尝试继续迁移低风险 `AttributeModifierData`，但当前 GodotAI 编辑器会话未重新编译修改后的 C# Resource 定义；实际运行仍把状态数组视为旧 C# 强类型，GDScript 条目无法加载。
- 该尝试已完整撤回，当前 `reusable_gathering` 套件恢复 15/15 通过。后续处理该条目时，必须先确认编辑器 C# 程序集刷新，再设计 `Array[Resource]` 兼容边界和资源序列化迁移。

## 2026-09-19 01:48：天气配置 Resource 迁移

### 本批边界

- 新增 `resources/weather/weather_data.gd`，保留 `WeatherName`、`ElementModifiers`、
  `CropGrowthSpeedMultiplier` 和 `CanExtinguishCampfire` 四个字段及原默认值。
- 天气配置当前没有项目资产或 GDScript 运行时消费者；因此本批只迁移数据载体，不切换
  `WeatherManager` Autoload、不修改 `ElementalSystem`，避免把 Autoload 专项和低风险 Resource
  混合迁移。
- 保留 `resources/weather/WeatherData.cs` 与 `core/autoloads/WeatherManager.cs` 作为兼容边界，
  后续 Autoload 批次再设计 `Resource` 信号和元素修正字典的跨语言封送。
- 将天气字段默认值、整数元素键/浮点倍率字典形状和新脚本路径断言并入已有
  `reusable_gathering` GodotAI 套件；未注册的独立天气测试入口已撤回。

### 验证

- GodotAI `session_manage(op="list")`：CUSGA 会话为 `ready`，编辑器版本 4.7.1。
- GodotAI `filesystem_manage(op="scan")` 完成扫描；`script_manage.find_symbols` 确认天气脚本
  继承 `Resource` 且有 4 个导出字段，测试脚本解析成功。
- GodotAI `test_run(suite="reusable_gathering")`：16/16 通过，0 失败；新增天气 Resource
  断言全部通过，基线由 15/15 提升为 16/16。
- GodotAI `project_run(mode="current")`：当前 `map_desert.tscn` 最终进入 live，游戏日志仅有
  helper 注册信息；停止后编辑器恢复 `ready`。
- 编辑器日志中的 AttributeModifierData 旧缓存错误和 ItemsControl 无效 UID 警告均未由本批
  触发；本批没有新增脚本解析、资源加载、信号或类型错误。
- UTF-8 读取检查通过；旧路径扫描仅保留 `WeatherData.cs` / `WeatherManager.cs` 兼容链和新
  `weather_data.gd` 测试引用。

### 遗留风险与下一步

- 天气 GDScript 资源尚未接入真实 `.tres` 资产，跨语言运行时封送仍需在 Autoload 专项中验证。
- `ElementModifiers` 的枚举键数值必须继续沿用 `ElementType` 的稳定值，不能在后续迁移中重排。
- 下一步继续盘点剩余低风险 Resource；处理 `StartingStats`、物品链、Crafting、Autoload
  或 `AttributeModifierData` 前，分别建立独立兼容边界，并先确认 C# 类型表已刷新。

## 2026-09-19 01:52：驻守状态 RefCounted 迁移

### 本批边界

- 新增 `core/map/passage_guard_state.gd`，等价实现 `Count`、`AddGuard`、`IsGuarded`、
  `ClearGuard` 和 `ClearAll`，沿用 C# `PassageGuardEdge` 的端点字典序规范化规则。
- `scripts/map_scripts/passage_guard_controller.gd` 改为显式预加载 GDScript 状态对象；
  `tests/godot/passage_guard_tests.gd` 和可注册的 `reusable_gathering` 套件同步使用新脚本。
- 新脚本不声明 `class_name PassageGuardState`，避免与旧 C# 全局类重名；
  `core/map/PassageGuardState.cs` 保留为兼容垫片，未删除任何 C# 工程文件或 Autoload。

### 验证

- GodotAI `filesystem_manage(op="scan")` 完成扫描；`script_manage.find_symbols` 确认状态脚本
  继承 `RefCounted`，控制器和测试脚本解析成功。
- GodotAI `test_run(suite="reusable_gathering")`：17/17 通过，新增驻守状态断言覆盖反向查询、
  无向边去重、单边清理和 `ClearAll`。
- 原 `passage_guard_tests.gd` 为旧式 `SceneTree` 测试，当前发现器不注册该文件；未将其未执行
  结果误报为通过，但已更新其状态实例化路径，并由可注册套件覆盖核心状态行为。
- GodotAI `project_run(mode="current")`：`map_desert.tscn` 进入 live，游戏日志仅有 helper
  注册信息；停止后编辑器恢复 `ready`，没有本批新增脚本或运行时错误。
- UTF-8 读取检查通过；旧路径扫描仅保留 `PassageGuardState.cs` 兼容文件及新 GDScript 引用。

### 遗留风险与下一步

- C# `PassageGuardEdge.cs` 和 `PassageGuardState.cs` 仍由工程保留，待所有 C# 驻守调用方清理后
  才能进入最终删除阶段。
- 下一步继续选择有真实资产或单一调用链的低风险 Resource/纯逻辑模块；`StartingStats`、物品链、
  Crafting、Autoload 和 `AttributeModifierData` 仍需各自独立设计与验证。

## 2026-09-19 02:02：天气 Autoload 迁移

### 本批边界

- 新增 `core/autoloads/weather_manager.gd`，保留 `CurrentWeather` 导出属性、`WeatherChanged`
  信号和 `ChangeWeather(Resource)` 方法的名称、触发顺序及日志行为。
- `project.godot` 的 `WeatherManager` Autoload 切换到 GDScript；旧的
  `core/autoloads/WeatherManager.cs` 与 `resources/weather/WeatherData.cs` 保留为迁移期兼容垫片，
  未删除 C# 工程文件或其他 Autoload。
- `core/combat/ElementalSystem.cs` 不再依赖 `WeatherManager.Instance` 或 C# `WeatherData` 类型，
  改通过 `/root/WeatherManager` 和通用 `Resource` 字段读取天气倍率；元素整数键和原有计算顺序不变。
- 修正 `resources/weather/weather_data.gd` 的迁移说明，使其与已切换的 GDScript Autoload 边界一致。
  当前项目仍没有天气 `.tres` 资产消费者，本批不扩展天气配置资产或 StartingStats、物品链、Crafting。

### 验证

- GodotAI `session_manage(op="list")`：CUSGA 会话为 `ready`，Godot 4.7.1；
  `project_manage(settings_get)` 与 `autoload_manage(list)` 均确认 `WeatherManager` 指向
  `res://core/autoloads/weather_manager.gd`。
- GodotAI `filesystem_manage(op="scan")` 完成扫描；`script_manage.find_symbols` 确认天气管理器
  的 `CurrentWeather`、`WeatherChanged`、`ChangeWeather` 以及天气 Resource 的四个导出字段均可解析。
- GodotAI `test_run(suite="reusable_gathering")`：18/18 通过，新增 Autoload 字段、信号、切换方法
  和 ProjectSettings 路径断言；测试返回的预加载缓存提示仅是编辑器通用提示，本批复跑结果无失败。
- GodotAI `project_run(mode="current")` 启动 `map_desert.tscn` 后通过 `editor_state` 确认
  `helper_live=true`、`session_active=true`；`logs_read(source="game")` 仅有 helper 注册信息。
  初次运行调用达到工具等待上限，但游戏随后已正常进入 live，已停止并恢复编辑器 `ready`。
- 编辑器日志中的 AttributeModifierData 旧缓存错误、旧测试 `assert_same` 解析记录和 ItemsControl
  无效 UID 警告均为本批之前已知记录；本批没有新增脚本解析、资源加载、信号或运行时错误。
- UTF-8 严格解码检查覆盖本批脚本、C# 桥接、测试、`project.godot` 和两份进度文档，全部通过；
  天气旧 C# 资源路径扫描无运行时引用，仅保留两个兼容 C# 文件。

### 兼容边界与遗留风险

- GDScript 管理器使用通用 `Resource` 作为天气输入，允许现有 C# `WeatherData` 或新的
  `weather_data.gd` 在边界传入；C# 元素系统仍保留旧 API 文件，但运行时通过节点路径取值。
- `ElementModifiers` 继续使用 `ElementType` 的稳定整数键；后续不得重排枚举值或把字典改成
  未验证的强类型数组。
- `WeatherManager.cs` / `WeatherData.cs` 以及其他 C# Autoload 仍不能删除；下一步按独立边界继续
  盘点 StartingStats、物品链、Crafting 或下一个低风险 Resource，并在再次处理
  `AttributeModifierData` 前确认 C# 程序集和类型表已刷新。

## 2026-09-19 02:11：驻守概率服务迁移

### 本批边界

- 新增 `core/map/passage_guard_probability_provider.gd`，等价实现 `Calculate`、标签过滤、
  加法修正、乘法修正和 0 到 1 的概率限制；不改变驻守配置 Resource、怪物解析或地图刷怪顺序。
- `scripts/map_scripts/passage_guard_controller.gd` 改为显式预加载 GDScript 服务，并通过稳定的
  `RefCounted.call("Calculate", ...)` 边界调用，移除控制器对 C# `PassageGuardProbabilityProvider`
  全局类型的编译期依赖。
- `tests/godot/passage_guard_tests.gd` 同步切换服务脚本；可注册的
  `reusable_gathering` 套件新增轻量标签假对象和概率顺序回归测试。
- 更新驻守配置资源注释以反映概率服务已迁移；保留 `core/map/PassageGuardProbabilityProvider.cs`
  作为兼容垫片，未删除 C# 工程文件。

### 验证

- GodotAI `filesystem_manage(op="scan")` 完成扫描；`script_manage.find_symbols` 确认服务继承
  `RefCounted` 并解析出 5 个函数，控制器和两份测试脚本均解析成功。
- GodotAI `test_run(suite="reusable_gathering")`：19/19 通过，新增概率计算顺序、标签过滤和
  GDScript 服务入口断言；期间发现测试整体替换 `Array[Resource]` 会丢弃条目，已改为逐项追加并复跑通过。
- GodotAI `project_run(mode="current")`：`map_desert.tscn` 最终进入 live；`logs_read(source="game")`
  仅有 helper 注册信息，随后停止并恢复编辑器 `ready`。
- 编辑器日志中仍可见本批之前的 AttributeModifierData 类型封送、旧测试缓存解析和 ItemsControl
  无效 UID 记录；本批新增的 `passage_guard_tests.gd` 类型推断错误已修复，当前符号扫描无新增解析错误。
- UTF-8 严格解码检查覆盖服务、控制器、配置脚本和测试文件，全部通过；旧 C# 概率服务路径扫描
  仅保留兼容文件及历史设计文档，没有运行时脚本或场景引用。

### 兼容边界与遗留风险

- 服务以通用 `Resource` 读取 `BaseGuardChance`、`ProbabilityModifiers`、`RequiredTag`、
  `AdditiveChance` 和 `Multiplier`，因此旧 C# 配置和 GDScript 配置均可继续使用。
- 标签边界只要求对象提供 `HasTag(StringName)`，不把 `TagComponent` C# 类型写入 GDScript，
  避免再次引入强类型封送依赖。
- `PassageGuardProbabilityProvider.cs`、`PassageGuardState.cs` 和其他驻守 C# 类型仍需等所有
  调用方迁移后统一清理；下一步继续处理 StartingStats、物品链、Crafting 或另一个明确低风险边界。

## 2026-09-19 02:28：ShopCatalog Resource 迁移

### 本批范围

- 新增 `resources/shop/shop_catalog.gd`，保留 `Goods`、`AlsoIncludeEveryPricedItem`、
  `DefaultBuyPrice` 三个导出字段、默认值和 `ContainsExplicitly` 公共方法。
- `resources/shop/shop_catalog.tres` 切换到 GDScript Resource，`Goods` 序列化数组改为
  `Array[Resource]`；89 个商品资源、数组顺序和既有目录值均保持不变，并移除该资产中仅用于
  C# 数组类型声明的 `ItemData.cs` 引用。
- `core/shop/ShopTradeBridge.cs` 的 `Catalog` 改为通用 `Resource`，新增集中式字段读取和
  显式商品判断，兼容旧 C# `ShopCatalog` 与新 GDScript 目录；价格回退、自动上架、去重和
  `CardId` 排序规则未改变。
- `scripts/shop/shop_control.gd` 仅更新目录职责注释；`scenes/Shop/Shop.tscn` 保持节点结构和
  目录资源注入关系不变，旧 `ShopCatalog.cs` 与 `ShopTradeBridge.cs` 继续作为兼容边界保留。
- `tests/godot/test_reusable_gathering_interaction.gd` 新增目录默认值、脚本路径、89 项数量及
  首尾顺序回归断言。

### 兼容边界

- GDScript 目录使用 `Array[Resource]`，商品条目仍可为 C# `ItemData`；桥接器通过
  `Resource.Get` 和 Variant 数组读取，不让运行时绑定已迁移的 C# 目录类型。
- `ShopTradeBridge` 保持原有公开交易方法和 `ShopService` 规则层不变；只有目录读取边界从
  强类型 `ShopCatalog` 放宽为通用 `Resource`。
- 未删除任何 C#、`.csproj`、`.sln` 或 Autoload；旧目录类型仍可作为兼容输入，待所有商店调用方
  完成迁移并通过最终清理审查后再处理。

### 验证结果

- GodotAI 会话 `cusga@2c3dc3c0106e8ec2` 始终可用，编辑器版本 4.7.1，最终恢复 `ready`。
- `filesystem_manage(scan)`、`script_manage.find_symbols` 和 `resource_manage(load)` 均通过；
  目录脚本识别为 `Resource`，资产脚本路径为 `res://resources/shop/shop_catalog.gd`，
  `Goods` 加载为 89 项，默认兜底买价为 100。
- `test_run(suite="reusable_gathering")`：20/20 通过，0 失败；新增商店目录断言通过。
- Shop 场景两次运行均进入 `live`（首次 helper 等待窗口超时后延迟进入）；`game_eval` 确认
  `ShopTradeBridge.Catalog` 使用新脚本且 `shop_control._shop_catalog` 实际构建为 89 项。
  游戏日志只有 GodotAI helper 注册信息，没有新增运行时错误；主地图冒烟也进入 `live`。
- UTF-8 严格解码检查通过；旧路径扫描在运行时资产和脚本中不再发现 `ShopCatalog.cs` 或旧
  `shop_catalog` C# 资源引用，仅保留兼容 C# 文件本身和 ShopTradeBridge 场景桥接。

### 遗留风险与下一步

- Shop 场景仍有迁移前就存在的 `tests/huhu9/test_gold.gd` 缺失脚本错误，以及商品图片无效 UID
  警告；本批未修改测试按钮或第三方/既有资产，需单独清理并重新验证场景错误面板。
- 编辑器仍显示 AttributeModifierData 类型封送、旧 `assert_same` 解析记录和驻守旧测试缓存记录；
  这些不是本批新增问题，AttributeModifierData 仍需先确认 C# 程序集和类型表刷新。
- 下一批继续选择一个独立边界；StartingStats、物品链、Crafting、其他 Autoload 和
   AttributeModifierData 不与本批合并。

## 2026-09-19 02:44：StartingStats Resource 迁移

### 本批范围

- 新增 `resources/stats/starting_stats.gd`，完整保留 C# `StartingStats` 的 30 个导出字段、字段名、默认值和序列化语义；不声明同名 `class_name`，避免与旧 C# 全局类冲突。
- 将 56 个真实 `.tres/.tscn` 资产中的 StartingStats 脚本引用切换到 GDScript，覆盖怪物资源、玩家场景和调试载荷；移除旧 C# UID 元数据，未改变任何属性值或嵌套资源标识。
- `MonsterData.InitialAttributes`、`DebugLoadoutData.PlayerStartingStats`、`AttributeComponent.InitialData` 改为通用 `Resource` 边界；AttributeComponent 通过同名字段读取，旧 C# StartingStats 和新 GDScript Resource 均可输入。
- `EncounterMonsterScaler` 保留原 C# 输出工厂和缩放公式，仅将输入读取改为通用 Resource；C# 测试断言同步使用 Variant 字段读取。
- `StartingStats.cs`、其余 C# 工程文件和 Autoload 均保留为迁移期兼容输入，未做物品链、Crafting、Autoload 或 AttributeModifierData 扩展。

### 兼容边界

- GDScript 资源只负责序列化 30 个初始属性字段；C# 组件和遭遇缩放器负责读取、校验和运行时计算，不在 Resource 中复制业务逻辑。
- C# 消费端使用 `Resource.Get` 与 Variant 数值检查，字段缺失时沿用原 C# 默认值；因此旧 C# 资源、迁移后的 GDScript 资源和缩放产生的 C# 兼容对象可以在同一调用链中共存。
- 所有场景节点结构、属性名、枚举值、数值、存档字段和 UI 行为均未改变；本批没有删除 C# 兼容垫片。

### 验证结果

- GodotAI 会话 `cusga@2c3dc3c0106e8ec2`：编辑器 4.7.1，文件系统扫描完成，最终恢复 `ready`。
- `script_manage.find_symbols`：`starting_stats.gd` 解析为 `Resource`，30 个导出字段全部出现；聚焦套件脚本解析成功。
- `test_run(suite="reusable_gathering")`：21/21 通过，新增 StartingStats 默认值、代表性怪物资产和调试载荷脚本路径断言全部通过。
- `project_run(mode="custom", scene="res://scenes/player_scenes/player.tscn")` 与怪物场景均进入 live；`game_eval` 确认运行时脚本路径为 `res://resources/stats/starting_stats.gd`，玩家默认物攻/生命/能量为 100/1000/100，且 AttributeComponent 无新增错误。
- 主地图此前运行已进入 live；本批相关运行日志仅有 GodotAI helper 和 `InitializeWithData<Resource>` 信息，没有新增 SCRIPT ERROR、Parse Error、资源加载、信号或类型错误。
- 本批 64 个脚本/资产文件严格 UTF-8 解码通过；旧路径扫描未发现运行时 `StartingStats.cs` 引用，仅保留兼容 C# 文件与历史文档。

### 遗留风险与下一步

- `StartingStats.cs` 仍由 C# 测试、缩放工厂和默认回退使用，必须等 AttributeComponent、MonsterData 和相关 C# 调用链完成 GDScript 迁移后才可清理。
- 旧编辑器错误中的 AttributeModifierData 类型封送、`assert_same` 缓存记录、商店缺失 `tests/huhu9/test_gold.gd` 和商品图片 UID 警告仍未触发本批；不得将其误判为本批回归。
- 下一步继续选择单一边界；优先盘点物品链或另一个低风险 Resource。处理 AttributeModifierData 前仍必须先确认 GodotAI 已刷新 C# 程序集和类型表。

## 2026-09-19 03:04：TerrainCardData Resource 迁移（运行冒烟阻塞）

### 本批范围

- 新增 `resources/interaction/terrain_card_data.gd`，保留 `TerrainCardData` 原有的 `CardId`、`CardName`、`CardIcon`、`Description` 和 `InteractionBehavior` 五个导出字段及默认值；不声明 `class_name`，避免与旧 C# 全局类型发生冲突。
- 将 7 个地形 Profile、3 个被地图子场景直接加载的 `res/terrain/*.tres` 地形卡，以及 5 个地图子场景中的地形卡脚本引用切换到 GDScript，移除仅属于旧 C# 脚本的 UID 元数据；所有池条目、交互资源、图标和倍率配置保持原值。
- `TerrainSpawnPlacement`、`TerrainInstance`、`RoomTerrainStore`、`BoardCardState`、`BoardCardView`、`RoomBoardPresenter`、`TerrainInteractionExecutor` 和 `WorldInteractionCoordinator` 的地形卡读取改为通用 `Resource` 字段协议；棋盘显示通过 `CardName/CardIcon` 读取，交互通过 `InteractionBehavior` 读取。
- 更新地形生成脚本与 `board_card_view_tests.gd` 的脚本路径；在 `test_reusable_gathering_interaction.gd` 增加地形卡默认值、7 个代表性资产、池条目交互资源和 GDScript 脚本路径断言。

### 兼容边界

- GDScript 资源只承担五个序列化字段，不复制地图生成、交互结算或棋盘行为；旧 `TerrainCardData.cs`、`RoomTerrainPoolEntry.cs` 和 C# 测试输入继续保留。
- C# 地图与棋盘代码统一接收 `Resource`，通过 `Resource.Get` 读取稳定字段；因此旧 C# `TerrainCardData` 与新 GDScript 资源都能进入同一调用接口，未改变节点结构、信号、输入、存档字段或玩法数值。
- 编辑器诊断确认 GDScript 无法直接继承 C# `BaseCardData`，该尝试已恢复为 `extends Resource`，没有留下半迁移脚本或旧资产引用。

### 验证结果

- GodotAI 会话 `cusga@2c3dc3c0106e8ec2` 保持 `ready`；`filesystem_manage(scan)` 完成，`script_manage.find_symbols` 确认新脚本为 `Resource` 且 5 个导出字段完整。
- `resource_manage(load)` 成功加载 7 个 Profile 中的代表资源和 `under_lake.tres`、`sand.tres`、`berryubush.tres` 三个直接地形卡；`scene_open` 成功打开对应地图子场景和 `Main.tscn`。
- `test_run(suite="reusable_gathering")`：22/22 通过，其中地形卡迁移断言覆盖 7 个 Profile、3 个直接地形卡和 38 个字段/路径断言；当前结果仍带编辑器 preload cache warning。
- 多次 `project_run`（主场景、沙漠、玩家、怪物）均未进入 GodotAI `helper_live`，最终状态为 `not_live`；编辑器增量日志没有新增 SCRIPT ERROR、Parse Error、Failed to load script 或资源加载错误，但 C# 棋盘运行边界因此未能完成运行时冒烟，不能宣称本批完整验收。
- 本批涉及脚本、C# 适配器、生成脚本和测试共 13 个文件严格 UTF-8 解码通过；旧路径扫描仅保留兼容 C# 文件、C# 对照测试和注释，运行时 `.tres/.tscn` 不再引用 `TerrainCardData.cs` 或其 UID。

### 遗留风险与下一步

- 当前最大阻塞是 GodotAI 项目运行链未进入 `helper_live`，且直接实例化 C# 棋盘对象的聚焦尝试在首个断言前被 C# 加载阻塞；必须先恢复编辑器 C# 程序集/运行时链并完成地图与棋盘冒烟，再把本批标记为完成。
- 既有 AttributeModifierData 类型封送、旧 `assert_same` 解析记录、商品图片 UID 警告和缺失测试脚本仍是历史噪声，不作为本批回归；处理 AttributeModifierData 前仍需单独确认 C# 类型表刷新。
- 在运行冒烟恢复前不扩展物品链、Crafting、Autoload、战斗或其他高耦合模块；恢复后先复跑本批 22/22、地图/棋盘场景，再决定是否进入下一独立 Resource 边界。

## 2026-09-19 03:20：TerrainCardData 运行链续诊断（仍阻塞）

### 诊断结果

- GodotAI 插件重载后新会话 `cusga@bb7641175acabad0` 已恢复 `ready`；`filesystem_manage(op="scan")` 报告全局类型表 160 项，`resource_manage.get_info` 可识别 `AttributeModifierData` 与 `StartingStats`，但不能据此证明运行时程序集已更新。
- `test_run(suite="reusable_gathering")` 仍为 22/22，通过结果带有 GDScript preload cache warning；在编辑器重启前不能把该结果作为依赖刷新后的最终证据。
- 主菜单、地图沙漠、玩家和怪物相关 `project_run` 均在完整等待窗口后为 `not_live`，`logs_read(source="game")` 无 helper 注册行；未观察到本批新增的 SCRIPT ERROR、Parse Error、Failed to load script 或资源加载错误。
- 文件时间核对显示 C# 适配器源码最后修改于 02:57，而 `.godot/mono/temp/bin/Debug/CUSGA.dll` 仍为 02:41；GodotAI 暴露的扫描和插件重载没有触发可确认的 C# 重新编译。运行时棋盘边界因此仍不可验收。

### 当前阻塞与下一步

- TerrainCardData 批次继续保持“实现已切换、运行验收未完成”，不进入物品链、Crafting、Autoload、战斗或 AttributeModifierData。
- 需要开发者在 Godot 编辑器内完成一次 C# Build/重启，使程序集时间戳晚于本批 C# 适配器修改，并重新打开项目；恢复后先复跑 22/22（无 cache warning）、地图/棋盘场景和主场景日志，再决定是否关闭本批。

## 2026-09-19 03:24：补齐直接地图地形卡资产

- 旧路径扫描确认 `map_underlake.tscn`、`map_desert.tscn`、`map_brook.tscn` 直接加载的 `res/terrain/under_lake.tres`、`sand.tres`、`berryubush.tres` 仍引用 C# TerrainCardData；本批已将三项顶层脚本和元数据切换为 `terrain_card_data.gd`，保留嵌套交互、掉落和卡牌字段值。
- 三个资源均通过 GodotAI `resource_manage.load`，相关场景均可 `scene_open`；聚焦套件仍为 22/22，测试新增直接资源分组断言。磁盘旧路径扫描不再发现任何 `.tres/.tscn` 的 `TerrainCardData.cs` 引用。
- 运行冒烟仍受同一 C# 程序集未刷新阻塞，批次状态不变：实现已切换但未完成 `helper_live` 场景验收。

## 2026-09-19 03:34：TerrainCardData 续接阻塞复核

### 本轮范围

- 仅复核 GodotAI 会话、程序集刷新迹象、聚焦测试和主场景启动；没有扩展到物品链、Crafting、Autoload、战斗或 AttributeModifierData。
- `session_manage(list)` 与 `editor_state` 均确认会话 `cusga@bb7641175acabad0`、Godot 4.7.1、编辑器 `ready`。
- `filesystem_manage(scan)` 完成但全局类注册增量为 0；`resource_manage(get_info, AttributeModifierData)` 可读到 6 个属性，但这仍不能证明 C# DLL 已重编译。

### 验证结果

- `test_run(suite="reusable_gathering")` 为 22/22、0 失败，但仍带 preload cache warning，因此不能作为编辑器重启后的最终证据。
- `project_run(mode="main")` 在等待窗口后仍为 `helper_live=false` / `not_live`；`logs_read(source="game")` 无运行日志，编辑器日志只有既有无效图片 UID 和 GDScript 未使用参数警告，没有本轮新增 SCRIPT ERROR、Parse Error、Failed to load script 或资源加载错误。
- 磁盘时间核对显示 `core/gameflow/WorldInteractionCoordinator.cs`（02:57）、`core/map/RoomBoardPresenter.cs`（03:19）均晚于 `.godot/mono/temp/bin/Debug/CUSGA.dll`（02:41），程序集刷新阻塞仍未解除。

### 兼容边界与遗留风险

- TerrainCardData GDScript 资源、通用 `Resource` 字段协议和旧 C# 兼容垫片保持不变；本轮没有改变序列化字段、节点结构、信号、输入或玩法数值。
- 在开发者于 Godot 编辑器内执行 C# Build/重启、使程序集更新时间晚于适配器修改，并完成无 cache warning 的 22/22、地图/棋盘和主场景 `helper_live` 冒烟前，TerrainCardData 批次仍不得标记完成，也不得开始下一独立模块。

## 2026-09-19 03:38：TerrainCardData 续接再次复核

### 复核证据

- `session_manage(list)`：`cusga@bb7641175acabad0` 仍为 Godot 4.7.1、编辑器 `ready`、当前场景 `res://scenes/Main.tscn`。
- `filesystem_manage(scan)`：全局类总数 160，注册增量 0；`resource_manage(get_info, AttributeModifierData)` 仍可读到 6 个属性，但不构成程序集刷新证据。
- `test_run(suite="reusable_gathering")`：22/22、0 失败，仍带 preload cache warning。
- `project_run(mode="main")`：run token 7 在等待后为 `helper_live=false` / `not_live`；`logs_read(source="game")` 无运行行。编辑器输出仅有既有图片 UID 和未使用信号/参数警告，未见本批新增脚本解析或资源加载错误。
- C# 文件时间未变：`WorldInteractionCoordinator.cs` 02:57、`RoomBoardPresenter.cs` 03:19，`CUSGA.dll` 仍为 02:41。

### 决策

同一程序集未刷新阻塞再次确认，继续停止迁移扩展；待开发者完成 Godot 编辑器 C# Build/重启后，先复跑无 cache warning 的测试、地图/棋盘场景和主场景冒烟。

## 2026-09-19 03:40：程序集阻塞审计

- 新一轮 `session_manage(list)` 仍为 `ready`，但 `CUSGA.dll` 时间戳仍为 02:41；`WorldInteractionCoordinator.cs` 和 `RoomBoardPresenter.cs` 分别为 02:57、03:19。
- 本轮未发现任何可由 GodotAI MCP 完成的 C# Build 操作；文件系统扫描、类型表读取和项目运行均不能替代程序集重编译。
- 同一外部阻塞已连续多轮复现，任务停止在 TerrainCardData 运行验收处；开发者完成 Godot 编辑器 C# Build/重启前，不进入下一批。

## 2026-09-19 03:49：修复 BoardCardState 的 Resource 命名空间错误

### 本轮范围

- 处理开发者反馈的四个相同 `CS0246: Resource` 构建错误；错误集中来自 `core/board/BoardCardState.cs` 的四个 `Resource` 类型引用。
- 仅在该文件加入 `using Godot;`，不改变棋盘卡牌状态类、属性、节点结构、序列化字段或运行时逻辑。

### 兼容边界

- `BoardCardState` 继续使用 Godot 原生 `Godot.Resource` 作为 `CardData` 和 `TerrainData` 的兼容边界；`LootBoardCardState` 与 `TerrainBoardCardState` 的原有返回值和校验保持不变。
- 未删除任何 C# 文件、工程文件或 Autoload，也未扩展到物品链、Crafting、AttributeModifierData 或战斗系统。

### 验证结果与遗留风险

- 文本扫描确认所有使用 `Resource` 的项目 C# 文件现在均有 `using Godot;` 或等价 Godot 类型引用；`BoardCardState.cs` 头部已正确加载该命名空间。
- GodotAI `filesystem_manage(scan)` 完成，但全局类注册增量为 0；`.godot/mono/temp/bin/Debug/CUSGA.dll` 仍为 02:41，尚未取得重新编译证据。
- `project_run(mode="main")` 仍为 `helper_live=false` / `not_live`；编辑器增量日志新增 `Missing class qualified name for reloading script`，因此不能宣称 C# 编译或 TerrainCardData 运行验收已通过。
- 修复后需要开发者重新执行 Godot 编辑器 C# Build/重启，再检查 CS0246 是否消失、DLL 时间戳、无 cache warning 的聚焦测试和主场景冒烟。

## 2026-09-19 03:56：BoardCardState 修复后的程序集复验

### 验证结果

- 开发者 Build 已成功产出 `.godot/mono/temp/bin/Debug/CUSGA.dll`（03:48，大小 701952），晚于 `BoardCardState.cs`（03:47）；增量编辑器日志不再出现 `CS0246: Resource`。
- `resource_manage(get_info, AttributeModifierData)` 仍可读取 6 个属性，确认 C# 类型表可用；`reusable_gathering` 为 22/22、0 失败，但仍带 preload cache warning。
- 清理旧 Debugger 错误后重跑主场景，仍复现 `Missing class qualified name for reloading script`，状态为 `helper_live=false` / `not_live`。当前编辑器布局仍把 `res://core/board/BoardCardState.cs` 作为打开/选中脚本，说明错误发生在编辑器脚本重载状态，不能归因于 TerrainCardData 逻辑。

### 决策与下一步

- `using Godot;` 修复本身已通过程序集产物和无新增 CS0246 日志间接确认；不继续修改棋盘状态类或迁移范围。
- 需要开发者完整重启 Godot 编辑器以清除旧 C# 脚本重载状态，然后复跑无 cache warning 的聚焦测试、主场景和地图/棋盘冒烟；全部通过后才关闭 TerrainCardData 批次。

## 2026-09-19 04:03：TerrainCardData 运行链恢复与旧 UID 清理

### 本轮范围

- 继续只收尾 TerrainCardData 批次；没有进入物品链、Crafting、Autoload、战斗或 AttributeModifierData 迁移。
- 复核 `BoardCardState.cs` 的 `using Godot;` 修复已进入 03:48 生成的 `CUSGA.dll`，并重新运行主场景与三张地图场景。
- 清理 `res/terrain/sand.tres` 顶层残留的 `metadata/_custom_type_script = uid://bpfhfc81wwi3i`；该 UID 对应旧 `TerrainCardData.cs.uid`。聚焦测试新增顶层旧类型元数据断言。

### 兼容边界

- `BoardCardState` 继续以 Godot `Resource` 作为 `CardData`/`TerrainData` 边界；本轮没有改变棋盘状态类、序列化字段或节点结构。
- `sand.tres` 仍保留原卡牌字段、图标、交互资源和嵌套掉落值，只删除已失效的旧 C# 类型元数据。
- C# 兼容垫片、`.csproj`、`.sln` 和 Autoload 均保留。

### 验证结果

- GodotAI 会话 `cusga@bb7641175acabad0` 为 `ready`；`AttributeModifierData` 类型表可读到 6 个属性。
- 主场景以及 `map_underlake.tscn`、`map_desert.tscn`、`map_brook.tscn` 延迟轮询后均进入 `helper_live=true`；游戏日志仅有 helper 注册信息，没有新增脚本、资源、信号或类型错误。
- 修复后的 `reusable_gathering` 测试在编辑器缓存刷新前为 21/22：新增断言读到了缓存中的旧 `sand.tres` 元数据；磁盘文件已确认该行删除。测试响应继续明确提示必须重启编辑器清理 GDScript/ResourceLoader preload 缓存。
- 旧路径扫描：运行时目录不再包含 `TerrainCardData.cs`，只剩兼容文件的 `.uid` 旁车；旧 UID 同样只剩 `resources/interaction/TerrainCardData.cs.uid`。本轮涉及文件严格 UTF-8 解码通过。

### 遗留风险与下一步

- TerrainCardData 批次仍未关闭：需要开发者完整关闭并重新打开 Godot 编辑器，使 `sand.tres` 和 GDScript preload 缓存刷新，然后复跑无 cache warning 的 22/22、主场景和三张地图冒烟。
- `Missing class qualified name for reloading script` 仍是编辑器 Mono 重载残留，但不再伴随 `CS0246: Resource`；不得据此继续修改 `BoardCardState`。
- 在上述复跑全部通过前，不进入物品链、Crafting、Autoload、战斗或 AttributeModifierData。

## 2026-09-19 04:07：编辑器导入后缓存阻塞复核

- GodotAI 会话短暂处于 `importing` 后恢复 `ready`，但编辑器 PID 未变，说明不是完整重启。
- `reusable_gathering` 复跑仍为 21/22；`sand.tres` 断言继续读到编辑器缓存中的旧 `_custom_type_script`，响应仍带 preload cache warning。
- 清理诊断后唯一错误仍为 Godot Mono `Missing class qualified name for reloading script`，没有新的 `CS0246`、脚本解析、资源加载或运行时错误。
- TerrainCardData 批次继续冻结；必须由开发者完整关闭并重新打开 Godot 编辑器，再复跑无 cache warning 的 22/22 和场景冒烟。

## 2026-09-19 04:17：旧 metadata 被编辑器写回后的再次清理

### 本轮范围

- 新编辑器会话 `cusga@759444691df20a65`（PID `43892`）启动后，聚焦测试仍为 21/22；GodotAI `filesystem_manage(read_text)` 发现 `sand.tres` 的旧 `TerrainCardData.cs` UID 已被编辑器重新写回磁盘。
- 再次只删除 `res/terrain/sand.tres` 顶层 `metadata/_custom_type_script` 行，没有修改卡牌字段、嵌套交互或掉落数据。

### 验证与阻塞

- 磁盘读取和 GodotAI `filesystem_manage(read_text)` 均确认旧 UID 已删除；`resource_manage(load)` 仍返回旧缓存对象，证明当前进程已持有旧 Resource 实例。
- 未继续运行可能触发保存的场景或项目操作；TerrainCardData 批次仍等待下一次完整编辑器重启后，复跑无 cache warning 的 22/22 和场景冒烟。
- C# 兼容垫片、工程文件和 Autoload 保持不变；本轮涉及资源和文档严格 UTF-8 检查通过。

## 2026-09-19 04:32：TerrainCardData 批次完成验收

### 本批范围

- 完成 `resources/interaction/terrain_card_data.gd` 及其 7 个地形 Profile、3 个直接地形卡和 5 个地图子场景引用的迁移验收。
- 保留 `TerrainCardData.cs`、地图/棋盘 C# 兼容读取和所有工程文件；未进入物品链、Crafting、Autoload、战斗或 AttributeModifierData 迁移。

### 兼容边界

- GDScript Resource 继续只负责 `CardId`、`CardName`、`CardIcon`、`Description`、`InteractionBehavior` 五个序列化字段。
- C# 地图、棋盘和交互执行器继续通过通用 `Resource` 字段协议读取 C# 或 GDScript 资源；节点结构、信号、输入、存档字段和玩法数值未改变。
- `sand.tres` 顶层旧 `_custom_type_script` 已清除，嵌套 C# `GatheringInteraction` 和 `LootTable` 仍按兼容边界保留。

### 验证结果

- GodotAI 新会话 `cusga@719b375e9263c636`、Godot 4.7.1、编辑器 PID `2612` 为 `ready`；`resource_manage(load)` 确认 `sand.tres` 和可重复木材 Profile 均能加载，旧 metadata 不再出现。
- `resource_manage(get_info, AttributeModifierData)` 返回 6 个属性，满足后续处理前的 C# 类型表刷新前置条件；本批未迁移该类型。
- `test_run(suite="reusable_gathering")`：22/22 通过、0 失败。响应仍带 GodotAI 通用 preload cache 提示，但新会话的实际资源读取与断言均已刷新，编辑器日志无新增错误。
- 主场景、`map_underlake.tscn`、`map_desert.tscn`、`map_brook.tscn` 延迟轮询均进入 `helper_live=true`；游戏日志仅有 helper 注册信息。
- 旧路径扫描：运行时目录不再包含 `TerrainCardData.cs` 或其 UID，只保留 `resources/interaction/TerrainCardData.cs.uid` 兼容旁车；涉及资源、脚本和文档严格 UTF-8 检查通过。

### 下一步

- 下一批按依赖顺序盘点物品链，先选择一个低耦合 `Resource` 数据边界，单独设计 C# / GDScript 兼容输入后再迁移。
- 继续保留所有 C#、`.csproj`、`.sln` 和 C# Autoload；处理 AttributeModifierData 前仍以已刷新类型表为前置条件。

## 2026-09-19 04:39：BoardCardState Resource 类型限定修复

### 本轮范围

- 处理开发者反馈的四个 `CS0246: Resource` 构建错误，范围仅限 `core/board/BoardCardState.cs` 的四个 Godot Resource 类型声明。
- 将四处 `Resource` 明确限定为 `Godot.Resource`；保留 `using Godot;`，不改变棋盘卡牌状态行为、字段值、节点结构或资源序列化格式。

### 兼容边界

- `BoardCardState.CardData`、`LootBoardCardState.CardData`、`TerrainBoardCardState.TerrainData/CardData` 继续使用 Godot 原生 `Resource` 作为 C# 与 GDScript 数据载体边界。
- 未删除 C# 兼容文件、工程文件或 Autoload，未扩展到物品链、Crafting、AttributeModifierData 或战斗迁移。

### 验证结果与遗留风险

- GodotAI 会话 `cusga@719b375e9263c636` 为 `ready`；`filesystem_manage(scan)` 完成，`script_manage(read)` 确认四处声明均为 `Godot.Resource`。
- `reusable_gathering` 聚焦测试为 22/22、0 失败；响应带既有 preload cache 提示，未新增本批相关错误。
- 主场景本轮未进入 helper live，编辑器增量日志未出现新的 `CS0246`、Parse Error、SCRIPT ERROR、Failed to load script 或资源加载错误；需开发者在本地构建后确认 IDE 错误列表已清空。
- 涉及 C# 文件、进度文档和反馈文档均通过 UTF-8 读取；旧路径扫描未发现因本修复产生的新 C# 路径问题。

### 下一步

- 开发者重新执行一次 Godot 编辑器 C# Build，确认四个 `CS0246` 不再出现；若仍有错误，请提供完整错误文件和行号以便定位是否为另一份未保存副本。
- 构建确认后再继续下一独立物品链 Resource 边界；不重复 TerrainCardData 已完成迁移。

## 2026-09-19 04:51：基础 ItemData GDScript 契约准备

### 本批范围

- 新增 `resources/item/base_card_data.gd` 和 `resources/item/item_data.gd`，分别对应 C# `BaseCardData` 与 `ItemData` 的字段、默认值、显示回退和 `ActualMaxStackSize` 契约。
- 在 `reusable_gathering` 套件中增加 `test_item_data_gdscript_contract_defaults`，覆盖基础字段、默认值、显示回退和最大堆叠值变化。
- 本批不切换生产 `.tres`、`.tscn`、Inventory、Crafting、Equipment、Tool 或 ItemsControl；这是为后续物品链兼容桥准备的独立数据边界，不重复已完成的地形/掉落迁移。

### 兼容边界与回滚点

- 新脚本不声明 `class_name`，避免与仍在运行的 C# 全局类型 `BaseCardData`/`ItemData` 冲突；当前生产资产继续使用 C# 脚本，旧存档和强类型 C# API 不受影响。
- 字段名保持 `CardId`、`CardName`、`CardIcon`、`Description`、`MaxStackSize`、`ItemTags`、`BuyPrice`、`SellPrice`；后续资产切换可直接复用原序列化键。
- 回滚点为删除两份新增 GDScript 及其契约测试，不涉及任何既有资源和场景文件。

### GodotAI 验证结果

- 会话 `cusga@719b375e9263c636`：`ready`；`filesystem_manage(scan)` 完成。
- `script_manage(read/find_symbols)` 成功读取两份脚本，确认继承关系和 8 个导出字段。
- 聚焦新增测试：1/1 通过，17 个断言；完整 `reusable_gathering`：23/23 通过，0 失败。响应仍带既有 preload cache 提示。
- 主场景 `project_run(mode="main")` 初始等待窗口未立即返回 live，随后 `logs_read(source="game")` 确认 `helper_live=true`；游戏日志仅有 helper 注册信息，编辑器增量日志无新增 SCRIPT ERROR、Parse Error、Failed to load script、类型或资源错误。

### 旧路径与 UTF-8 检查

- 新增脚本、测试、进度文档和反馈文档均通过显式 UTF-8 解码。
- 生产资源没有新增 GDScript ItemData 引用；现有 108 个 `ItemData.cs`/`ResourceCardData.cs` 资产路径仍作为预期兼容输入保留，未执行批量替换。

### 遗留风险与下一步

- 物品链尚未迁移完成：`ItemStack`、Inventory、ItemsControl、Shop、LootTable、Crafting 和 GDScript UI 仍依赖 C# `ItemData`，不能仅替换脚本路径。
- 下一批应单独实现并验证通用 ItemData 兼容桥（字段读取、C# 旧对象与 GDScript Resource 的身份/堆叠语义），再选择一组不影响 Crafting 的资产切换；不得在桥未稳定前修改 103 个生产物品资产。
- `AttributeModifierData` 未触碰；其迁移仍需先确认程序集和类型表刷新。

## 2026-09-19 04:58：BoardCardState 构建诊断复验

### 本轮范围

- 响应开发者仍看到四处 `CS0246: Resource` 的反馈，重新核对 `core/board/BoardCardState.cs` 及工程内同名文件。
- 当前磁盘唯一的 `BoardCardState.cs` 四处跨语言边界均已使用 `Godot.Resource`；不存在第二份同名源码或未限定的 `Resource` 声明。

### GodotAI 验证结果

- 会话 `cusga@719b375e9263c636` 保持 `ready`；`resource_manage(get_info)` 可读取 `BaseCardData`、`ItemData`、`TerrainCardData`，说明当前加载的程序集和 Godot `Resource` 类型表可用。
- 清理编辑器诊断后运行主场景，延迟状态进入 `helper_live=true`；游戏日志只有 GodotAI helper 注册信息。
- `reusable_gathering` 聚焦套件为 23/23、0 失败；编辑器日志扫描没有新增 `CS0246`、Parse Error、SCRIPT ERROR、Failed to load script、类型或信号错误。

### 遗留风险与下一步

- GodotAI 没有提供替代本地 IDE C# Build 的命令；开发者看到的四条错误与当前磁盘源码及已加载程序集不一致，优先按旧诊断缓存或未重新加载的工作副本处理。
- 不再重复修改 `BoardCardState`，也不进入物品链下一批；需要开发者在当前工作区重新加载文件并执行一次 Godot 编辑器 C# Build。若仍报错，请提供新的完整文件路径、行列号和构建时间戳。

## 2026-09-19 05:09：ItemData 兼容读取桥

### 本批范围

- 新增 `resources/item/item_data_compat.gd`，提供跨语言 ItemData 的字段读取、显示回退、堆叠上限、标签、买卖价和对象身份比较。
- 扩展 `reusable_gathering` 测试，分别用并行 GDScript ItemData 和现有 C# `items/items/applecore.tres` 验证桥接输入。
- 未修改生产物品 `.tres`、`.tscn`、Inventory、ItemStack、Shop、Crafting、Equipment、Tool 或任何 C# 消费者。

### 兼容边界与回滚点

- 工具只接受 `Resource` 并通过 `Object.get`/属性表读取稳定字段，不创建包装对象、不复制资源，因此 `ItemStack` 原有的引用相等堆叠规则保持不变。
- 稳定字段为 `CardId`、`CardName`、`CardIcon`、`Description`、`MaxStackSize`、`ItemTags`、`BuyPrice`、`SellPrice`；显示属性优先，缺失时回退到序列化字段。
- 回滚点为删除 `item_data_compat.gd` 和新增测试段，不涉及任何资产脚本 UID。

### GodotAI 验证结果

- `filesystem_manage(scan)` 完成；`script_manage(find_symbols/read)` 确认桥脚本继承 `RefCounted` 并包含 13 个函数。
- 聚焦桥测试：1/1 通过，22 个断言；完整 `reusable_gathering`：24/24 通过、0 失败。
- 旧 C# `applecore.tres` 可加载，脚本仍为 `res://resources/item/ItemData.cs`；新 GDScript ItemData 实例字段和标签读取均通过。
- 主场景延迟进入 `helper_live=true`；停止后游戏/编辑器日志过滤没有新增 `CS0246`、Parse Error、SCRIPT ERROR、Failed to load script、资源、类型或信号错误。既有物品图标 UID 警告仍属于未改动资产。

### 旧路径、编码与遗留风险

- 新增脚本、测试、进度文档和反馈文档通过显式 UTF-8 解码。
- 旧 C# 资产路径仍按兼容边界保留：约 103 个 `ItemData.cs` 资源和 5 个 `ResourceCardData.cs` 资源；测试中的旧路径引用继续允许存在。
- `ClassDB.instantiate("ItemData")` 不能实例化 C# 全局类，但真实旧 `.tres` 仍可加载；后续测试和运行时桥接必须使用资源路径或现有 C# 消费端，不把 `ClassDB` 当作 C# Resource 工厂。

### 下一步

- 先在开发者环境确认 `BoardCardState` 的 C# Build 诊断已刷新；随后选择一组不涉及 Crafting/Equipment 的低风险物品资产，接入该桥并逐个验证序列化值。
- 继续保留所有 C#、`.csproj`、`.sln` 和 Autoload；不批量替换 108 个生产资源。

## 2026-09-19 05:18：BoardCardState 构建错误再次复核

### 本轮范围

- 响应开发者再次反馈四处 `CS0246: Resource`，仅复核 `core/board/BoardCardState.cs` 的磁盘源码、程序集类型表和运行链，不重复修改棋盘状态或物品迁移范围。
- 当前唯一同名源码的四处声明仍为 `Godot.Resource`；报错文本仍显示未限定的 `Resource`，且给出的行号对应旧文件内容，说明本地构建器/IDE 使用了旧缓冲或旧诊断。

### 验证结果与阻塞

- GodotAI 会话 `cusga@719b375e9263c636` 保持 `ready`；`filesystem_manage(scan)` 完成，`BaseCardData`、`ItemData`、`TerrainCardData` 和 `AttributeModifierData` 类型表均可读取。
- `reusable_gathering` 为 24/24、0 失败；主场景延迟进入 `helper_live=true`，编辑器和游戏日志没有新增 `CS0246`、Parse Error、SCRIPT ERROR、Failed to load script、资源或信号错误。
- GodotAI 没有可替代开发者本地 C# Build 的构建命令，因此仍不能替开发者确认 IDE 错误列表已刷新；在该外部状态确认前冻结下一批迁移。

### 下一步

- 开发者需关闭旧的 `BoardCardState.cs` 编辑器标签或重新从磁盘加载文件，再执行一次 Godot 编辑器 C# Build；确认构建输入中的四处声明是 `Godot.Resource`。
- 若仍报错，请提供新的完整错误时间戳、行号和构建输出；在此之前不进入物品资产切换、Crafting、Autoload、战斗或 `AttributeModifierData` 迁移。

## 2026-09-19 13:42：ItemStack GDScript 并行契约

### 本批范围

- 新增 `resources/item/item_stack.gd`，并行复刻 C# `core/inventory/ItemStack.cs` 的 Resource 引用、数量、空/满判断、可用空间、溢出添加、复制、清空、洗炼属性和变化通知语义。
- 新增独立 GodotAI 套件 `tests/godot/test_item_stack_contract.gd` 及根目录转发入口 `tests/test_item_stack_contract.gd`，覆盖 18 个断言。
- 未切换任何生产 `.tres/.res/.tscn`，未修改 Inventory、Shop、Crafting、Equipment、LootTable 或 C# ItemStack 消费者；综合 `reusable_gathering` 测试主体恢复原状。

### C# / GDScript 兼容边界与回滚点

- GDScript 实现不声明 `class_name`，避免与仍在运行的 C# `ItemStack` 类型冲突；`Item` 继续保存原始 Resource 引用，不包装或复制物品资源。
- `ItemStack` 通过 `item_data_compat.gd` 读取 `ActualMaxStackSize/MaxStackSize`，因此旧 C# ItemData 与并行 GDScript ItemData 使用同一上限协议。
- `OnStackChanged` 以同名 GDScript 信号表达 C# 事件的通知边界；装备洗炼读取兼容的 `AttributeBonuses` 字段，整数范围仍为闭区间随机。
- 回滚点为删除 `resources/item/item_stack.gd`、独立测试脚本和入口，不涉及生产资产或存档格式。

### GodotAI 验证结果

- `filesystem_manage(scan)` 完成；`script_manage(find_symbols)` 解析 ItemStack 为 `RefCounted`，8 个函数、1 个 `OnStackChanged` 信号。
- `test_run(suite="item_stack_contract")`：1/1 通过，18 个断言；`test_run(suite="reusable_gathering")`：24/24 通过，0 失败。
- 主场景 `res://scenes/Main.tscn` 延迟进入 `helper_live=true`；以运行前编辑器日志游标 102 增量读取，新增编辑器错误为 0，游戏错误过滤为空。
- 编辑器 Debugger 中仍显示本批第一次测试编辑产生的两条旧解析记录，但当前脚本诊断为空、独立套件和综合套件均已重新加载并通过；该旧行不代表当前运行新增错误。

### 旧路径、资产和编码检查

- 生产 `.tres/.res/.tscn` 没有引用 `item_stack.gd`；旧 `ItemStack.cs` 路径扫描未发现新增资源或场景依赖。
- 本批新增脚本、测试和既有测试文件均通过严格 UTF-8 解码。
- `CUSGA.dll`（04:38:58）晚于 `BoardCardState.cs`（04:38:04），此前 `Godot.Resource` 修复已进入程序集；本批未改变该 C# 文件。

### 遗留风险与下一步

- ItemStack 仍只是并行实现，尚未接入 C# Inventory/Equipment/Board/ Loot 链；在这些消费者迁移前不得切换物品资产脚本。
- 下一批应单独盘点并迁移 `InventoryComponent` / `ItemStack` 的跨语言运行边界，先定义槽位数组、对象身份、容量和信号兼容方案，再考虑生产物品资产切换。
- 继续保留 `ItemStack.cs`、全部 C#、`.csproj`、`.sln` 和 C# Autoload；Crafting、Autoload、战斗与 `AttributeModifierData` 不在本批范围。

## 2026-09-19 14:09：InventoryComponent 物品链并行边界

### 本批范围

- 新增 `entities/components/inventory_component.gd`，并行复刻固定槽位、容量扩展、堆叠合并、交换、复制、排序、标签查询、原子移除、跨库存移动和 `InventoryChanged` 信号语义。
- 新增 `entities/components/warehouse_inventory_component.gd` 与 `entities/components/battle_deck_component.gd`，分别保留仓库拖拽来源标识，以及技能卡过滤、自动扩容和尾部空槽规则。
- 新增 `tests/godot/test_inventory_component_contract.gd`、`tests/test_inventory_component_contract.gd` 和 `tests/godot/inventory_skill_card_probe.gd`，覆盖 26 个断言。
- 未修改生产 `.tscn`、`.tres`、`.res`；玩家、全局仓库和 C# UI 继续挂载旧 C# 组件，未进入 Crafting、Equipment、Shop 或 Autoload 切换。

### C# / GDScript 兼容边界与回滚点

- 新脚本不声明 `class_name`，避免与 C# `InventoryComponent`、`WarehouseInventoryComponent`、`BattleDeckComponent` 全局类型冲突；生产节点仍由 C# 组件提供运行时行为。
- GDScript 槽位保存并行 `item_stack.gd` 实例，但 Item Resource 始终保存原始引用；旧 C# `ItemData` 可经 `item_data_compat.gd` 直接作为输入，身份相等和堆叠上限规则不变。
- `InventoryChanged`、`Slots`、`Capacity`、`AddItem`、`TryRemoveItems`、跨库存移动及出战卡组 `GetSkillCards` 是后续 UI/Crafting 迁移使用的稳定边界。
- 回滚点为删除三份组件、技能卡探针和独立测试，不影响现有生产资源、场景结构或存档格式。

### GodotAI 验证结果

- 会话 `cusga@719b375e9263c636`（Godot 4.7.1）停止播放后恢复 `ready`；`filesystem_manage(scan)` 完成，`battle_deck_component.gd` 通过 `script_create` 重新加载且诊断为空。
- `test_run(suite="inventory_component_contract")`：4/4 通过，共 26 个断言。
- `test_run(suite="item_stack_contract")`：1/1 通过，共 18 个断言；`test_run(suite="reusable_gathering")`：24/24 通过。
- 主场景延迟轮询后进入 `helper_live=true`；当前游戏日志只有 GodotAI helper 注册信息，无新增 SCRIPT ERROR、Parse Error、Failed to load script、资源加载、信号或类型错误。编辑器日志中的物品图标 UID 警告属于既有资产警告；先前重复常量解析记录为缓存残留，重载后未再次出现在当前日志尾部。

### 旧路径、资产和编码检查

- 相关新增脚本、测试、进度文档和反馈文档严格 UTF-8 解码通过（10 个文件）。
- 生产 `scenes/player_scenes/player.tscn`、`core/autoloads/global_warehouse.tscn` 仍引用 `InventoryComponent.cs`、`BattleDeckComponent.cs` 和 `WarehouseInventoryComponent.cs`；这是本批刻意保留的兼容输入，不是遗漏的生产切换。
- `ItemStack.cs`、Inventory/Equipment/Shop/Crafting C# 消费者和全部 `.csproj`、`.sln`、C# Autoload 均保留；未执行批量资源脚本替换。

### 遗留风险与下一步

- GodotAI 测试接口仍提示 preload cache warning；生产边界切换前必须由开发者完整重启编辑器，再复跑本批测试和相关场景。
- 开发者本地 C# Build 仍报告旧版 `BoardCardState.cs` 的四处 `CS0246: Resource`，但当前磁盘唯一源码四处均为 `Godot.Resource`，GodotAI 类型表和运行日志没有对应新错误；需开发者重新加载文件/清理 IDE 构建输入后确认，期间不重复修改该文件。
- 下一批应先选择独立的 StartingStats 或继续物品链消费者边界，单独设计 C# / GDScript 兼容协议；不得把 Crafting、Autoload、战斗或 `AttributeModifierData` 混入本批。

## 2026-09-19 14:48：EquipmentData 与 ToolData Resource 并行迁移

### 本批范围

- 新增 `resources/item/equipment/equipment_data.gd`，保留 `ValidSlots`、`SetType`、`AttributeBonuses`、`GrantedTags` 字段；装备默认 `MaxStackSize=1`。
- 新增 `resources/item/tool/tool_data.gd`，保留 `TargetGatheringTag`、`YieldGrowth`、`GatheringTimeReduction` 字段和原默认值。
- 将装备、工具字段契约合并进 `tests/godot/test_reusable_gathering_interaction.gd`，移除因 GodotAI 发现器缓存导致无法注册的独立测试入口；不重复已完成迁移。

### C# / GDScript 兼容边界与回滚点

- 新 Resource 不声明 `class_name`，避免与 `EquipmentData.cs`、`ToolData.cs` 全局类型冲突；生产 `Ax.tres`、`Pickaxe.tres`、EquipmentComponent、ItemStack 和 C# UI 仍使用旧 C# 类型。
- 字段名、装备槽位整数值 `0..15`、套装整数值 `0..3`、默认堆叠值和采集参数保持原序列化协议；`AttributeBonuses` 使用通用 Dictionary，暂不绑定未完成的 `AttributeModifierData`。
- 回滚点为删除两份并行 GDScript 与新增测试段，不涉及生产资产、场景、存档或节点结构。

### GodotAI 验证结果

- 会话 `cusga@dcd1033051d34c32`，Godot 4.7.1，`filesystem_manage(scan)` 完成；新脚本和合并后的测试 `script_manage.find_symbols` 成功。
- `test_run(suite="reusable_gathering")`：27/27 通过；`inventory_component_contract`：4/4；`item_stack_contract`：1/1。
- `project_run(mode="main")` 初始等待窗口未立即返回 live，随后 `editor_manage(state)` 确认 `helper_live=true`；游戏日志仅有 helper 注册信息，编辑器增量日志无新增错误。

### 旧路径与 UTF-8 检查

- 生产资源扫描仍只发现 `items/tool/Ax.tres`、`items/tool/Pickaxe.tres` 指向 `ToolData.cs`，属于本批刻意保留的 C# 兼容输入；未新增旧路径引用。
- 新脚本、测试、反馈文档和进度文档均通过严格 UTF-8 解码。
- 独立 `test_equipment_resource_contract.gd` 入口已删除，避免测试发现器将其缓存为 abstract/broken；装备断言已由稳定的 `reusable_gathering` 套件执行。

### 遗留风险与下一步

- 开发者本地 C# Build 仍报告旧版 `core/board/BoardCardState.cs` 四处 `CS0246: Resource`；当前磁盘唯一源码四处均为 `Godot.Resource`，GodotAI 类型表和运行日志无对应错误。此为外部 IDE/构建输入缓存阻塞，必须由开发者重新加载文件并重新 Build 后确认；不再重复修改该文件。
- 生产装备/工具资产尚未切换，EquipmentComponent、Crafting、Autoload、战斗和 `AttributeModifierData` 仍未迁移。外部构建阻塞确认前暂停扩展下一批。

## 2026-09-19 15:10：BoardCardState 绝对类型限定与新程序集复验

### 本批范围

- 针对开发者再次反馈的四处 `CS0246: Resource`，仅将 `BoardCardState.CardData`、`LootBoardCardState.CardData`、`TerrainBoardCardState.TerrainData/CardData` 从 `Godot.Resource` 加固为 `global::Godot.Resource`。
- 未修改棋盘逻辑、序列化字段、资源资产、场景、数值或迁移批次；本次修改同时产生新的 C# 源码时间戳，用于排除旧构建输入。

### C# / GDScript 兼容边界

- 四个公开属性继续以 Godot 原生 `Resource` 承载 C# 与 GDScript 卡牌数据，返回对象、空值约束和调用行为均不变。
- `global::` 只要求编译器从根命名空间解析 `Godot.Resource`，消除局部命名空间遮蔽可能，不改变 CLR 类型或运行时协议。

### GodotAI 验证结果

- 会话 `cusga@dcd1033051d34c32` 为 `ready`；`filesystem_manage(read_text)` 确认四处均为 `global::Godot.Resource`，扫描完成。
- 源码时间为 15:08:31，新 `CUSGA.dll/PDB` 时间为 15:09:25，证明编辑器已经针对本次源码生成新程序集，而非继续沿用 04:38 的旧二进制。
- 主场景启动调用等待超时后，延迟状态确认进入 `helper_live=true`；本轮增量编辑器日志为 0 条，游戏日志仅有 GodotAI helper 注册信息。
- `reusable_gathering` 聚焦套件 27/27 通过；后续新增的 8 条日志均为既有物品图标 UID 回退警告，没有 `CS0246`、C# 编译错误、SCRIPT ERROR、Parse Error 或资源加载失败。

### 旧路径、编码与下一步

- `BoardCardState.cs` 已无未限定的 `Resource` 类型声明；本批没有新增或修改任何 `.tres/.res/.tscn` 路径。
- 相关源码和文档继续执行严格 UTF-8 检查。开发者 IDE 若仍显示 `(40,12)`，该位置在当前文件中为空行，可直接判定为旧错误列表；Godot 编辑器本次实际构建已成功。
- C# 构建阻塞解除。下一批继续盘点 EquipmentData/ToolData 生产资产切换与 `ItemStack`、`EquipmentComponent` 消费边界，不把 Crafting、Autoload、战斗或 `AttributeModifierData` 混入。

## 2026-09-19 15:17：EquipmentSetData 与 SetBonusTier 并行 Resource

### 本批范围

- 完成 EquipmentData/ToolData 生产资产切换盘点：`Ax.tres`、`Pickaxe.tres` 不能直接改脚本，因为 C# `ItemStack.Item/SetItem` 仍要求 `ItemData`，`EquipmentComponent` 仍以 `is EquipmentData/ToolData` 读取槽位、标签、套装和采集增益。
- 因强切两个工具资产会被迫同时改造 ItemStack、Inventory、Shop、EquipmentComponent 和 Debug 生成链，本批未扩大该高耦合范围。
- 新增 `resources/item/equipment/set_bonus_tier.gd` 与 `equipment_set_data.gd`，等价保留需求件数、属性加成、标签、套装类型和分级列表；在 `reusable_gathering` 中增加聚焦契约测试。

### C# / GDScript 兼容边界与回滚点

- 两个新 Resource 不声明 `class_name`，避免与 C# `SetBonusTier`、`EquipmentSetData` 全局类型冲突；`Tiers` 使用 `Array[Resource]`，可在后续生产边界中同时承载旧 C# 和新 GDScript tier。
- `SetType` 继续保存 `EquipmentSet` 的整数值 0..3；`AttributeBonuses` 使用通用 Dictionary 保留 AttributeType 整数键和 float 值；标签顺序和 Resource 引用身份不变。
- 当前仓库没有这两个类型的生产 `.tres/.tscn` 实例，`EquipmentComponent.cs` 继续使用旧强类型，回滚点为删除两份新脚本、UID 和新增测试段。

### GodotAI 验证结果

- 会话 `cusga@dcd1033051d34c32` 扫描完成；`find_symbols` 确认 `set_bonus_tier.gd` 有 3 个导出字段，`equipment_set_data.gd` 有 2 个导出字段，均继承 Resource 且无同名全局类。
- 新增聚焦测试 1/1 通过，共 12 个断言；完整 `reusable_gathering` 为 28/28 通过、0 失败。
- 主场景延迟进入 `helper_live=true`；本轮编辑器增量日志为 0 条，游戏日志仅有 GodotAI helper 注册信息。测试产生的 8 条物品图标 UID 回退警告为既有资产问题。

### 旧路径、编码与遗留风险

- 新脚本路径只出现在聚焦测试中，没有生产资产误切换；旧 C# 文件继续作为 `EquipmentComponent` 的兼容类型保留，运行时资产中不存在需要替换的 `SetBonusTier.cs`/`EquipmentSetData.cs` 路径。
- 两份新脚本和测试文件严格 UTF-8 解码通过；Godot 自动生成两份 `.gd.uid` 旁车。
- 工具生产资产仍被 ItemStack/Inventory/Shop/Equipment 强类型链阻塞。下一批应单独迁移 EquipmentComponent 的运行时兼容读取，或继续迁移物品链调用方；不得仅替换 `Ax.tres` / `Pickaxe.tres`。

## 2026-09-19 15:22：装备与工具 Resource 兼容读取桥

### 本批范围

- 新增 `resources/item/equipment/equipment_data_compat.gd`，统一识别并读取 C# / GDScript EquipmentData 与 ToolData 的槽位、套装、属性、标签、采集标签、额外产量和时间减免。
- 扩展 `reusable_gathering` 套件，用并行 GDScript ToolData、旧 C# `Ax.tres` 和普通 C# ItemData 同时验证正向读取与误判保护。
- 未修改 ItemStack、Inventory、EquipmentComponent、Shop、生产资产或场景引用。

### C# / GDScript 兼容边界与回滚点

- 兼容桥只读稳定字段，不创建包装 Resource、不复制物品对象；槽位和套装统一输出旧枚举整数值，标签统一为 StringName 数组。
- 属性 Dictionary 返回深复制，避免运行时消费者误改 Resource 配置；额外产量和时间减免保留原始整数，由 EquipmentComponent 决定是否限制负值。
- 普通 ItemData 必须先通过装备字段集合检查，不能因拥有基础物品字段而被误判为装备或工具。回滚点为删除 helper、UID 和新增测试段。

### GodotAI 验证结果

- `filesystem_manage(scan)` 完成；`find_symbols` 确认 helper 继承 RefCounted 并包含 11 个函数，测试套件已发现新增方法。
- 新增聚焦测试 1/1 通过，共 18 个断言；完整 `reusable_gathering` 为 29/29 通过、0 失败。
- 主场景延迟进入 `helper_live=true`；本轮编辑器增量日志为 0 条，游戏日志只有 helper 注册信息。测试的 8 条旧图标 UID 警告未由本批触发。

### 旧路径、编码与遗留风险

- helper 路径目前只由测试加载；生产 `Ax.tres`、`Pickaxe.tres` 仍按兼容边界引用 `ToolData.cs`，扫描结果精确为这两处。
- helper 与测试严格 UTF-8 解码通过，Godot 已生成 `equipment_data_compat.gd.uid`。
- 下一批可在不切生产场景的前提下迁移 EquipmentComponent 并行实现，使用本桥接受两种 Resource；C# UI 的事件订阅与 out 参数仍是最终生产切换前必须单独处理的边界。

## 2026-09-19 15:25：EquipmentSlot 与 EquipmentSet 枚举协议

### 本批范围

- 新增 `core/constants/equipment_types.gd`，完整保留 C# `EquipmentSlot` 的 16 个整数值和 `EquipmentSet` 的 4 个整数值。
- 扩展 `reusable_gathering` 套件验证关键槽位及全部套装值；未修改任何生产 Resource、场景、C# 枚举或调用方。

### C# / GDScript 兼容边界与回滚点

- 新脚本不声明 `class_name`，由后续 GDScript 装备组件显式 preload；C# 运行时继续使用 `EquipmentTypes.cs`。
- 两边共享已经序列化的整数协议：槽位 0..15、套装 0..3。不得插入或重排枚举成员。
- 回滚点为删除 `equipment_types.gd`、UID 和新增测试段，不影响现有资产与 C# 类型。

### GodotAI 验证结果

- GodotAI 扫描和脚本符号读取成功；新增枚举聚焦测试 1/1 通过，共 14 个断言。
- 完整 `reusable_gathering` 为 30/30 通过、0 失败。
- 主场景延迟进入 `helper_live=true`；编辑器增量日志为 0 条，游戏日志仅有 helper 注册信息；停止后编辑器恢复 `ready`。

### 旧路径、编码与下一步

- 新脚本路径目前只由测试加载；`EquipmentTypes.cs` 和 UID 继续作为 C# 兼容输入保留，没有运行时资产脚本路径需要替换。
- 新脚本和测试严格 UTF-8 解码通过，Godot 已生成 `.gd.uid` 旁车。
- 下一批使用该枚举协议与 `equipment_data_compat.gd` 实现并行 EquipmentComponent；生产玩家场景仍保持 C# 脚本，直到 C# UI 边界单独迁移。

## 2026-09-19 15:35：EquipmentComponent 并行运行时迁移

### 本批范围

- 新增 `entities/components/equipment_component.gd`，等价实现装备/卸下、库存交换、最佳槽位、装备槽交换、洗炼属性、GrantedTags、套装阶级、采集增益、时间减免和火把倍率。
- 更新现有 `inventory_component_contract`，新增旧 C# 斧头、新 GDScript 工具、属性/标签/两件套、火把与魔法物品兜底、双戒指快速装备/交换/卸下覆盖。
- 在现有 `design.md` / `implement.md` 固化生产切换限制；未修改玩家场景、C# UI、生产工具资产或任何存档格式。

### C# / GDScript 兼容边界与回滚点

- 新组件通过 ItemStack 的稳定属性/方法协议接受 C# 或 GDScript 堆叠，通过 `equipment_data_compat.gd` 读取两种语言的装备和工具 Resource；装备时仍复制堆叠但保留同一 Item Resource 身份。
- `EquipmentChanged`、PascalCase 方法名、拖拽来源标识与数值规则保持不变。C# `TryGetEquippedStack(slot, out stack)` 在 GDScript 侧等价为返回堆叠或 null，因此 C# UI 未迁移前不能切换生产节点。
- AttributeComponent/TagComponent 继续按同级节点和稳定方法名结算；套装效果每次先完整撤销旧 tier，再按当前件数重算，保持旧顺序。
- 回滚点为删除 `equipment_component.gd`、UID 和新增测试段；`EquipmentComponent.cs` 与 `player.tscn` 完整保留。

### GodotAI 验证结果

- GodotAI 扫描完成；`find_symbols` 确认新组件继承 Node，包含 2 个导出字段、35 个函数和 `EquipmentChanged` 信号。
- 四项装备聚焦测试分别通过：旧/新工具 18 个断言，属性与套装 11 个断言，标签槽位兜底 7 个断言，库存/双戒指 12 个断言。
- 完整 `inventory_component_contract`：8/8；`item_stack_contract`：1/1；`reusable_gathering`：30/30，全部 0 失败。
- 主场景重新启动并延迟进入 `helper_live=true`；编辑器增量日志为 0 条，游戏日志仅有 helper 注册信息。

### 旧路径、编码与遗留风险

- 新组件路径只由聚焦测试和设计文档引用；生产旧路径扫描精确保留 `scenes/player_scenes/player.tscn` 对 `EquipmentComponent.cs` 的一处引用。
- 新组件、测试和设计文档严格 UTF-8 解码通过，Godot 已生成 `.gd.uid` 旁车。
- 生产切换仍需要单独迁移 `InventoryUI`、`EquipmentSlotUI`、`SlotUI` 的强类型事件/out 参数边界，并确认 Player 暴露的 Equipment 类型；不得仅替换 player 场景脚本。下一批优先盘点装备 UI 边界或继续迁移 ItemStack 的 C# 消费者。

## 2026-09-19 15:45：DraggableData 拖拽载荷并行迁移

### 本批范围

- 新增 `core/ui/draggable/draggable_data.gd`，等价保留 `SourceSystem`、`FromIndex`、`SourceInventory`、`SourceEquipment`、`FromEquipmentSlot`、`HeldStack` 六个字段及默认值。
- 扩展 `tests/godot/test_inventory_component_contract.gd`，验证默认来源、库存与装备组件引用、Ring2 整数值和 ItemStack 引用身份。
- 本批只迁移数据载荷，不复制库存或装备移动规则，不修改生产 UI、场景、Resource 或存档格式。

### C# / GDScript 兼容边界与回滚点

- 新脚本不声明 `class_name`，避免与 C# `DraggableData` 全局名称冲突；来源组件使用 `Node`，允许承载旧 C# 或并行 GDScript Inventory/Equipment 组件，堆叠使用共同基类 `RefCounted`。
- `SourceSystem` 默认值继续为 `&"SystemInventory"`，`FromIndex` 和 `FromEquipmentSlot` 默认值继续为 0；装备槽通过既有整数协议传递，不改变枚举序列化。
- `SlotUI.cs`、`EquipmentSlotUI.cs` 仍创建和识别旧 C# `DraggableData`，这是生产 C# UI 尚未迁移时的必要兼容输入；新脚本目前只由聚焦测试加载。
- 回滚点为删除新脚本、UID 和新增测试段，不影响生产节点或旧 C# 运行路径。

### GodotAI 验证结果

- 会话 `cusga@dcd1033051d34c32`（Godot 4.7.1）扫描完成；新载荷脚本符号解析为 `RefCounted`，测试套件成功发现新增测试。
- DraggableData 聚焦测试：1/1 通过，共 12 个断言；完整 `inventory_component_contract`：9/9，`item_stack_contract`：1/1，`reusable_gathering`：30/30，全部 0 失败。
- 主场景第 12 次运行延迟进入 `helper_live=true`；启动后的编辑器增量日志为 0 条，游戏日志只有 GodotAI helper 注册信息，停止后编辑器恢复 `ready`。
- `reusable_gathering` 的 8 条物品图标 UID 回退警告属于既有资产问题，未由本批触发；没有新增 SCRIPT ERROR、Parse Error、Failed to load script、资源加载、信号或类型错误。

### 旧路径、编码与遗留风险

- `draggable_data.gd` 与更新后的聚焦测试严格 UTF-8 解码通过；新脚本路径只出现在聚焦测试中。
- 旧 `DraggableData.cs` 只由 `SlotUI.cs`、`EquipmentSlotUI.cs` 等 C# UI 继续使用，符合本批兼容边界；未发现生产 `.tres/.res/.tscn` 误引用新脚本。
- `EquipmentSlotUI` 仍同时依赖 C# `DraggableData`、`EquipmentComponent`、`ItemStack` 和 `ItemTooltipPresenter`，`InventoryUI` 还以强类型实例化它，因此不能直接切换生产场景。
- 下一批先单独迁移并行 `ItemTooltipPresenter`，以 `item_data_compat.gd` 读取新旧物品；随后再迁移 `EquipmentSlotUI`。在 InventoryUI 迁移前不得切换 `EquipmentSlotUI.tscn`，且继续不触碰 `AttributeModifierData`。

## 2026-09-19 15:55：ItemTooltipPresenter 并行迁移

### 本批范围

- 新增 `core/ui/item_tooltip_presenter.gd`，等价保留立即显示、隐藏、空值/空堆叠隐藏、名称回退、缺省描述和共享空 Presenter 行为。
- 扩展 `tests/godot/test_inventory_component_contract.gd`，用旧 C# ItemData + ItemStack 和新 GDScript ItemData + ItemStack 覆盖跨语言展示输入。
- 本批未修改 TooltipPanel 场景和脚本，未切换 `InventoryUI`、`WarehouseUI`、`SlotUI` 或 `EquipmentSlotUI`。

### C# / GDScript 兼容边界与回滚点

- C# 的 `Show(ItemStack)` / `Show(ItemData)` 重载合并为 GDScript `Show(Variant)`；直接 Resource 与堆叠最终都解析为原始 ItemData Resource，不包装、不复制。
- 物品名称和描述经 `item_data_compat.gd` 读取，纯空白 DisplayName 继续回退 CardName，空白描述继续显示 `暂无描述`；提示框方法名保持 `show_tooltip_now` / `hide_tooltip`。
- C# ItemStack 的非导出公共属性可由 `Object.get()` 读取，但不会完整列入 `get_property_list()`；因此使用新旧实现共同拥有的 `Clear`、`SetItem`、`Duplicate` 方法协议识别堆叠，再读取 `IsEmpty` / `Item`。
- 新脚本不声明 `class_name`；旧 `ItemTooltipPresenter.cs` 和四个 C# UI 消费者继续作为生产兼容输入。回滚点为删除新脚本、UID 和新增测试段。

### GodotAI 验证结果

- 扫描完成；`find_symbols` 确认新 Presenter 继承 RefCounted，包含 `Empty`、`Show`、`Hide` 等 8 个函数，测试套件发现新测试。
- 聚焦测试：1/1 通过，共 7 个断言；完整 `inventory_component_contract`：10/10，`item_stack_contract`：1/1，`reusable_gathering`：30/30，全部 0 失败。
- 首轮测试暴露 `.tres` 在该测试缓存中降级为普通 Resource 后不能传入强类型 C# `ItemStack.SetItem(ItemData)`；改为直接实例化当前 `ItemData.cs` 测试夹具，并修正 C# 属性表识别后复验通过，未放宽生产类型。
- 主场景第 13 次运行延迟进入 `helper_live=true`；启动后的编辑器增量日志为 0 条，游戏日志只有 GodotAI helper 注册信息，停止后恢复 `ready`。
- 修复后日志没有新增 SCRIPT ERROR、Parse Error、Failed to load script、资源加载、信号或类型错误；完整可重复采集套件仍只有 8 条既有图标 UID 回退警告。

### 旧路径、编码与遗留风险

- 新 Presenter 与测试严格 UTF-8 解码通过，Godot 已生成 `.gd.uid`；新路径只由自身空对象构造和聚焦测试引用，没有生产 `.tres/.res/.tscn` 引用。
- 旧 C# Presenter 仍由 `InventoryUI.cs`、`WarehouseUI.cs`、`SlotUI.cs`、`EquipmentSlotUI.cs` 使用，符合本批生产兼容边界。
- 下一批可并行迁移 `EquipmentSlotUI`，接受 GDScript DraggableData、动态 EquipmentComponent / ItemStack 和新 Presenter；但 `InventoryUI.cs` 仍以 `PackedScene.Instantiate<EquipmentSlotUI>()` 强类型实例化，因此迁移后仍不得切换 `EquipmentSlotUI.tscn`。

## 2026-09-19 16:04：EquipmentSlotUI 并行迁移

### 本批范围

- 新增 `core/ui/equipment_slot_ui.gd`，等价实现装备槽绑定、16 个槽位文案、图标/数量刷新、提示框、拖拽预览、放置判断和实际放置委托。
- 扩展 `tests/godot/test_inventory_component_contract.gd`，用与生产场景相同的三个必需节点路径构造轻量装备槽 UI。
- 本批不修改 `scenes/inventory/EquipmentSlotUI.tscn`、`inventory_ui.tscn` 或 `InventoryUI.cs`，生产 UI 仍走完整 C# 链。

### C# / GDScript 兼容边界与回滚点

- 新 UI 使用新 DraggableData 生成 `SystemEquipment` 载荷，并按脚本路径同时识别旧 `DraggableData.cs` 与新 `draggable_data.gd`；来源组件、槽位和 HeldStack 引用不包装、不复制。
- `CanEquipFromInventory` / `EquipFromInventory` 与 `CanMoveEquipment` / `MoveEquipment` 仍是唯一合法性和状态修改入口，UI 不复制装备规则。
- C# `TryGetEquippedStack(slot, out stack)` 不能直接从 GDScript 调用；新 UI 支持无 out 的 `GetEquippedStack(slot)` 桥和当前 GDScript 单参数 `TryGetEquippedStack(slot)`。在生产组件边界切换前，旧 C# UI 必须保留。
- `EquipmentChanged` 后的所有槽位重绑仍由外层 InventoryUI 负责；单槽只订阅 GDScript ItemStack 的 `OnStackChanged` 做视觉刷新。回滚点为删除新脚本、UID 和新增测试段。

### GodotAI 验证结果

- 扫描完成；`find_symbols` 确认新 UI 继承 PanelContainer，包含 22 个函数；测试套件发现两项新聚焦测试。
- 视觉与拖拽载荷测试：1/1 通过，共 17 个断言；放置委托测试：1/1 通过，共 6 个断言。
- 完整 `inventory_component_contract`：12/12，`item_stack_contract`：1/1，`reusable_gathering`：30/30，全部 0 失败。
- 初次放置委托测试漏掉生产 `EquipmentChanged → InventoryUI.RebindEquipmentSlots()` 步骤，孤立视图仍保存空槽快照；测试补充显式重绑后通过，没有把外层职责错误塞进槽位 UI。拖拽预览透明度使用 `is_equal_approx` 验证，避免 Color 分量浮点表示造成假失败。
- 主场景第 14 次运行延迟进入 `helper_live=true`；启动后的编辑器增量日志为 0 条，游戏日志只有 GodotAI helper 注册信息，停止后恢复 `ready`。既有 8 条图标 UID 回退警告未由本批触发。

### 旧路径、编码与遗留风险

- 新 UI 和测试严格 UTF-8 解码通过，Godot 已生成 `.gd.uid`；新路径只由聚焦测试加载。
- `EquipmentSlotUI.tscn` 继续引用 `core/ui/EquipmentSlotUI.cs`；`InventoryUI.cs` 继续持有 `List<EquipmentSlotUI>` 并调用 `Instantiate<EquipmentSlotUI>()`，均为明确兼容输入。
- 下一批可并行迁移普通 `SlotUI`，复用 DraggableData、ItemTooltipPresenter 与动态 Inventory/Equipment 协议；在 `InventoryUI` 和 `WarehouseUI` 迁移前仍不得切换 `SlotUI.tscn` 或 `EquipmentSlotUI.tscn`。

## 2026-09-19 16:12：SlotUI 并行迁移

### 本批范围

- 新增 `core/ui/slot_ui.gd`，等价实现槽位绑定、公开 SlotIndex / Inventory / CurrentStack、等宽尺寸、图标/数量、悬停提示、Shift/Alt 快捷点击和拖放委托。
- 扩展 `tests/godot/test_inventory_component_contract.gd`，用与生产 `SlotUI.tscn` 相同的 ItemIcon / AmountLabel 路径构造轻量槽位。
- 本批不修改 `SlotUI.tscn`、`inventory_ui.tscn`、`warehouse_ui.tscn`、InventoryUI 或 WarehouseUI，生产普通槽位仍使用 C#。

### C# / GDScript 兼容边界与回滚点

- `SlotShortcutKind` 显式固定 ShiftClick=0、AltClick=1；Callable 接收当前槽位和整数类型，Alt 与 Shift 的优先级、左键按下条件保持原实现。
- 新 SlotUI 生成新 DraggableData，并同时识别旧 `DraggableData.cs`；SourceSystem、FromIndex、SourceInventory 与 HeldStack 继续传递原值和引用身份。
- 库存→库存只调用 `CanReceiveItemFrom` / `MoveItemFrom`，装备→库存只调用 `CanUnequipToInventory` / `UnequipToInventory`；不在 UI 内复制容量、堆叠或装备合法性规则。
- 旧 C# ItemStack 没有可连接的 GDScript Godot 信号时仍可在 Bind 中读取；组件变化后的整组重绑由未来 InventoryUI / WarehouseUI 负责。回滚点为删除新脚本、UID 和新增测试段。

### GodotAI 验证结果

- 扫描完成；`find_symbols` 确认新 SlotUI 继承 PanelContainer，包含 22 个函数；测试套件发现两项新聚焦测试。
- 绑定、视觉、快捷与载荷测试：1/1 通过，共 21 个断言；库存移动与装备卸下委托测试：1/1 通过，共 7 个断言。
- 完整 `inventory_component_contract`：14/14，`item_stack_contract`：1/1，`reusable_gathering`：30/30，全部 0 失败。
- 首轮尺寸断言误用请求宽度而非 Godot 布局后的实际 `size.x`，已改为验证原契约 `custom_minimum_size.y == size.x`；装备夹具改用显式 `Array[int]` 后消除动态 set 的类型歧义。
- 主场景第 15 次运行延迟进入 `helper_live=true`；启动后编辑器增量日志为 0 条，游戏日志只有 GodotAI helper 注册信息。启动回包曾引用修复前测试的陈旧 Ring1 错误并明确标记 `may_predate_run=true`；取证后已清空 Debugger 面板 27 条旧记录。

### 旧路径、编码与遗留风险

- 新 SlotUI 和测试严格 UTF-8 解码通过，Godot 已生成 `.gd.uid`；新路径只由聚焦测试加载。
- `SlotUI.tscn` 继续引用 `core/ui/SlotUI.cs`；`InventoryUI.cs` 与 `WarehouseUI.cs` 继续持有 `List<SlotUI>` 并强类型实例化，两个生产场景继续引用旧 SlotUI 场景。
- 下一批应先独立盘点 InventoryUI 或 WarehouseUI 的 GameplayPort、组件事件和生成槽位边界；未完成父 UI 迁移前不得切换两个槽位场景。

## 2026-09-19 16:38：WarehouseUI 并行迁移

### 本批范围

- 新增 `core/ui/warehouse/warehouse_ui.gd`，等价保留仓库请求、玩家/仓库双库存绑定、按容量生成槽位、相同容量节点复用、关闭按钮样式、提示框关闭和退出清理。
- 扩展 `tests/godot/test_inventory_component_contract.gd`，使用生产 `warehouse_ui.tscn` 子树和最小槽位协议验证父面板职责。
- 本批不修改 `scenes/Warehouse/warehouse_ui.tscn`、`SlotUI.tscn`、InventoryUI、生产库存组件或物品资产。

### C# / GDScript 兼容边界与回滚点

- `SlotPrefab`、`GameplayPortPath`、`TooltipPanelPath` 保持原序列化键；`Open` / `Close` 和三项信号名称保持不变。
- GameplayPort 与两种库存组件通过 Node、稳定方法名和 Godot 信号连接，面板不复制移动、堆叠或装备规则；容量不变时重用现有槽位实例。
- GDScript Presenter 不能传给强类型 C# SlotUI，因此生产仓库场景继续完整走 C#。待 InventoryUI 迁移后再统一切换共享 SlotUI 与两个父 UI。
- 回滚点为删除并行脚本、UID 和新增测试段；旧 C# 文件及生产场景未改动。

### GodotAI 验证结果

- 会话 `cusga@de7eb8d975b22612` 扫描完成；`find_symbols` 确认 WarehouseUI 继承 Control，包含 3 个导出字段和 17 个函数，测试套件发现新增用例。
- WarehouseUI 聚焦测试 1/1 通过，共 19 个断言；完整 `inventory_component_contract` 15/15、`item_stack_contract` 1/1、`reusable_gathering` 30/30，全部 0 失败。
- 初始失败来自 McpTestSuite 不是 Node，以及编辑器内非 `@tool` PackedScene 脚本会成为 placeholder；测试改为显式调用并行父面板生命周期，并用内嵌最小槽位协议隔离已独立验证的 SlotUI 视觉行为，生产脚本没有加入测试分支。
- 主场景启动后最终进入 `helper_live=true`；游戏日志只有 GodotAI helper 注册，启动后编辑器增量日志为 0，停止后恢复 `ready`。

### 旧路径、编码与遗留风险

- 新脚本和测试严格 UTF-8 解码通过，Godot 已生成 `.gd.uid`；新脚本路径只由聚焦测试加载。
- `warehouse_ui.tscn` 继续引用 `WarehouseUI.cs`，其 SlotPrefab 继续引用 C# `SlotUI.tscn`，符合本批兼容边界。
- 回归日志只有 1 条 applecore 与 8 条商店物品图标 UID 路径回退警告，均为既有资产问题；没有新增脚本、资源、信号或类型错误。
- 下一批独立迁移 InventoryUI，完成普通槽、装备槽、出战卡组、快捷移动和 GameplayPort 边界后，再统一设计生产 UI 切换。

## 2026-09-19 16:53：InventoryUI 并行迁移

### 本批范围

- 新增 `core/ui/inventory_ui.gd`，等价保留背包开关、玩家背包、装备、出战卡组三组视图、容量增长复用、组件变化重绑、属性摘要、提示框和退出清理。
- 扩展 `tests/godot/test_inventory_component_contract.gd`，用最小 GameplayPort、玩家、装备、属性摘要和两类槽位协议覆盖父面板职责。
- 本批不修改 `scenes/inventory/inventory_ui.tscn`、`SlotUI.tscn`、`EquipmentSlotUI.tscn`、玩家组件、Crafting 或任何生产资产。

### C# / GDScript 兼容边界与回滚点

- `SlotPrefab`、`EquipmentSlotPrefab`、`GameplayPortPath`、`TooltipPanelPath` 保持原序列化键；`Open` / `Close`、InventoryToggleRequested、InventoryChanged 和 EquipmentChanged 名称保持不变。
- 父面板通过 Node、Callable 和稳定方法名接受新旧组件；Shift 单击继续移动单个技能堆叠或交给最佳装备槽，Alt 单击继续在背包与出战卡组之间批量移动技能卡。
- 合成按钮继续先关闭背包，再调用 `RequestOpenCrafting`。AttributeSummaryUI 仅接收玩家引用，未把属性计算搬入父面板。
- 生产 C# InventoryUI 仍强类型实例化 C# SlotUI / EquipmentSlotUI；生产切换必须等待玩家 Inventory、BattleDeck、Equipment 和共享槽位场景同时就绪。回滚点为删除新脚本、UID 和新增测试段。

### GodotAI 验证结果

- `find_symbols` 确认新脚本继承 Control，包含 4 个导出字段和 29 个函数；InventoryUI 聚焦测试 1/1 通过，共 38 个断言。
- 完整 `inventory_component_contract`：16/16；`item_stack_contract`：1/1；`reusable_gathering`：30/30，全部 0 失败。
- 主场景启动后最终进入 `helper_live=true`；游戏日志只有 GodotAI helper 注册。停止运行后分别打开 `inventory_ui.tscn` 和 `Main.tscn` 成功，cursor 37 之后编辑器增量日志为 0 条。
- 测试夹具最初复用了编辑器 placeholder 形式的 C# AttributeSummaryUI，已改为内嵌最小 GDScript 协议；修复后的聚焦、完整套件和主场景冒烟均通过，旧错误未继续出现。

### 旧路径、编码与遗留风险

- `inventory_ui.gd`、UID 和更新后的测试严格 UTF-8 解码通过；未发现 TODO、FIXME 或空 `pass` 实现。
- 新脚本路径只由聚焦测试加载；生产 `inventory_ui.tscn` 继续精确引用 `InventoryUI.cs`、`SlotUI.tscn` 和 `EquipmentSlotUI.tscn`，符合并行兼容边界。
- Alt 批量移动依赖 GDScript Inventory / BattleDeck 的 Callable 协议，C# 强类型父 UI 和玩家组件仍阻止生产切换。下一批继续选择不要求整体切换的独立模块，优先盘点剩余物品 UI/组件消费者；StartingStats、Crafting、Autoload 与 AttributeModifierData 继续单独处理。

## 2026-09-19 17:11：HUD 背包与合成输入桥生产迁移

### 本批范围

- 新增 `core/ui/hud/hud_controller.gd` 和 `core/ui/hud/backpack_button.gd`，等价迁移背包/合成快捷键与背包按钮转发。
- 更新 `scenes/Main.tscn` 的 HUDController 和 BackpackButton 两条脚本资源，保留原节点结构与导出 NodePath 值。
- 扩展 `tests/godot/test_inventory_component_contract.gd`，新增 HUD 输入桥聚焦契约；未修改 GameplayPort、InventoryUI、CraftingUI 或输入映射。

### C# / GDScript 兼容边界与回滚点

- `GameplayPortPath`、`BackpackButtonPath` 序列化键保持不变；`toggle_crafting` 仍由 `_input` 处理，`toggle_inventory` 仍由 `_unhandled_input` 处理。
- 两条键盘路径继续抑制 echo，并先调用 C# GameplayPort 的稳定方法，再执行 `set_input_as_handled`。背包按钮继续只请求切换，不直接引用背包界面。
- `HUDController.cs`、`BackpackButton.cs` 及 UID 保留到最终清理。回滚点为恢复 Main 的两条旧 ext_resource 并删除新脚本、UID 和新增测试段。

### GodotAI 验证结果

- 两个新脚本符号解析成功；HUD 聚焦测试 1/1 通过，共 11 个断言。
- 完整 `inventory_component_contract`：17/17；`item_stack_contract`：1/1；`reusable_gathering`：30/30，全部 0 失败。
- Main 场景强制重载成功，节点运行属性确认原导出 NodePath 未丢失；主场景最终进入 `helper_live=true`。
- 运行时实际触发 Tab 打开 CraftingUI、B 打开 InventoryUI、BackpackButton 关闭 InventoryUI，三条路径均保持原行为。最终游戏日志无错误，编辑器 cursor 58 后仅有 8 条既有商店物品图标 UID 警告。

### 旧路径、编码与遗留风险

- 两个新脚本、UID、Main 和更新后的测试严格 UTF-8 解码通过；新脚本无 TODO、FIXME 或空 `pass`。
- 运行时 `.tscn/.tres/.res/.gd/project.godot` 已无 `HUDController.cs`、`BackpackButton.cs` 旧路径或旧 UID；新 GDScript 路径只由 Main 和聚焦测试引用。
- GameplayPort、HealthBarUI、CraftingUI 与其组件仍为 C# 边界。下一批继续迁移不要求更改核心玩法的叶子 UI；AttributeModifierData 仍保持撤回状态。

## 2026-09-19 17:12：AttributeSummaryUI 并行迁移

### 本批范围

- 新增 `core/ui/attribute_summary_ui.gd`，等价保留五项摘要、八项详情、详情弹窗、Bind、信号刷新和退出清理。
- 新增 `tests/godot/test_attribute_summary_ui_contract.gd` 及 `tests/test_attribute_summary_ui_contract.gd` GodotAI 发现入口。
- 本批不修改 `scenes/inventory/AttributeSummaryUI.tscn`、InventoryUI、AttributeComponent、AttributeModifierData 或任何属性数值。

### C# / GDScript 兼容边界与回滚点

- AttributeType 的 15 个整数值固定为 0..14，与 C# `Attributes.cs` 顺序一致；新 UI 通过 Node、稳定信号名和 `GetEffectiveValue(int)` 接受现有 C# 属性组件。
- 普通数值保持整数或最多一位小数，百分比保持乘 100 后最多一位小数，穿透保持“固定值 | 比率%”；空绑定时全部显示 `-`。
- `AttributeChanged` 与 `AvailablePointsChanged` 都触发完整刷新，重复绑定不重复连接，切换和退出均解除旧信号。
- 生产 C# InventoryUI 仍强类型获取 AttributeSummaryUI，因此生产场景继续引用 `AttributeSummaryUI.cs`。回滚点为删除新脚本、UID 和两个测试入口。

### GodotAI 验证结果

- `find_symbols` 确认新脚本继承 PanelContainer，包含 15 个函数且没有 class_name。
- 独立聚焦套件 3/3 通过，共 38 个断言；`inventory_component_contract` 17/17、`item_stack_contract` 1/1、`reusable_gathering` 30/30，全部 0 失败。
- `AttributeSummaryUI.tscn` 成功打开并解析 36 个节点，随后返回 Main；主场景最终进入 `helper_live=true`。
- 当前游戏日志只有 GodotAI helper 注册，cursor 66 之后编辑器增量日志为 0；8 条物品图标 UID 回退为既有警告。

### 旧路径、编码与遗留风险

- 新脚本、两个测试入口及其 UID 严格 UTF-8 解码通过；未发现 TODO、FIXME 或空 `pass`。
- 新 GDScript 只由聚焦测试引用；生产 `AttributeSummaryUI.tscn` 继续精确引用 `AttributeSummaryUI.cs`，符合并行边界。
- 生产切换仍受 C# InventoryUI 强类型约束。下一批继续迁移可独立切入生产的 HealthBarUI；AttributeModifierData 继续保持撤回状态。

## 2026-09-19 17:24：HealthBarUI 生产迁移

### 本批范围

- 新增 `core/ui/hud/health_bar_ui.gd`、GodotAI 聚焦测试与测试发现入口。
- 更新 `scenes/Main.tscn` 的 HealthBarUI 脚本资源，使生产节点直接使用 GDScript；节点结构、导出路径和生命数值未改动。
- 本批不修改 GameplayPort、HealthComponent、Player、生命公式、战斗或 AttributeModifierData。

### C# / GDScript 兼容边界与回滚点

- `GameplayPortPath` 保持原序列化键与 `../../../../../Gameplay/GameplayPort` 值；新视图继续从现有 C# GameplayPort 读取 `PlayerHealth`。
- 跨语言边界仅依赖 `ValueChanged`、`CurrentValue`、`MaxValue`；UI 只绑定、刷新与清理信号，不承担伤害、恢复或生命上限规则。
- 重复绑定、切换绑定和退出清理保持原时序。旧 `HealthBarUI.cs` 与 UID 保留；回滚点为恢复 Main 的旧 ext_resource 并删除新脚本、UID 和聚焦测试。

### GodotAI 验证结果

- `find_symbols` 已确认新脚本继承 Control，包含 1 个导出字段和 6 个函数；HealthBarUI 聚焦测试 1/1 通过，共 11 个断言。
- `inventory_component_contract` 17/17、`item_stack_contract` 1/1、`reusable_gathering` 30/30，全部 0 失败。
- Main 自定义运行最终进入 `helper_live=true`；运行时生产节点实际加载 `res://core/ui/hud/health_bar_ui.gd`，保留原 GameplayPortPath，并显示 `1000 / 1000`。
- cursor 74 后只有 8 条既有商店物品图标 UID 路径回退警告；没有新增 SCRIPT ERROR、Parse Error、资源加载、信号、类型或运行时错误。

### 旧路径、编码与遗留风险

- 运行时资产已无 `HealthBarUI.cs` 或旧 UID `uid://2pdyihf4cpq1` 引用；旧 UID 只保留在兼容 C# 的 `.uid` 旁车文件。新 GDScript 路径只由 Main 与聚焦测试引用。
- 新实现没有 TODO、FIXME 或空 `pass`；本批文件严格 UTF-8 解码通过。
- GameplayPort 与 HealthComponent 仍为 C#，但边界已缩小为三个稳定成员。下一批继续选择独立叶子 UI 或低耦合组件；StartingStats、物品链、Crafting、Autoload、战斗及 AttributeModifierData 继续分别处理。

## 2026-09-19 17:31：TimePanelUI 生产迁移

### 本批范围

- 新增 `core/ui/hud/time_panel_ui.gd`、GodotAI 聚焦测试与测试发现入口。
- 更新 `scenes/Main.tscn` 的 TimePanelUI 脚本资源，使 HUD 最后一个 C# 叶子视图切换为 GDScript；节点结构和时间数值未改动。
- 本批不修改 TimeSystem、时间流逝、昼夜切换、天数增长、天赋触发或其他 Autoload。

### C# / GDScript 兼容边界与回滚点

- 新视图继续读取 `/root/TimeSystem` 的 `CurrentDay`、`IsNight`、`PhaseProgress`，并订阅原五参数 `TimeChanged` 信号。
- 旧 C# `PhaseLength=100` 是静态常量，无法作为 Autoload 实例属性可靠读取；初始刷新在视图内保留同值兼容常量，信号刷新仍使用 TimeChanged 实参，避免改变动态协议。
- 四个唯一节点路径、三种文本格式、进度条最小值 0 及信号清理时序保持不变。旧 `TimePanelUI.cs` 与 UID 保留；回滚点为恢复 Main 的旧 ext_resource 并删除新脚本、UID 和聚焦测试。

### GodotAI 验证结果

- `find_symbols` 确认新脚本继承 Control，包含 4 个函数且无新增导出字段；TimePanelUI 聚焦测试 1/1 通过，共 14 个断言。
- `inventory_component_contract` 17/17、`item_stack_contract` 1/1、`reusable_gathering` 30/30，全部 0 失败。
- Main 场景节点确认挂载 `res://core/ui/hud/time_panel_ui.gd`；自定义运行首次轮询即进入 `helper_live=true`，生产界面显示“第 1 天”“白天”“0 / 100”，进度条范围和值为 0..100 / 0。
- 首轮扫描发现初始 `_refresh` 多传了未展示的 TotalTimePassed，已删除该实参；修复后重新扫描、聚焦测试、相关回归和主场景均通过。cursor 85 后只有 8 条既有商店物品图标 UID 路径回退警告，没有新增错误。

### 旧路径、编码与遗留风险

- 运行时资产已无 `TimePanelUI.cs` 或旧 UID `uid://ti4khreb8ppy` 引用；旧 UID 只保留在兼容 C# 的 `.uid` 旁车文件。新 GDScript 路径只由 Main 与聚焦测试引用。
- 新实现没有 TODO、FIXME 或空 `pass`；本批文件严格 UTF-8 解码通过。
- TimeSystem 仍为 C# Autoload，PhaseLength 的兼容常量需在 TimeSystem 独立迁移时统一来源。下一批转向低耦合非 HUD 模块，不把 StartingStats、Crafting、战斗或 AttributeModifierData 混入。

## 2026-09-19 17:38：LootTable 并行迁移

### 本批范围

- 新增 `resources/loot/loot_table.gd`，等价保留掉落表数组、概率掷骰、数量范围、产量修正和 ItemStack 创建。
- 扩展 `tests/godot/test_reusable_gathering_interaction.gd`，新增并行掉落表聚焦契约；同步修正 LootDrop 的长期职责注释。
- 本批不修改 62 个生产 `.tres/.tscn`、LootComponent、GatheringInteraction、MonsterData、库存组件或掉落事件总线。

### C# / GDScript 兼容边界与回滚点

- `Drops` 保持 `Array[Resource]`，动态读取 `Item`、`DropChance`、`MinAmount`、`MaxAmount`，兼容旧 C# 与新 GDScript LootDrop。
- 掷骰继续使用 `randf() * 100 <= DropChance`，数量继续包含上下界并加 yieldGrowth，最终数量不大于零时跳过。生成堆叠保持原 Item Resource 引用身份。
- 并行实现返回 GDScript ItemStack；现有 C# 消费者仍要求 C# LootTable 与 `Array<ItemStack>`，因此生产资产继续使用旧脚本。回滚点为删除新脚本、UID 和新增测试段。

### GodotAI 验证结果

- `find_symbols` 确认新脚本继承 Resource，包含 1 个导出字段和 4 个函数；LootTable 聚焦测试 1/1 通过，共 8 个断言。
- 完整 `reusable_gathering`：31/31；`item_stack_contract`：1/1；`inventory_component_contract`：17/17，全部 0 失败。
- Main 自定义运行首次轮询即进入 `helper_live=true`；当前运行 44 条日志均为 info，没有脚本、资源、信号、类型或运行时错误。
- cursor 93 后只有 8 条既有商店物品图标 UID 路径回退警告，没有新增错误。

### 旧路径、编码与遗留风险

- 新 GDScript 路径只由聚焦测试加载；62 个生产资产继续引用 `LootTable.cs` / `uid://cauw0y3vbjow1`，符合并行兼容边界。
- 新实现与更新测试没有 TODO、FIXME 或空 `pass`；本批文件严格 UTF-8 解码通过。
- 生产切换仍受 LootComponent、GatheringInteraction、ReusableGatheringInteraction、MonsterData 与 C# ItemStack 约束。下一批可单独迁移 LootComponent 的动态掉落表边界，再评估怪物场景切换；AttributeModifierData 继续保持撤回状态。

## 2026-09-19 17:42：LootComponent 并行迁移

### 本批范围

- 新增 `entities/components/loot_component.gd`、GodotAI 聚焦测试与测试发现入口。
- 等价保留空掉落表短路、额外产量转发和 GlobalEventBus 掉落广播。
- 本批不修改 Monster、怪物场景、LootTable 生产资产、库存或事件总线。

### C# / GDScript 兼容边界与回滚点

- `DropTable` 保持原序列化键但使用通用 Resource，可动态调用旧 C# 或新 GDScript `RollLoot(int)`。
- `TriggerDrop(Vector2, int)` 方法名、`/root/GlobalEventBus` 路径、`on_entity_dropped` 信号名及“位置、堆叠数组”参数顺序保持不变。
- `Monster.cs` 仍以 `GetNodeOrNull<LootComponent>` 强类型持有组件，因此生产 `monster.tscn` 继续引用旧 C#。回滚点为删除新脚本、UID 和两个测试入口。

### GodotAI 验证结果

- `find_symbols` 确认新脚本继承 Node，包含 1 个导出字段和 1 个函数；LootComponent 聚焦测试 1/1 通过，共 7 个断言。
- `reusable_gathering` 31/31、`item_stack_contract` 1/1、`inventory_component_contract` 17/17，全部 0 失败。
- Main 自定义运行首次轮询即进入 `helper_live=true`；当前运行 44 条日志全部为 info，没有脚本、资源、信号、类型或运行时错误。
- cursor 101 后只有 8 条既有商店物品图标 UID 路径回退警告，没有新增错误。

### 旧路径、编码与遗留风险

- 新 GDScript 只由聚焦测试加载；旧 `LootComponent.cs` 与 UID 仅保留在兼容 C# 文件、`Monster.cs` 强类型调用和 `monster.tscn` 脚本/metadata 引用中。
- 新脚本和测试没有 TODO、FIXME 或空 `pass`；本批文件严格 UTF-8 解码通过。
- 生产切换必须先把 Monster 的 Loot 字段和调用降到稳定 Node/方法协议，或整体迁移 Monster；下一批不直接改战斗实体，可继续迁移掉落链的普通数据/消费者边界。AttributeModifierData 继续保持撤回状态。

## 2026-09-19 17:47：FactionComponent 生产迁移

### 本批范围

- 新增 `entities/components/faction_component.gd`、GodotAI 聚焦测试与测试发现入口。
- 修改 `entities/Monster.cs` 的阵营组件字段、节点获取与初始化写入，改为稳定 Node/属性协议。
- 更新 `scenes/monster_scenes/monster.tscn` 的 FactionComponent 脚本和 metadata UID；不修改其他组件、MonsterData 或战斗逻辑。

### C# / GDScript 兼容边界与回滚点

- `Faction` 序列化键保持不变，Hostile=0、PlayerSummon=1、Neutral=2 三个整数值与 C# MonsterFaction 完全一致。
- Monster 仍从 `Components/FactionComponent` 获取同名节点，只把编译期类型降为 Node，并在 Initialize 中写入同值整数。
- 旧 `FactionComponent.cs` 与 UID 保留。回滚点为恢复 Monster 强类型字段/赋值、场景旧 ext_resource 与 metadata，并删除新脚本、UID 和测试入口。

### GodotAI 验证结果

- `find_symbols` / 节点属性确认新脚本继承 Node，导出 `Faction`；聚焦测试 1/1 通过，共 3 个断言。
- `monster.tscn` 成功打开，生产节点脚本为 `res://entities/components/faction_component.gd`，默认值为 0。
- 直接运行怪物场景进入 `helper_live=true`；运行时 C# `Monster.cs` 成功获取 GDScript 子组件，Faction 保持 0，当前 3 条游戏日志全部为 info。
- `reusable_gathering` 31/31 通过；cursor 109 后只有 8 条既有商店物品图标 UID 路径回退警告，没有新增编译、脚本、资源、信号、类型或运行时错误。

### 旧路径、编码与遗留风险

- 运行时资产已无 `FactionComponent.cs` 或旧 UID `uid://qp3tco60xl2b` 引用；旧文件及旁车 UID仅作为最终清理输入保留。
- 新 GDScript 只由怪物场景和聚焦测试引用；没有 TODO、FIXME 或空 `pass`，本批文件严格 UTF-8 解码通过。
- Monster 仍是 C# 高耦合实体，其余组件继续逐个迁移；本批没有改变阵营判断或战斗行为。AttributeModifierData 继续保持撤回状态。

## 2026-09-19 17:50：LootComponent 生产切换

### 本批范围

- 修改 `entities/Monster.cs`，将私有 Loot 字段、节点获取和死亡掉落调用改为 Node / 稳定方法协议。
- 更新 `scenes/monster_scenes/monster.tscn` 的 LootComponent 脚本和 metadata UID，使用已验证的 `entities/components/loot_component.gd`。
- 本批不修改 LootTable 资产、MonsterData、掉落数值、GlobalEventBus 或库存逻辑。

### C# / GDScript 兼容边界与回滚点

- Monster 仍在相同 `_Ready` 时机获取 `Components/LootComponent`，仍在死亡处理的相同位置以 `(GlobalPosition, 0)` 调用 `TriggerDrop`。
- 场景原本没有给 LootComponent 序列化 DropTable，新脚本继续保持 null 并短路；没有把 MonsterData.LootTable 自动赋值引入本批。
- 旧 `LootComponent.cs` 与 UID 保留。回滚点为恢复 Monster 私有强类型字段/调用和场景旧 ext_resource / metadata。

### GodotAI 验证结果

- 怪物场景强制重载后，生产 LootComponent 脚本为 `res://entities/components/loot_component.gd`，DropTable 保持 null。
- LootComponent 聚焦测试 1/1（7 个断言）和 `reusable_gathering` 31/31 全部通过。
- 直接运行怪物场景进入 `helper_live=true`；运行时 C# `Monster.cs` 成功获取 GDScript LootComponent，调用空表路径无错误，当前 3 条游戏日志全部为 info。
- cursor 117 后只有 8 条既有商店物品图标 UID 路径回退警告，没有新增 C# 编译、脚本、资源、信号、类型或运行时错误。

### 旧路径、编码与遗留风险

- 运行时资产已无 `LootComponent.cs` 或旧 UID `uid://dhw7i7lc8tobr` 引用；旧文件及旁车 UID只作为最终清理输入保留。
- 新路径只由怪物场景、UID 和聚焦测试引用；没有 TODO、FIXME 或空 `pass`，本批文件严格 UTF-8 解码通过。
- 既有 MonsterData.LootTable 没有在 Monster.Initialize 中赋给 LootComponent，实际生产掉落可能仍依赖其他未发现的赋值入口；本批为保持迁移等价未擅自修复，后续处理 MonsterData / 掉落链时必须单独确认。

## 2026-09-19 17:54：TagComponent 并行迁移

### 本批范围

- 新增 `entities/components/tag_component.gd`、GodotAI 聚焦测试与测试发现入口。
- 等价保留空标签过滤、叠层增加、逐层移除、归零删除、存在性与层数查询。
- 本批不修改 Player、EquipmentComponent、TagTalentEffect、PassageGuardProbabilityProvider 或玩家场景。

### C# / GDScript 兼容边界与回滚点

- 四个 PascalCase API 与 StringName 参数保持不变；内部 Dictionary 只保存正整数层数，缺失标签层数继续为 0。
- GDScript EquipmentComponent 和 PassageGuard 已通过稳定方法名兼容；C# Player / EquipmentComponent / Provider 仍要求 C# TagComponent，生产场景继续旧脚本。
- 回滚点为删除新脚本、UID 和两个测试入口；旧 `TagComponent.cs` 与所有生产引用不修改。

### GodotAI 验证结果

- `find_symbols` 确认新脚本继承 Node，包含 4 个函数且无导出字段；聚焦测试 1/1 通过，共 10 个断言。
- `inventory_component_contract` 17/17、`reusable_gathering` 31/31，全部 0 失败。
- Main 自定义运行首次轮询即进入 `helper_live=true`；当前运行 44 条日志全部为 info，没有脚本、资源、信号、类型或运行时错误。
- cursor 125 后只有 8 条既有商店物品图标 UID 路径回退警告，没有新增错误。

### 旧路径、编码与遗留风险

- 新 GDScript 只由聚焦测试加载；旧 C# 路径继续出现在 Player、EquipmentComponent、旧 PassageGuard 测试/Provider 和 `player.tscn`，符合并行边界。
- 新脚本和测试没有 TODO、FIXME 或空 `pass`；本批文件严格 UTF-8 解码通过。
- 生产切换需要一起解除 Player、EquipmentComponent、TagTalentEffect 与旧 Provider 的强类型依赖，不能只替换 player 场景节点。AttributeModifierData 继续保持撤回状态。

## 2026-09-19 18:25：TagComponent 生产切换

### 本批范围

- 更新 `entities/Player.cs`、`entities/components/EquipmentComponent.cs`、`resources/talents/TagTalentEffect.cs` 与保留的 `core/map/PassageGuardProbabilityProvider.cs`，解除对具体 C# TagComponent 类型的编译期依赖。
- 更新 `scenes/player_scenes/player.tscn`，让生产玩家的标签节点使用 `entities/components/tag_component.gd`；扩展标签聚焦契约，并让旧 PassageGuard 测试改用新标签脚本。
- 本批没有迁移 Equipment、Talent、PassageGuard 的业务规则，也没有触碰 Vital、Crafting、Autoload、战斗或 AttributeModifierData。

### C# / GDScript 兼容边界与回滚点

- Player 与 EquipmentComponent 仍获取相同节点路径，只把字段类型降为 Node；装备、套装和天赋仍在原调用位置使用 `AddTag` / `RemoveTag`，标签叠层语义不变。
- 旧 C# PassageGuardProbabilityProvider 同时接受旧 C# 与新 GDScript 标签节点，并以 `HasMethod` + `Call("HasTag")` 查询；缺少方法的节点只忽略有标签条件的修正，不产生运行时错误。
- 旧 `TagComponent.cs` 与 UID 保留。回滚点为恢复四个 C# 消费者的强类型成员/调用和玩家场景旧脚本引用；新 GDScript 与契约可以继续作为并行实现保留。

### GodotAI 验证结果

- 编辑器运行 Main 后已刷新 C# 程序集；标签聚焦契约最终 3/3 通过，共 16 个断言，覆盖生产场景引用、旧 C# Provider → GDScript TagComponent 以及缺少 `HasTag` 的安全退化。
- `inventory_component_contract` 17/17、`reusable_gathering` 31/31，全部 0 失败。
- `player.tscn` 强制重载后确认生产标签节点挂载新 GDScript；直接运行玩家场景只有 info 日志。Main 运行进入 `helper_live=true`，最终 44 条运行日志全部为 info。
- 运行时 `game_eval` 确认 Player 属性与生产标签节点为同一对象；C# TagTalentEffect 赋予/清理标签为 1→0，C# EquipmentComponent 装备/卸下标签也为 1→0。cursor 147 后编辑器增量日志为 0。

### 旧路径、编码与遗留风险

- 生产场景、资源、GDScript 与测试已无旧 `TagComponent.cs` 路径或旧 UID；旧路径只剩 C# 文件自身及 `.uid` 旁车，C# 强类型扫描只剩兼容类声明。
- 本批八个相关文本文件严格 UTF-8 解码通过；没有新增 TODO、FIXME 或空 `pass`。PassageGuard 测试中的三个空 `pass` 是既有 FakeMapLittle 夹具，不是本批功能替代。
- 初次聚焦运行命中过编辑器旧 C# 程序集并出现旧签名 InvalidCast；启动 Main 刷新程序集后，新 Node 签名、聚焦测试与运行时链全部通过。下一批继续选择可独立生产切换的普通组件；Vital 族仍需完整强类型消费者设计后单独处理。

## 2026-09-19 18:48：DebugLoadout 生产迁移

### 本批范围

- 新增 `debug_loadout_data.gd`、`debug_item_stack_entry.gd`、`debug_generated_equipment_entry.gd` 与 `debug_loadout_seeder.gd`，等价迁移三个配置 Resource 和启动 Seeder。
- 更新 `default_inventory_loadout.tres` 的三个脚本、通用 Resource 数组和条目 metadata；更新 Main 的 Seeder 脚本引用，并增加独立聚焦契约及已注册测试入口。
- 本批不修改 ItemData、ItemStack、InventoryComponent、BattleDeckComponent、EquipmentComponent、AttributeComponent、Crafting 或战斗规则。

### C# / GDScript 兼容边界与回滚点

- 固定物品条目继续用原 ItemData Resource 创建保留的 C# ItemStack；动态装备继续创建 C# EquipmentData / ToolData，保证三个生产 C# 组件收到原强类型对象。
- Seeder 只依赖 `Components/...` 节点路径、Capacity、GetStackAt、TrySetStackAt、TryClearStackAt、Equip、Unequip、InitialData 和 InitializeWithData；清空、填充和 ApplyOnce 时序未改变。
- 六个旧 null 采集标签归一为空 StringName，仍走普通装备分支。旧四个 C# 文件与 UID 保留；回滚只需恢复默认 `.tres` 的三个旧脚本和 Main 的旧 Seeder 引用。

### GodotAI 验证结果

- DebugLoadout 聚焦入口 4/4 通过；完整 `reusable_gathering` 35/35、`inventory_component_contract` 17/17、`item_stack_contract` 1/1，全部 0 失败。
- `scenes/Main.tscn` 强制重载并自定义运行进入 `helper_live=true`，44 条当前游戏日志全部为 info；cursor 150 后没有新增脚本、资源、信号、类型或运行时错误。
- 运行时确认生产 Seeder/Loadout 脚本路径已切换，背包得到 16 个原顺序条目，出战卡组得到 4 张卡，固定物品 Resource 身份保持不变；动态条目生成 C# EquipmentData / ToolData，装备后物防 151、速度 105。
- 首轮编辑器聚焦测试曾把旧 C# `.tres` 视为基础 Resource 并触发 typed-call InvalidCast；测试改为直接实例化 C# 数据脚本，生产 `.tres` 身份与行为由 Main 游戏态单独验证，没有复制或替换物品 Resource。

### 旧路径、编码与遗留风险

- 生产资产、场景和 GDScript 已无四个旧 DebugLoadout C# 路径或 UID；旧 UID 只剩四个 `.cs.uid` 旁车文件，C# 控制台测试仍可继续使用旧兼容类。
- 本批八个代码、资产和测试文本严格 UTF-8 解码通过；没有新增 TODO、FIXME 或空 `pass`。完整资源回归只产生 8 条既有商店图标 UID 路径回退警告。
- 物品链仍是 C#，因此新条目有意输出 C# ItemStack / ItemData 派生类型；迁移物品链时必须在该单一工厂边界统一切换。下一批继续选择不要求修改 ItemStack 强类型核心的叶子模块。

## 2026-09-19 18:56：AttributeSummaryUI 生产切换

### 本批范围

- 修改 `InventoryUI.cs` 的属性摘要字段、节点获取和 Bind 调用，解除对具体 C# AttributeSummaryUI 类型的编译期依赖。
- 更新 `AttributeSummaryUI.tscn` 根脚本为既有 `attribute_summary_ui.gd`，扩展聚焦测试锁定生产场景引用，并同步测试注释。
- 本批不修改 AttributeComponent、AttributeModifierData、15 个枚举值、任何属性公式/数值、Inventory 操作或 UI 节点结构。

### C# / GDScript 兼容边界与回滚点

- C# InventoryUI 仍从 `%AttributeSummaryUI` 获取相同节点，只通过 `Node.Call("Bind", Player.Attributes)` 传入原 AttributeComponent；打开、关闭和槽位初始化时序不变。
- GDScript 视图继续读取 GetEffectiveValue(int)，订阅 AttributeChanged / AvailablePointsChanged，并保持普通数值、百分比和穿透格式。
- 旧 `AttributeSummaryUI.cs` 与 UID 保留。回滚点为恢复 InventoryUI 的三处强类型代码和场景旧 ext_resource。

### GodotAI 验证结果

- 目标场景强制重载后确认根节点实际挂载 `res://core/ui/attribute_summary_ui.gd`；聚焦套件 4/4 通过，共 41 个断言。
- `inventory_component_contract` 17/17 通过；Main 自定义运行进入 `helper_live=true`，当前 44 条游戏日志全部为 info，cursor 158 后无新增编辑器错误。
- 运行时真实 C# InventoryUI 成功打开并绑定 GDScript AttributeSummaryUI；视图持有同一 AttributeComponent，五维显示为实际数值，关闭后父面板恢复隐藏。

### 旧路径、编码与遗留风险

- 生产场景、资产和运行时调用已无 `AttributeSummaryUI.cs` 或旧 UID；旧内容只剩兼容 C# 文件和 `.cs.uid` 旁车。
- 四个直接修改文本严格 UTF-8 解码通过；没有新增 TODO、FIXME 或空 `pass`。
- AttributeComponent 仍为 C#，但视图边界已经缩小到一个方法和两个信号。AttributeModifierData 继续保持完整撤回状态，除非先确认 C# 类型表刷新成功，否则不得重试。

## 2026-09-19 19:09：共享背包 UI 链生产切换

### 本批范围

- 将 `inventory_ui.tscn`、`SlotUI.tscn`、`EquipmentSlotUI.tscn` 的生产脚本切换到既有 GDScript，并确认 `warehouse_ui.tscn` 生产入口使用 `warehouse_ui.gd`。
- 在 `EquipmentComponent.cs` 增加只读 `GetEquippedStack(EquipmentSlot)`，让 GDScript 装备槽绕过 C# `out` 参数限制；扩展 Inventory 契约锁定四个生产场景脚本引用。
- 本批没有迁移 ItemData、ItemStack、InventoryComponent、BattleDeckComponent、EquipmentComponent 主体、Crafting、Autoload、战斗或 AttributeModifierData。

### C# / GDScript 兼容边界与回滚点

- GDScript 父面板和槽位继续使用原 NodePath、PascalCase 方法及 InventoryChanged / EquipmentChanged 信号，与现有 C# 玩家组件和堆叠对象交互；对象身份、快捷键和拖放规则不变。
- `GetEquippedStack` 只读取现有装备字典并返回原 ItemStack 或 null，不修改装备状态；旧 `TryGetEquippedStack(..., out ...)` 继续供 C# 调用方使用。
- 旧 `InventoryUI.cs`、`WarehouseUI.cs`、`SlotUI.cs`、`EquipmentSlotUI.cs` 与 UID 全部保留。回滚点为恢复四个场景旧脚本引用并移除新增查询桥。

### GodotAI 验证结果

- `inventory_component_contract` 18/18、`attribute_summary_ui_contract` 4/4、`item_stack_contract` 1/1，全部 0 失败。
- Main 实际运行进入 helper live；InventoryUI 打开后背包与卡组容量/槽位数均为 27，装备槽为 16，普通槽脚本为 `slot_ui.gd`，装备槽脚本为 `equipment_slot_ui.gd`，属性组件身份一致。
- C# EquipmentComponent 的新桥返回默认头盔与靴子原堆叠，两个 GDScript 装备槽持有同一对象且图标、中文标签正常；关闭后面板隐藏。
- WarehouseUI 使用真实 `/root/GlobalWarehouse` 打开，两栏各 27 个 `slot_ui.gd` 槽位，玩家与仓库库存引用一致，关闭后隐藏。当前运行 44 条日志无错误；cursor 158 后无新增编辑器错误。

### 旧路径、编码与遗留风险

- 生产场景与资源扫描未发现四个旧 C# UI 路径；旧实现只作为兼容文件和 UID 保留。本批相关文件没有 TODO、FIXME、NotImplemented 或空 `pass`。
- 10 个相关代码、场景和测试文本严格 UTF-8 解码通过。
- 共享背包 UI 已切入生产，但底层 ItemStack、ItemData 与三个玩家组件仍为 C#；下一批应继续选择单一边界模块，不把 StartingStats、物品链、Crafting 或 Autoload 混批。用户报告的 `Resource` 编译错误不再阻断当前编辑器：Main 已加载当前 C# 程序集并成功调用新增桥，但仍不以本地 build 作为验收手段。

## 2026-09-19 19:18：VaultInteraction 生产迁移

### 本批范围

- 新增 `resources/interaction/vault_interaction.gd`，等价迁移密库耗时与操作序列；更新火山地图唯一生产子资源引用。
- 在 `TerrainInteractionExecutor.cs` 的既有 GDScript 操作映射中增加 `enter_vault`，继续复用 `EnterVaultOp` 和 GameplayPort 仓库请求。
- 扩展 `test_reusable_gathering_interaction.gd`，覆盖默认值、顺序、长按第三参数兼容和火山生产场景引用；未迁移 TerrainInteraction 基类、其它交互、GameplayPort、TimeSystem 或仓库组件。

### C# / GDScript 兼容边界与回滚点

- 新 Resource 返回 Dictionary 描述，不直接调用 Autoload 或 UI；C# 执行器仍是唯一运行时编排者，时间操作和仓库操作继续使用原 C# TerrainOp。
- `build_ops` 接受可选第三参数以匹配正耗时长按调用，但有意忽略耗时快照，保持旧 VaultInteraction 使用自身 `TimeCost` 的语义。
- 旧 `VaultInteraction.cs` 与 UID、`EnterVaultOp.cs` 均保留。回滚点为恢复火山场景旧脚本与 metadata，并移除新映射和 GDScript。

### GodotAI 验证结果

- Vault 聚焦测试 2/2 通过；完整 `reusable_gathering` 37/37、`inventory_component_contract` 18/18，全部 0 失败。
- 火山场景可强制打开，新脚本符号确认为 Resource、1 个导出字段和 1 个公开函数；生产子资源脚本路径、`TimeCost=20` 和唯一地形池条目均保持正确。
- Main 实际运行中生成生产密库棋盘卡并长按 2 秒：执行器读取 2 个 GDScript 操作，TimeSystem 总时间 0→20，WarehouseUI 打开且玩家/仓库各生成 27 格；关闭后面板隐藏，测试卡已移除。
- 初次真实长按发现正耗时入口会传第三个快照参数，旧两参数签名产生运行时错误；脚本改为接收但忽略兼容参数后重新启动验证，当前 run 无脚本、资源、信号、类型或运行时错误。

### 旧路径、编码与遗留风险

- 全仓库旧路径扫描只剩 `VaultInteraction.cs.uid` 旁车；生产场景、资源、GDScript 与测试已无旧路径和旧 UID。
- 4 个直接修改文本严格 UTF-8 解码通过；没有 TODO、FIXME、NotImplemented 或空 `pass`。
- 完整资源回归仍产生 8 条既有商店图标 UID 回退，真实火山场景加载产生 1 条既有 `map_volcanic.gd` UID 回退；均未涉及本批替换的密库脚本。下一批继续处理单一叶子边界，Boss/Farming 等交互必须先补齐各自操作映射。

## 2026-09-19 19:26：BossInteraction 生产迁移

### 本批范围

- 新增 `resources/interaction/boss_interaction.gd`，等价迁移 Boss 耗时、MonsterData 引用与三个操作的固定顺序。
- 在 `TerrainInteractionExecutor.cs` 增加 `spawn_monster` 与 `remove_source_card` 映射；更新 Boss 房间和雪原两个生产地图的 Boss 子资源脚本。
- 扩展 `test_reusable_gathering_interaction.gd`，覆盖字段、Monster 引用、第三参数兼容、操作顺序与两个生产引用；未迁移 MonsterData、战斗主体、GameplayPort 或棋盘系统。

### C# / GDScript 兼容边界与回滚点

- 新 Resource 只返回操作描述，不直接发起遭遇、生成怪物或删除卡牌；C# 执行器继续按原顺序实例化 PassTimeOp、MonsterSpawnOpOp 与 RemoveSourceCardOp。
- Monster 继续保存原 C# MonsterData Resource 身份；正耗时入口的第三个快照参数被接收但忽略，保持旧实现读取自身 `TimeCost=20`。
- 旧 `BossInteraction.cs`、UID、`MonsterSpawnOp.cs` 与 `RemoveSourceCardOp.cs` 均保留。回滚点为恢复两个地图场景旧脚本/metadata、移除两个执行器映射和新脚本。

### GodotAI 验证结果

- Boss 聚焦测试 2/2 通过，共 24 个断言；完整 `reusable_gathering` 39/39，全部 0 失败。
- Main 真实 Boss 卡长按后 TimeSystem 总时间 0→20，`EncounterRequested` 发出一次且消息为 `Boss Battle!`，源卡被移除并成功进入战斗。
- 战斗实际生成 1 个怪物，正常进入玩家回合并抽取 5 张牌；当前 Boss run 共 71 条日志，全部为 info。
- cursor 174 后只有完整资源套件的 8 条既有商店图标 UID 回退；未新增脚本、资源、信号、类型、C# 或运行时错误，最终日志基线为 cursor 182。

### 旧路径、编码与遗留风险

- 全仓库旧 Boss 路径扫描只剩 `resources/interaction/BossInteraction.cs.uid` 旁车；两个生产地图、GDScript 与测试已无旧路径和旧 UID。
- 5 个直接修改文本严格 UTF-8 解码通过；没有 TODO、FIXME、NotImplemented 或空 `pass`。
- GodotAI `game_eval` 直接以 `==` 比较 C# Resource / RefCounted 包装会返回 false；生产资源仍加载原 MonsterData，遭遇内容与实际生成 1 个怪物已作为对象传递的运行时证据。本批不据此扩大到 MonsterData 或战斗迁移。

## 2026-09-19 19:39：GatheringInteraction 生产迁移

### 本批范围

- 新增 `resources/interaction/gathering_interaction.gd`，等价迁移一次性采集的耗时、标签、掉落表调用、已采集判断和操作顺序。
- 在 `TerrainInteractionExecutor.cs` 增加 `mark_harvested` 映射；切换 3 个直接地形卡、6 个群系 Profile 和沙漠地图共 10 个生产资产，并同步 `scripts/generated/create_terrain_profiles.gd`。
- 扩展 `test_reusable_gathering_interaction.gd`；未迁移 LootTable、ItemStack、ItemData、Inventory、EncounterManager、Autoload 或战斗系统。

### C# / GDScript 兼容边界与回滚点

- GDScript Resource 继续把原 C# LootTable 作为通用 Resource，并调用稳定 `RollLoot(extraYield)`；掉落对象原样交给执行器，现有 ConvertDrops 与 SpawnLootOp 继续过滤和生成 C# ItemStack 卡牌。
- 操作顺序保持 pass_time、mark_harvested、可选 spawn_loot、check_gathering_encounter、remove_source_card；已采集输入只跳过掉落，正耗时快照参数被接收但忽略。
- 旧 `GatheringInteraction.cs` 与 UID、LootTable、所有 TerrainOp 均保留。回滚点为恢复 10 个资产和生成器旧路径，移除 `mark_harvested` 映射及新脚本。

### GodotAI 验证结果

- 新脚本符号确认为 Resource、3 个导出字段和 1 个公开函数；操作顺序聚焦测试 1/1 通过（17 个断言），生产资产/生成器聚焦测试 1/1 通过（87 个断言）。
- 完整 `reusable_gathering` 41/41 通过，0 失败；新增 8 条警告仍是既有商店物品图标 UID 路径回退，与本批资产无关。
- Main 自定义运行进入 helper live，启动 44 条日志全部为 info。真实受控一次性采集卡通过当前 C# 执行器后，时间 0→1、`IsHarvested=true`、源卡已移除且棋盘无残留；新增 12 条交互日志全部为 info。
- cursor 182 到 190 仅包含上述 8 条既有图标警告，没有新增脚本、资源、信号、类型、C# 编译或运行时错误。

### 旧路径、编码与遗留风险

- 运行时 GDScript、场景、Resource、测试与生成脚本已无旧 `GatheringInteraction.cs` 路径或旧 UID；旧内容只保留兼容 C# 文件及 `.cs.uid` 旁车。
- 本批代码、10 个资产、生成器、测试与新 UID 旁车共 15 个直接文本将执行严格 UTF-8 检查；没有 TODO、FIXME、NotImplemented 或空 `pass`。
- 生产 DropTable 与 ItemStack 仍是 C#，这是有意兼容边界；迁移掉落/物品链时必须统一处理强类型数组与对象身份，不能在本批提前切换。

## 2026-09-19 19:43：FarmingInteraction 并行迁移

### 本批范围

- 新增 `resources/interaction/farming_interaction.gd`，等价迁移农场耗时、占用判断和打开面板操作描述。
- 在 `TerrainInteractionExecutor.cs` 增加 `open_farming_panel` 映射，并扩展 reusable_gathering 聚焦契约。
- 仓库扫描确认没有生产场景或 Resource 引用 FarmingInteraction；本批未修改 TerrainInstance、GameplayPort、农场 UI、地图资产、Autoload 或 Crafting。

### C# / GDScript 兼容边界与回滚点

- GDScript Resource 只读取 `IsOccupied` 并返回操作描述；C# OpenFarmingPanelOp 继续把同一个 TerrainInstance 传给 GameplayPort 的 FarmingPanelRequested 信号。
- 未占用时顺序为 pass_time、open_farming_panel；已占用时只保留 pass_time。第三个耗时快照参数被接收但忽略，继续使用资源自身 TimeCost。
- 旧 `FarmingInteraction.cs`、UID 与 OpenFarmingPanelOp 保留。回滚点为删除新脚本、测试段和执行器映射；没有生产资产需要恢复。

### GodotAI 验证结果

- 新脚本符号确认为 Resource、1 个导出字段和 1 个公开函数；聚焦测试 1/1 通过，共 7 个断言。
- 完整 `reusable_gathering` 42/42 通过，0 失败；新增 8 条警告仍是既有商店图标 UID 路径回退。
- Main 真实受控农场卡通过当前 C# 执行器后，时间 0→1，FarmingPanelRequested 恰好发出一次并携带同一 TerrainInstance；源卡在操作后保持有效，测试清理前棋盘仍为 1 张卡。
- Main 启动 44 条与交互新增 10 条日志均为 info；cursor 190 之后除完整回归的 8 条既有图标警告外没有新增错误。

### 旧路径、编码与遗留风险

- 生产场景、Resource、GDScript 与生成脚本原本就没有旧 FarmingInteraction 路径或 UID；旧内容仅为兼容 C# 文件和 `.cs.uid` 旁车，新脚本当前只由聚焦测试和受控运行时探针使用。
- 本批新脚本/UID、执行器、测试及四份文档共 8 个直接文本将执行严格 UTF-8 检查；没有 TODO、FIXME、NotImplemented 或空 `pass`。
- 这是并行实现完成而非生产切换。未来出现真实农场地形资产时必须显式引用新脚本并补充对应场景烟测，不能据本批声称农场 UI 已迁移。

## 2026-09-19 20:03：TalentCard 生产迁移

### 本批范围

- 新增 `resources/talents/talent_card.gd`，等价迁移卡片数据展示、点击转发、底部中心缩放轴心和悬浮动画。
- 将 `TalentManager.cs` 的卡片实例边界改为 Control + `Initialize` / `OnCardClicked` 动态协议，并切换 `talent_card.tscn` 生产脚本。
- 在 `test_inventory_component_contract.gd` 增加两个聚焦契约；本批未迁移 TalentData、TalentEffect、Player、TimeSystem、GlobalEventBus 或天赋效果规则。

### C# / GDScript 兼容边界与回滚点

- C# Manager 继续拥有天赋池、洗牌、暂停、显示、移除、事件广播和恢复运行；GDScript 卡片只读取同一 C# TalentData 的 TalentName、Description、TalentTexture 并原样发回。
- 四个原序列化字段、Initialize 方法名、OnCardClicked 信号名、节点结构和动画参数不变；旧 `TalentCard.cs` 与 UID 保留。
- 回滚点为恢复场景旧脚本，以及 Manager 的 `Instantiate<TalentCard>()`、直接 Initialize 与 C# 事件订阅。

### GodotAI 验证结果

- TalentCard 聚焦测试 2/2 通过，共 18 个断言；完整 `inventory_component_contract` 20/20 通过，0 失败。
- Main 真实运行中 C# Manager 生成 1 张 `talent_card.gd` 卡片；标题、描述和贴图来自同一 C# TalentData，悬浮层级为 1→0。
- TimeSystem 的 TalentSelectionTriggered 触发后场景暂停且界面可见；点击后 C# Manager 收到 OnCardClicked、界面隐藏并恢复运行。
- 最终 run 6 条日志全部为 info；cursor 199 后没有新增编辑器脚本、资源、信号、类型或运行时错误。

### 旧路径、编码与遗留风险

- 生产场景已无 `TalentCard.cs` 路径或旧 UID；旧实现只保留为兼容 C# 文件与 `.cs.uid` 旁车，测试仍显式锁定新脚本路径。
- 本批相关代码、场景、测试和文档执行严格 UTF-8、TODO/FIXME/空 pass 与差异格式检查。
- TalentData、效果数组、Manager 与 Player 仍为 C#；后续天赋数据/效果迁移必须单独设计 typed Array 与效果应用边界。下一批继续选择独立叶子模块，不处理 AttributeModifierData，除非先确认 C# 类型表刷新成功。

## 2026-09-19 20:12：TalentManager 生产迁移

### 本批范围

- 新增 `resources/talents/talent_manager.gd`，等价迁移天赋池复制、独立随机洗牌、最多三张生成、空池短路、暂停显示、选择移除和恢复运行。
- 切换 `talent_screen.tscn` 生产脚本，并把空池序列化声明从 `Array[Object]` 收窄为可同时承接新旧实现的 `Array[Resource]`。
- 在 `test_inventory_component_contract.gd` 增加生产场景契约；未迁移 TalentData、TalentEffect、Player、TimeSystem、GlobalEventBus 或效果应用逻辑。

### C# / GDScript 兼容边界与回滚点

- 新 Manager 继续订阅 C# TimeSystem 的 TalentSelectionTriggered，并向现有 GDScript GlobalEventBus 发出 `on_player_acquired_talent`；C# Player 仍接收同一个 C# TalentData。
- 卡片创建继续使用 CardScenePrefab、Initialize 和 OnCardClicked 协议；三个序列化字段、HBoxContainer、CanvasLayer process_mode 和节点结构不变。
- 旧 `TalentManager.cs` 与 UID 保留。回滚点为恢复场景旧脚本和空池的旧数组声明；已迁移的 TalentCard 无需回滚。

### GodotAI 验证结果

- TalentManager 聚焦测试 1/1 通过，共 9 个断言；完整 `inventory_component_contract` 21/21 通过，0 失败。
- Main 真实运行中用 4 个 C# TalentData 触发选择：生成 3 张卡，场景暂停并显示，卡片标题来自其绑定的同一 Resource。
- 点击后 `on_player_acquired_talent` 恰好广播一次且 instance_id 与所选 Resource 相同；可用池 4→3，界面隐藏并恢复运行。空池再次验证为 0 张、不暂停、不显示。
- 最终 run 只包含 game helper、卡片点击、Manager 选择与空池提示的 info 日志；cursor 199 后无新增编辑器错误。

### 旧路径、编码与遗留风险

- 生产场景已无 `TalentManager.cs` 路径或旧 UID；旧实现只保留为兼容 C# 文件与 `.cs.uid` 旁车。
- 本批相关脚本、UID、场景、测试和文档执行严格 UTF-8、占位实现、旧路径和差异格式检查。
- TalentData 与 TalentEffect 仍是 C# typed Array 边界；迁移数据和效果时必须单独验证 Player 的强类型事件接收与 Apply 调用，不能与本批合并。

## 2026-09-19 20:23：TalentData 与 TalentEffect 兼容迁移

### 本批范围

- 新增 `talent_data.gd`、`talent_effect.gd`、`attribute_talent_effect.gd`、`tag_talent_effect.gd`，保持原四类 Resource 的字段和效果行为。
- 修改 `Player.cs`，把天赋事件回调从 TalentData 降为 Resource，并按 Effects 数组动态调用 Apply；旧 C# 数据和效果继续兼容。
- 扩展 `test_inventory_component_contract.gd`；仓库扫描确认当前没有生产 `.tres` 或场景引用这些数据/效果脚本，因此本批不虚构生产资产切换。

### C# / GDScript 兼容边界与回滚点

- 新 TalentData 用 `Array[Resource]` 保存旧 C# 或新 GDScript 效果；Player 只读取 TalentName、Effects 和 Apply，保持数组顺序与原 Resource 身份。
- 属性效果保持 TargetAttribute 0..14、BonusValue 与直属 `AttributeComponent` 路径；标签效果继续通过 Player.TagComponent 调用 AddTag，空标签短路不变。
- 由于当前编辑器不支持可用的 `@override`，且路径继承的抽象方法使具体脚本不可实例化，GDScript 基类改为显式 `push_error` 的故障快速协议；具体效果均有完整实现。
- 旧 TalentData、TalentEffect、AttributeTalentEffect、TagTalentEffect C# 文件和 UID 保留。回滚点为恢复 Player 的强类型 Callable/遍历，并删除四个新脚本及契约段。

### GodotAI 验证结果

- 天赋 Resource 聚焦测试 1/1 通过，共 13 个断言；完整 `inventory_component_contract` 22/22 通过，0 失败。
- Main 真实运行中，新 TalentData + 新 TagTalentEffect 和新 TalentData + 旧 C# TagTalentEffect 均令生产 TagComponent 0→1，清理后回到 0；旧 C# TalentData 也能被新 Player 回调接收。
- 属性效果通过受控直属 AttributeComponent 探针收到 `LifestealRate=14` 与 `BonusValue=2.5`，证明字段和值未改变。
- 最终 Main 56 条日志全部为 info；首次扫描的抽象类标记、错误断言 API 与 `@override` 兼容问题均已修复，cursor 215 后没有新增编辑器错误。

### 旧路径、编码与遗留风险

- 生产场景/资产原本没有旧 TalentData/Effect 脚本路径；旧路径和 UID 只允许保留在四个兼容 C# 文件及旁车。新实现当前由契约测试和受控运行时链使用。
- 本批相关代码、UID、测试和四份文档执行严格 UTF-8、占位实现、旧路径及差异格式检查。
- AttributeTalentEffect 原实现只查找玩家直属 `AttributeComponent`，实际生产玩家组件位于 `Components/AttributeComponent`；这是既有行为，本批按语言迁移原则原样保留并记录，未擅自修复。

## 2026-09-19 20:37：MonsterSkillPreview 与 MonsterSkillComponent 生产迁移

### 本批范围

- 新增 `resources/monster/monster_skill_preview.gd`，等价迁移旧 C# 预览值对象的技能标识、名称、说明、图标、元素和目标类型字段及整数别名。
- 新增 `entities/components/monster_skill_component.gd`，迁移技能集合读取、有效技能过滤、随机技能选择、预览可见过滤和说明回退；切换 `scenes/monster_scenes/monster.tscn` 的技能组件脚本。
- 将 `entities/Monster.cs` 的 `SkillComponent` 降为 `Node` 稳定边界，保留 `Initialize`、`GetCombatSkills` 和 `GetRandomCombatSkill` 的 C# 公开行为，并将 GDScript 返回值转换回旧 `CombatSkillData`。
- 在 `test_reusable_gathering_interaction.gd` 增加组件与预览契约；未迁移 `CombatSkillData`、技能效果、战斗结算、`MonsterData` 或物品链。

### C# / GDScript 兼容边界与回滚点

- `SkillSet`、技能条目和技能本体均使用通用 `Resource`，旧 C# 与 GDScript 资源保持同一序列化字段和 Resource 身份；组件只读取 `Skills`、`Skill`、`VisibleInPreview` 与 `GetPreviewDescription` 协议。
- `Monster.cs` 继续以 `Array<CombatSkillData>` 和 `CombatSkillData` 对外提供战斗 API；只有组件内部改为 `Node.Call`，不改变战斗管理器和自动回合的调用名称。
- 预览值对象不复制技能 Resource，只保存旧值对象原本暴露的展示快照；旧 `MonsterSkillPreview.cs`、`MonsterSkillComponent.cs` 与 UID 保留为 C# 兼容垫片。回滚点为恢复 `monster.tscn` 的旧组件路径和 `Monster.cs` 的强类型组件成员。

### GodotAI 验证结果

- `reusable_gathering` 聚焦测试 `test_monster_skill_component_and_preview_contract`：1/1，通过 11 个断言，覆盖同一技能 Resource 身份、可见过滤、字段快照、枚举整数值和生产场景路径。
- GodotAI 脚本符号解析通过：预览值对象 `RefCounted` 2 个函数；技能组件 `Node`、1 个导出字段、11 个函数。
- `monster.tscn` 强制重载成功，技能组件节点出现在完整 28 节点层级；直接运行怪物场景 helper 正常，当前 run 仅有 info（helper 注册与两次初始化）。
- Main 场景真实运行 helper 正常；本批运行日志只有 helper/info。编辑器错误面板中的 inventory placeholder、历史测试空节点和 HUD null 记录均早于本批，未作为本批新增错误。

### 旧路径、编码与遗留风险

- 场景已从 `MonsterSkillComponent.cs` 切到 `entities/components/monster_skill_component.gd`，并将 ext_resource UID 与 `.gd.uid` 对齐；旧 C# 路径仅保留在兼容文件/旁车扫描范围内。
- 新脚本、UID、`Monster.cs`、场景、测试和文档严格 UTF-8 检查通过；新增实现无 TODO、FIXME 或空 `pass`。
- 战斗技能数据、技能效果和战斗 UI 仍依赖 C#；`GetSkillPreviews` 当前没有生产 UI 消费者，后续若迁移技能预览 UI 需继续沿该 Node/Resource 协议，不应直接恢复 C# 强类型引用。

### 下一步建议

- `CurrentMapBackgroundResolver` 虽然代码量小，但当前由 `WorldCombatScenePresenter` 静态 C# 调用并由 PassageGuard 测试直接构造；迁移前需先定义 GDScript 实例工厂/测试入口，不与本批技能组件边界混做。
- 之后优先处理 StartingStats、物品链与 Crafting 的独立兼容边界；Autoload、战斗效果和 `AttributeModifierData` 仍按原阻塞规则单独迁移。

## 2026-09-19 21:08：CurrentMapBackgroundResolver 并行实现（生产切换阻塞）

### 本批范围

- 新增 `core/gameflow/current_map_background_resolver.gd`，等价保留当前房间背景查找、复制、命名、层级和绝对 Z 顺序。
- 扩展 `test_reusable_gathering_interaction.gd` 与 `passage_guard_tests.gd` 的 GDScript 入口，验证 `current_scene` 优先、背景复制和非背景 Sprite 不受影响。
- 曾尝试把 `WorldCombatScenePresenter` 切到 GDScript Resolver 工厂，但 Main/战斗场景未进入 GodotAI helper live，已恢复 C# Presenter 静态调用，避免留下半切换生产边界。

### C# / GDScript 兼容边界与回滚点

- GDScript Resolver 仅负责 `MapInstantiator.current_scene`/子节点的背景解析与复制；战斗场景实例化、过场和世界视图仍由 C# Presenter 负责。
- 旧 `CurrentMapBackgroundResolver.cs` 保留为当前生产兼容实现；新脚本只作为并行测试入口，不改变战斗场景节点、背景名称、`z_index=-1` 或 `z_as_relative=false`。
- 回滚点为删除新脚本和测试入口；不修改 `WorldCombatScenePresenter.cs` 的现有静态调用。

### GodotAI 验证结果

- `script_manage.find_symbols` 成功解析新 Resolver；`test_run(suite="reusable_gathering", test_name="test_current_map_background_resolver_contract")`：1/1，通过 5 个断言。
- `session_manage`/`editor_state` 在收口前均为可用会话；停止项目后聚焦测试仍可执行。
- 本轮尝试的玩家/Main 场景运行未进入 helper live，且编辑器保留历史 C#/GDScript 警告；未把该次烟测算作通过，也未发现可定位的本批 Resolver 新错误。

### 旧路径、编码与遗留风险

- 旧 C# Resolver 路径仍只由生产 Presenter 与兼容文件使用；新增 GDScript 路径只出现在聚焦测试和并行实现。
- 新脚本、UID、测试及本批文档按 UTF-8 保存；未删除 C#、`.csproj`、`.sln` 或 Autoload。
- 生产切换的真实阻塞是 C# 程序集/编辑器场景缓存未刷新，当前不能宣称 Resolver 已生产迁移。下一批必须先让开发者刷新程序集并确认 Main/战斗 helper live，再处理 Resolver 工厂；在此之前继续选择不依赖该工厂的单一物品链边界。

## 2026-09-19 21:23：Crafting 数据 Resource 并行迁移

### 本批范围

- 新增 `resources/recipe/crafting_ingredient.gd`、`crafting_recipe.gd` 与 `recipe_book_data.gd`，等价保留三个 C# Resource 的字段名、默认值、导出类型和嵌套 Resource 关系。
- 扩展已注册的 `tests/godot/test_reusable_gathering_interaction.gd`，覆盖 GDScript 默认值/赋值/对象身份，并加载火把与石斧两个现有 C# 配方资产验证兼容输入。
- 未切换 `player.tscn`、`resources/recipe/res/*.tres`、`CraftingComponent.cs`、`CraftingService.cs` 或 CraftingUI；本批只推进数据层，不宣称 Crafting 生产迁移完成。

### C# / GDScript 兼容边界与回滚点

- 新 Resource 不使用 `class_name`，避免依赖未确认的 C# 全局类型表；`RequiredItem`、`OutputItem`、`Inputs` 与 `Recipes` 使用通用 `Resource`/`Array[Resource]`，可同时接收旧 C# ItemData/CraftingRecipe 和未来 GDScript 对象。
- CraftingComponent、CraftingService 继续拥有配方有效性、材料统计、虚拟库存、扣除、产出和信号；新脚本不复制任何副作用。
- 回滚只需移除三个 `.gd`、旁车 UID 和新增聚焦断言；旧 C# 资产、场景引用和序列化数据不受影响。

### GodotAI 验证结果

- `session_manage(op="list")`：最新会话 `cusga@a8122297b03bfc7d`，Godot 4.7.1，最终 readiness `ready`。
- `script_manage.find_symbols`：三个新 Resource 均解析为 `extends Resource`，导出数量分别为 2/4/1；测试脚本解析出两个 Crafting 用例。
- 聚焦测试：`reusable_gathering` 中 Crafting 两用例 2/2，通过 27 个断言；修复 typed-array 测试夹具后无失败。
- 相关回归：`reusable_gathering` 完整 46/46，通过。
- 主场景冒烟：GodotAI `project_run(mode="main")` 最终进入 `helper_live=true`；`game_eval` 返回当前场景 `MainMenu`；游戏日志仅有 helper 注册 info。编辑器日志只有既有 GlobalEventBus/ItemsControl/拖拽变量警告及物品图标 UID 回退，没有本批新增错误。

### 旧路径、编码与遗留风险

- 直接修改的 4 个 UTF-8 文件严格解码通过；GodotAI filesystem scan 完成，global class 注册增量为 0（符合无 `class_name` 设计）。
- 旧 Crafting C# 路径只在 `player.tscn` 与两个现有配方 `.tres` 中保留，属于本批明确的生产兼容输入；新 GDScript 路径只出现在测试和并行实现，不存在半切换生产引用。
- CraftingComponent/Service/UI、配方资产切换、物品链消费者、Autoload、战斗和 `AttributeModifierData` 仍未完成；后续必须先为 CraftingComponent 单独设计运行时消费桥，再决定资产切换。

## 2026-09-19 21:34：CraftingService 并行迁移

### 本批范围

- 新增 `core/crafting/crafting_service.gd`，等价实现需求汇总、材料检查、最大可合成量二分、消耗后虚拟库存空间预检和成功/失败执行顺序。
- `TryCraftWithReason` 返回与 C# `CraftingFailureReason` 相同的 0..4 整数码，`TryCraft` 通过 `LastFailureReason` 兼容 GDScript 无 `out` 参数边界。
- 在已注册 `reusable_gathering` 套件新增 `test_crafting_service_parallel_contract`，使用并行 Inventory/ItemData 验证材料释放槽位、空间失败不扣除、成功产出和数量错误；未切换 CraftingComponent、CraftingUI 或 C# 生产服务。

### C# / GDScript 兼容边界与回滚点

- 服务只依赖 `CanStore`、`ItemCnt`、`TryRemoveItems`、`AddItem`、`Slots` 稳定方法，物品最大堆叠通过 `item_data_compat.gd` 读取；真实库存和配方 Resource 身份不复制。
- 虚拟槽位按原实现从后往前扣除材料，随后先填同物品堆叠再填空槽；只有预检成功才调用真实库存的扣除与加入，保留失败时不改库存的行为。
- 旧 `CraftingService.cs`、`CraftingComponent.cs`、CraftingUI、配方资产及其场景引用均保留；回滚只需移除新服务和测试段。

### GodotAI 验证结果

- `script_manage.find_symbols` 成功解析 `crafting_service.gd`：18 个函数，包含全部公开规则入口和虚拟库存辅助函数。
- Crafting 聚焦：`test_crafting_service_parallel_contract` 1/1，通过 13 个断言；Crafting 数据两用例仍为 2/2、27 个断言。
- 相关回归：`reusable_gathering` 完整 47/47，通过。
- Main 冒烟：`project_run(mode="main")` 最终 `helper_live=true`；`game_eval` 成功加载服务并确认 `TryCraftWithReason`/`CanReceiveOutputAfterConsuming`；停止后会话回到可用状态。游戏无新增错误，编辑器仅保留既有 UID/未使用信号/变量警告。

### 旧路径、编码与遗留风险

- 新增服务与更新测试严格 UTF-8；filesystem scan 后 GDScript 可加载，未新增全局类。
- 旧 Crafting 路径扫描结果不变：生产组件、UI、player 场景和两个配方资产仍明确保留 C# 兼容输入；新服务只出现在并行脚本和测试。
- 尚未完成 CraftingComponent 的信号/GameplayPort 边界、C# 配方资产切换、物品链最终生产切换、TimeSystem/PlayerWallet Autoload、战斗效果和 `AttributeModifierData`；下一批应单独处理 CraftingComponent 运行时桥或一个明确的物品链消费者。

## 2026-09-19 21:45：CraftingComponent 并行迁移

### 本批范围

- 新增 `entities/components/crafting_component.gd`，等价实现配方书读取、同级 InventoryComponent 绑定、CraftingService 委托、最大合成数、合成成功/失败信号和失败提示。
- 扩展 `test_reusable_gathering_interaction.gd` 的组件契约，覆盖旧 C# `out` 参数在 GDScript 边界上的原因码替代、配方 Resource 身份、库存扣除/产出与信号参数。
- 未切换 `scenes/player_scenes/player.tscn`、`GameplayPort.cs`、`CraftingUI.cs`、生产配方 `.tres` 或旧 C# 组件；本批完成并行运行时实现，不宣称 Crafting 生产链已切换。

### C# / GDScript 兼容边界与回滚点

- 新组件保留 `RecipeBook`、`Inventory`、`Recipes`、`CanCraft`、`MaxCraftableQuantity`、`TryCraft`、`CraftingCompleted` 与 `CraftingFailed` 的 PascalCase 协议；配方字段以通用 Resource 读取，可同时接收旧 C# 和新 GDScript 资源。
- `TryCraftWithReason` 返回与 C# `CraftingFailureReason` 相同的 0..4 整数码，`LastFailureReason` 保存最近一次结果；成功/失败信号参数顺序与 C# 保持一致。
- 旧 C# `CraftingComponent.cs` 继续负责生产场景，C# GameplayPort/CraftingUI 不被迫降型；回滚只需移除新组件、UID 和测试段。

### GodotAI 验证结果

- `session_manage(op="list")`：当前会话 `cusga@9084d4367345f50a`，Godot 4.7.1，最终 readiness `ready`。
- `script_manage.find_symbols`：新组件解析为 `extends Node`、1 个导出字段、8 个函数、2 个信号；测试脚本成功解析新增用例。
- 组件聚焦：`test_crafting_component_parallel_contract` 1/1，通过 15 个断言；`reusable_gathering` 完整回归 48/48；`inventory_component_contract` 回归 22/22。
- Main 冒烟：`project_run(mode="main")` 首次等待窗口内未报告 helper live，但随后 `logs_read(source="game")` 确认同一 run 已进入 `helper_live=true`/`ready=true`，`game_eval` 返回 `MainMenu`；停止后会话恢复 ready。游戏日志仅 helper/info。
- 编辑器日志仅出现既有物品图标 UID 回退、未使用信号/参数等警告；未发现本批新增 SCRIPT ERROR、Parse Error、Failed to load script、信号或类型错误。

### 旧路径、编码与遗留风险

- 旧 `CraftingComponent.cs`、`CraftingRecipe.cs`、`RecipeBookData.cs` 路径仍仅出现在生产 `player.tscn`、配方资产和兼容文件中，符合并行迁移预期；新 GDScript 路径只出现在测试和并行实现。
- 新组件与测试严格 UTF-8 解码通过；filesystem scan 完成，未注册新的全局类；未删除任何 C#、`.csproj`、`.sln` 或 Autoload。
- Crafting 生产切换仍受 C# GameplayPort/CraftingUI 强类型边界约束；下一步需单独降型 GameplayPort 与 CraftingUI 信号/方法协议，不能与物品链、Autoload、战斗或 `AttributeModifierData` 合并。

## 2026-09-19 21:57：CraftingUI 生产迁移

### 本批范围

- 新增 `core/ui/crafting/crafting_ui.gd`，等价保留合成面板的配方按钮、输出信息、材料汇总、数量限制、成功/失败文本、库存变化刷新和 Open/Close 行为。
- 切换 `scenes/crafting/crafting_ui.tscn` 的脚本 ext_resource/UID；`Main.tscn` 的实例路径和 `GameplayPortPath` 序列化值保持不变。
- 扩展 `test_inventory_component_contract.gd`，覆盖生产场景脚本身份、旧/新组件动态边界、配方按钮、材料行、成功扣除/产出、状态文本、关闭和信号清理；旧 C# UI 文件保留。

### C# / GDScript 兼容边界与回滚点

- UI 通过 `GameplayPortPath` 动态连接 `CraftingToggleRequested`/`CraftingOpenRequested`，组件通过 `Recipes`、`Inventory`、`MaxCraftableQuantity`、`TryCraft`/`TryCraftWithReason` 协议访问；不复制服务层库存规则。
- 新组件返回固定失败码时直接显示结果；仍为生产所有者的 C# CraftingComponent 仅提供 `TryCraft` 时，UI 按配方材料和库存协议推断 MissingMaterials/NotEnoughSpace，保持旧 UI 的可见行为。
- `crafting_ui.tscn` 已切换 GDScript，但 `player.tscn`、GameplayPort.cs、CraftingComponent.cs、配方资产与 C# 兼容文件均未切换；回滚只需恢复场景脚本引用。

### GodotAI 验证结果

- `session_manage(op="list")`：当前会话 `cusga@3b687b3cf010adc9`，Godot 4.7.1，最终 readiness `ready`。
- `script_manage.find_symbols`：`crafting_ui.gd` 解析为 `extends Control`、1 个导出字段、29 个函数；场景强制重载后根节点脚本为新 GDScript。
- CraftingUI 聚焦：`test_crafting_ui_preserves_cross_language_contract` 1/1，通过 9 个断言；`inventory_component_contract` 完整 23/23；`reusable_gathering` 完整 48/48。
- Crafting 场景直接运行随后进入 `helper_live=true`/`ready=true`，Main 运行同样进入 `helper_live=true`；两次游戏日志仅 helper/info，`current_run_errors=[]`，未见本批新增脚本、信号、类型或资源错误。
- 首次场景预加载测试曾因编辑器 placeholder 失败，已改用脚本实例 + 场景子树夹具并重跑通过；该历史错误已记录，不是当前生产脚本错误。

### 旧路径、编码与遗留风险

- `crafting_ui.tscn` 不再引用 `CraftingUI.cs`；旧 C# UI 仅作为兼容文件保留。`player.tscn` 仍引用 C# CraftingComponent/RecipeBookData/CraftingRecipe，属于本批明确未切换边界。
- 新 UI、场景、测试和文档严格 UTF-8；filesystem scan 完成，未新增全局类；未删除 C#、`.csproj`、`.sln` 或 Autoload。
- Crafting 组件和 GameplayPort 的 C# 强类型生产链仍未完全迁移；下一步需单独降型 GameplayPort 或继续迁移物品链消费者，仍不得声称 Crafting 全链路完成。

## 2026-09-19 22:05：ShopService 并行迁移

### 本批范围

- 新增 `core/shop/shop_service.gd`，等价实现商店可购买判断、卖价回退、买卖前置校验、总价溢出保护、原子扣款/加货与固定失败码。
- 扩展 `test_reusable_gathering_interaction.gd`，使用并行 ItemData、Inventory 和 FakeWallet 覆盖成功买卖、余额不足、容量不足、数量错误、缺货、显式卖价和最近失败原因。
- 未切换 `ShopTradeBridge.cs`、PlayerWallet Autoload、商店 UI、ShopCatalog 生产消费者或 Inventory C# 生产实现；本批只推进物品链纯规则层。

### C# / GDScript 兼容边界与回滚点

- 服务通过 `Gold`、`TrySpend`、`Add`、`CanAddItem`、`AddItem`、`TryRemoveItem`、`ItemCnt` 动态访问旧/新钱包与库存；目录显式单价使用 `CanBuyWithPrice`/`TryBuyWithPrice`，不改变默认物品买价和卖价折半规则。
- `TryBuyWithReason`/`TrySellWithReason` 返回与 C# `ShopFailureReason` 一致的 0..6 整数码并更新 `LastFailureReason`；所有校验在副作用前完成，购买写入异常会退回金币，出售缺少钱包时不会先移除物品。
- 旧 C# ShopService/ShopTradeBridge 保持生产所有权，回滚只需移除新服务、UID 和测试段，无需恢复任何生产资产。

### GodotAI 验证结果

- `session_manage(op="list")`：当前会话 `cusga@3b687b3cf010adc9`，Godot 4.7.1，最终 readiness `ready`。
- `script_manage.find_symbols`：新服务解析为 `extends RefCounted`、15 个函数；测试脚本成功解析新增用例。
- ShopService 聚焦：`test_shop_service_parallel_contract` 1/1，通过 20 个断言；`reusable_gathering` 完整回归 49/49。
- Main 冒烟：`project_run(mode="main")` 后 `logs_read(source="game")` 确认同一 run 进入 `helper_live=true`/`ready=true`；游戏日志仅 helper/info，`current_run_errors=[]`，停止后会话 ready。
- 编辑器保留此前 CraftingUI placeholder 测试失败的历史错误行及既有 UID/未使用信号警告；本批 ShopService 未新增 SCRIPT ERROR、Parse Error、资源、信号或类型错误。

### 旧路径、编码与遗留风险

- 新 ShopService 路径只出现在并行服务与测试；旧 `ShopService.cs`、`ShopTradeBridge.cs`、`IPlayerWallet.cs`、`IShopInventory.cs` 和钱包 Autoload 仍保留，符合物品链分阶段迁移约束。
- 新服务、测试和四份文档严格 UTF-8；filesystem scan 完成，未新增全局类；未删除 C#、`.csproj`、`.sln` 或 Autoload。
- 商店生产交易仍依赖 C# Bridge/钱包/库存；下一步可单独迁移 ShopTradeBridge 或 PlayerWallet，但不得与 Crafting GameplayPort、战斗或 AttributeModifierData 合并。

## 2026-09-19 22:16：ShopTradeBridge 并行迁移

### 本批范围

- 新增 `core/shop/shop_trade_bridge.gd`，等价实现商店目录解析、显式/自动商品排序、价格回退、钱包/库存读取及交易方法协议。
- 扩展 `test_reusable_gathering_interaction.gd` 的 `test_shop_trade_bridge_parallel_contract`，覆盖目录顺序、自动 `CardId` 排序、Resource 身份去重、目录兜底买价、卖价折半、动态交易和库存/钱包结果。
- 未切换 `scenes/Shop/Shop.tscn`、`scripts/shop/shop_control.gd`、PlayerWallet、GlobalWarehouse、ItemData/ItemStack 或旧 C# Bridge；本批只完成并行边界。

### C# / GDScript 兼容边界与回滚点

- 新 Bridge 保留 C# Bridge 的 PascalCase 入口与参数顺序，`Catalog` 继续使用通用 `Resource`，可接收旧 C# ShopCatalog 或 `resources/shop/shop_catalog.gd`。
- 目录显式商品按原顺序保留，自动补入仅取自身 `BuyPrice > 0` 的物品并按 `CardId` 升序；显式无买价商品使用 `DefaultBuyPrice`，所有买卖副作用委托 `shop_service.gd` 的固定失败码协议。
- 回滚只需移除 `shop_trade_bridge.gd`、旁车 UID 与测试段；旧 `ShopTradeBridge.cs` 仍是 `Shop.tscn` 生产节点，无需恢复场景或资产。

### GodotAI 验证结果

- `session_manage(op="list")`：会话 `cusga@3b687b3cf010adc9`，Godot 4.7.1；测试/冒烟后已恢复 `stopped + ready`。
- `filesystem_manage(op="scan")` 完成；`script_manage.find_symbols` 解析新 Bridge 为 `extends Node`、1 个导出字段、17 个函数，测试脚本解析出新增用例。
- Bridge 聚焦 `test_shop_trade_bridge_parallel_contract`：1/1，通过 15 个断言；`reusable_gathering` 完整回归 50/50 通过。
- `Shop.tscn` 直接冒烟：同一 run 后续进入 `helper_live=true`/`ready=true`，游戏日志只有 GodotAI helper；编辑器报告的 3 条错误来自场景原有缺失 `res://tests/huhu9/test_gold.gd` 和既有资源 UID 回退，不是本批新脚本产生。
- Main 运行时 `game_eval` 实例化新 Bridge 并读取旧 C# `PlayerWallet`/`GlobalWarehouse`：返回 `gold=1200`、`stock_count=89`、首项 `Ax`、仓库数量 `0`；验证动态边界可用，随后停止运行并恢复 `ready`。

### 旧路径、编码与遗留风险

- 新 Bridge、测试和三份任务文档均以 UTF-8 保存；旧路径扫描确认 `Shop.tscn` 仍引用 `ShopTradeBridge.cs`，符合并行迁移预期。
- 当前仍保留旧 C# Bridge/ShopService、钱包与库存接口、PlayerWallet/GlobalWarehouse Autoload；商店生产 UI 尚未切换，不能宣称 ShopTradeBridge 生产迁移完成。
- 下一批应在不混入 Crafting GameplayPort、战斗或 `AttributeModifierData` 的前提下，单独处理 PlayerWallet 或 ShopTradeBridge 生产切换，并先解决/确认商店场景缺失测试脚本的既有编辑器错误。

## 2026-09-19 22:24：ShopTradeBridge 生产切换

### 本批范围

- 将 `scenes/Shop/Shop.tscn` 的 `ShopTradeBridge` ext_resource/UID 切换到 `res://core/shop/shop_trade_bridge.gd`，节点名、Catalog 资源和 UI 结构全部保留。
- 更新 `scripts/shop/shop_control.gd` 注释以反映 Bridge/Service 的双语言兼容边界；未改分页、按钮、信号、钱包或仓库流程。
- 扩展商店契约测试，锁定生产节点脚本身份；旧 `ShopTradeBridge.cs` 继续保留为回滚兼容垫片。

### C# / GDScript 兼容边界与回滚点

- 新生产 Bridge 通过既有 PascalCase 方法和 `Array[ItemData].assign` 与 `shop_control.gd` 连接，读取旧 C# `PlayerWallet`/`GlobalWarehouse`；Catalog 和物品 Resource 身份不复制、不重建。
- 旧 C# Bridge、ShopService、钱包/库存接口和 Autoload 均未删除。回滚点是把 `Shop.tscn` 的单条 ext_resource 恢复到 `ShopTradeBridge.cs` UID，其他资产无需回滚。

### GodotAI 验证结果

- `filesystem_manage(op="scan")` 完成；生产场景契约测试 `test_shop_trade_bridge_parallel_contract` 1/1，通过 17 个断言；`reusable_gathering` 完整回归 50/50 通过。
- `Shop.tscn` 直接运行后同一 run 进入 `helper_live=true`/`ready=true`；运行时 `game_eval` 确认 `bridge_script=res://core/shop/shop_trade_bridge.gd`、`catalog_path=res://resources/shop/shop_catalog.tres`、商品数 89、金币文本 `金币：1200`、首项购买可用。
- Main/商店运行日志没有新 Bridge 的 SCRIPT ERROR、Parse Error、Failed to load script、信号或类型错误；编辑器仍报告商店原有缺失 `res://tests/huhu9/test_gold.gd` 与资源 UID 回退，均为本批未触碰的既有问题。

### 旧路径、编码与遗留风险

- `Shop.tscn` 旧路径扫描只允许 `ShopTradeBridge.cs` 作为保留兼容文件；新场景引用已切换到 GDScript，所有修改文件严格 UTF-8。
- 物品链核心、PlayerWallet/GlobalWarehouse Autoload、ShopService C#、Shop UI 和 Crafting/战斗仍未全量迁移；本批仅完成商店 Bridge 生产切换，不代表商店全链路或项目全量完成。
- 下一步继续选择单一边界，优先 PlayerWallet Autoload 或 ItemStack/Inventory 生产兼容桥；不得与 StartingStats、AttributeModifierData 或战斗高耦合系统混做。

## 2026-09-19 22:30：PlayerWallet 并行迁移

### 本批范围

- 新增 `core/autoloads/player_wallet.gd`，等价实现默认金币、`GoldChanged`、`TrySpend`、`Add`、SettingsManager 持久化边界、非法存档回退和 int 上限钳制。
- 扩展 `test_reusable_gathering_interaction.gd` 的 `test_player_wallet_parallel_contract`，覆盖默认余额、成功/失败扣款、信号次数、非正数进账和最大值钳制。
- 未切换 `project.godot` 的 `PlayerWallet` Autoload，未修改 `PlayerProgression.cs`、GlobalWarehouse、ShopTradeBridge 或 ItemStack；旧 C# 钱包仍为生产实现。

### C# / GDScript 兼容边界与回滚点

- 新脚本保留 `Gold`、`GoldChanged`、`TrySpend`、`Add` 的 PascalCase 协议和 1200 默认值；存档键继续为 `player/gold`，通过 SettingsManager 动态方法访问。
- `PlayerProgression.cs` 仍依赖 C# `IPlayerWallet`，生产 Autoload 不得提前替换；回滚只需删除新脚本、UID 和测试段，不触碰 project.godot 或旧存档。

### GodotAI 验证结果

- `filesystem_manage(op="scan")` 完成；`script_manage.find_symbols` 解析脚本为 `extends Node`、6 个函数、1 个 `GoldChanged` 信号。
- PlayerWallet 聚焦 `test_player_wallet_parallel_contract`：1/1，通过 11 个断言；`reusable_gathering` 完整回归 51/51 通过。
- Main 运行后 `game_eval` 将并行脚本挂入根节点并通过 SettingsManager 初始化，返回 `before=1200`、扣款后 `1150`、退款后 `1200`、`PersistAcrossRuns=false`；随后停止运行并恢复 `ready`。
- 游戏日志只有 helper/info；编辑器保留商店缺失 `res://tests/huhu9/test_gold.gd` 和既有 UID/placeholder 警告，未发现本批新增脚本解析、信号或类型错误。

### 旧路径、编码与遗留风险

- 新脚本、测试和三份任务文档严格 UTF-8；旧路径扫描确认 `project.godot` 仍指向 `PlayerWallet.cs`，符合 C# 接口消费者未降型的兼容边界。
- PlayerWallet 生产切换、PlayerProgression 动态降型、GlobalWarehouse Autoload、ItemStack/Inventory、StartingStats、AttributeModifierData 与战斗仍未完成；本批仅完成并行钱包规则。
- 下一步应单独处理 PlayerProgression 的钱包动态协议并再评估 Autoload 切换，不得把该切换与 GlobalWarehouse 或物品链批量混做。

## 2026-09-19 22:42：PlayerProgression 并行迁移

### 本批范围

- 新增 `core/progression/player_progression.gd`，等价保留仓库与带入栏的等级、容量、费用表、最大等级、升级方法、存档键和 `UpgradeChanged` 信号。
- 扩展 `tests/godot/test_reusable_gathering_interaction.gd` 的 `test_player_progression_parallel_contract`，覆盖 20 个断言：初始规则、钱包扣款、容量同步、信号、失败回滚和满级边界。
- 未切换 `project.godot` 的 PlayerProgression Autoload，未修改旧 C# 钱包、GlobalWarehouse、ItemStack 或 Crafting 生产边界。

### C# / GDScript 兼容边界与回滚点

- 新脚本保留 `GetWarehouseLevel`、`GetWarehouseCapacity`、`TryUpgradeWarehouse`、`GetCarrySlotCount`、`TryUpgradeCarrySlots` 等 PascalCase 入口；钱包、仓库和设置均通过动态 Node 方法访问。
- 仓库初始容量 27、每级 +9、费用 300/600/1000；带入栏初始 5、每级 +1、费用 200/350/550/800/1100；数值、等级上限和存档键均未改变。
- 旧 `PlayerProgression.cs` 继续拥有生产 Autoload，回滚只需移除新脚本、UID 和测试段，不影响现有存档或运行节点。

### GodotAI 验证结果

- `session_manage(op="list")`：`cusga@3b687b3cf010adc9`，Godot 4.7.1，验证前后均为 ready；`script_manage.find_symbols` 解析新脚本为 `extends Node`、24 个函数、1 个信号。
- PlayerProgression 聚焦测试：1/1，通过 20 个断言；`reusable_gathering` 完整回归：52/52 通过。
- Main 场景运行后进入 helper live，随后通过 `project_manage(op="stop")` 停止并恢复 ready；日志未出现本批新增脚本解析、信号或类型错误。

### 旧路径、编码与遗留风险

- 新脚本、测试和三份任务文档均以 UTF-8 保存；旧路径扫描确认 `project.godot` 仍指向 `PlayerProgression.cs`，符合并行迁移边界。
- 运行日志中仍有商店旧测试脚本缺失、资源图片 UID 回退以及 CraftingUI placeholder 错误，这些均为既有测试/编辑器夹具问题，不是本批新增阻塞。
- PlayerProgression 生产切换、GlobalWarehouse Autoload、ItemStack/Inventory、StartingStats、AttributeModifierData、Crafting GameplayPort 和战斗仍未完成；下一批只选择 GlobalWarehouse/Inventory 兼容边界或 PlayerProgression 动态降型之一。

## 2026-09-19 22:47：PlayerProgression 生产切换

### 本批范围

- 将 `project.godot` 的 `PlayerProgression` Autoload 切换到 `res://core/progression/player_progression.gd`；节点名、升级数值、存档键、公开方法和信号保持不变。
- 保留 `core/progression/PlayerProgression.cs` 与 UID 作为兼容垫片；未切换 PlayerWallet、GlobalWarehouse、ItemStack、Crafting 或战斗。

### C# / GDScript 兼容边界与回滚点

- 新生产脚本继续通过 `TrySpend` 访问旧 C# PlayerWallet，通过 `SetCapacity` 访问旧 C# GlobalWarehouse；`warehouse_control.gd`、UI 和其他调用方无需改名或改节点路径。
- 回滚只需把 `PlayerProgression` Autoload 路径恢复为 `res://core/progression/PlayerProgression.cs`；升级费用、容量和运行时存档格式无需恢复。

### GodotAI 验证结果

- `autoload_manage(op="list")` 确认生产路径为 `res://core/progression/player_progression.gd`；`script_manage.find_symbols` 解析成功。
- PlayerProgression 聚焦：1/1，通过 20 个断言；完整 `reusable_gathering`：52/52 通过。
- Main 运行后 `game_eval` 返回 `script=res://core/progression/player_progression.gd`、等级 0、仓库容量 27、带入栏 5；执行一次仓库升级返回成功、金币 1200→900、仓库容量 27→36，随后退款并停止运行，编辑器恢复 ready。
- 本批未新增 SCRIPT ERROR、Parse Error、Failed to load script、信号错误或类型错误；日志中的旧 CraftingUI placeholder、商店测试脚本缺失和图片 UID 回退保持原状。

### 旧路径、编码与遗留风险

- `project.godot`、新脚本、UID、测试和三份任务文档 UTF-8 检查通过；场景/Resource/GDScript 旧路径扫描未发现生产引用 `PlayerProgression.cs`，仅保留 C# 源文件、UID 和历史文档说明。
- 当前仍保留 C# PlayerWallet、GlobalWarehouse、Inventory/ItemStack、StartingStats、AttributeModifierData、Crafting GameplayPort 和战斗系统；下一批优先单独处理 GlobalWarehouse/Inventory 兼容边界。

## 2026-09-19 22:55：PlayerWallet 生产切换

### 本批范围

- 将 `project.godot` 的 `PlayerWallet` Autoload 切换到 `res://core/autoloads/player_wallet.gd`；保留 `Gold`、`GoldChanged`、`TrySpend`、`Add`、默认金币 1200 和 `player/gold` 存档键。
- 保留 `core/autoloads/PlayerWallet.cs`、`IPlayerWallet.cs` 和商店 C# 服务作为兼容垫片；未切换 GlobalWarehouse、ItemStack、StartingStats、Crafting 或战斗。

### C# / GDScript 兼容边界与回滚点

- PlayerProgression、ShopTradeBridge、ShopService 和仓库/商店 UI 均通过动态 `Gold`/`TrySpend`/`Add`/`GoldChanged` 协议消费新 Autoload；节点名和信号参数不变。
- 回滚只需把 `PlayerWallet` Autoload 恢复为 `res://core/autoloads/PlayerWallet.cs`；本批未修改存档格式，测试扣款在运行结束前已退款。

### GodotAI 验证结果

- `autoload_manage(op="list")` 确认 PlayerWallet 生产路径为 `res://core/autoloads/player_wallet.gd`，PlayerProgression 同时保持 GDScript 生产路径。
- 钱包聚焦：1/1，通过 11 个断言；完整 `reusable_gathering`：52/52 通过。
- Main 运行后 `game_eval` 返回 `wallet_script=res://core/autoloads/player_wallet.gd`、金币 1200、`progression_script=res://core/progression/player_progression.gd`、仓库容量 27；实测 `TrySpend(50)` 得到 1200→1150，`Add(50)` 恢复 1200。
- Main helper 运行后已停止并恢复 ready；编辑器增量日志 cursor 88 后无新增错误。一次错误的商店 `game_eval` 仅因传入了错误的 `BuildStockList` 参数，已清理 Debugger，不是生产脚本错误。

### 旧路径、编码与遗留风险

- `project.godot`、钱包脚本、UID、测试和三份任务文档均通过 UTF-8 检查；生产场景/Resource/GDScript 旧路径扫描未发现 `PlayerWallet.cs` Autoload 引用，仅保留 C# 源文件、接口和历史文档。
- GlobalWarehouse 仍是 C# 场景 Autoload，Inventory/ItemStack、StartingStats、AttributeModifierData、Crafting GameplayPort 和战斗仍未完成；下一批优先处理 GlobalWarehouse/Inventory 的动态消费边界。

## 2026-09-19 23:05：TimeSystem 并行迁移

### 本批范围

- 新增 `core/autoloads/time_system.gd`，保留时间累计、昼夜切换、天数增长、每七天天赋触发、地图移动耗时、阶段进度和完整 `TimeChanged` 快照信号。
- 扩展 `test_reusable_gathering_interaction.gd` 的 `test_time_system_parallel_contract`，覆盖 13 个断言；未切换 `project.godot` 的 TimeSystem Autoload，也未修改 C# 时间消费者。

### C# / GDScript 兼容边界与回滚点

- 新脚本保留 `PhaseLength=100`、`MapMoveTimeCost=10`、`PassTime`、`PassMapMoveTime`、`SetMapMoveTimeCost` 和四个信号；`_get` 动态暴露 `TotalTimePassed`、`CurrentDay`、`PhaseProgress`。
- 由于 EncounterManager、RoomBoardPresenter 等 C# 代码仍通过 `TimeSystem.Instance` 静态属性取值，本批只完成并行实现；旧 `TimeSystem.cs` 与生产 Autoload 保持不变。
- 回滚只需移除新脚本、UID 和测试段，不影响运行时天数或场景结构。

### GodotAI 验证结果

- `filesystem_manage(op="scan")` 与 `script_manage.find_symbols` 成功；新脚本解析为 `extends Node`、1 个导出字段、10 个函数、4 个信号。
- TimeSystem 聚焦：1/1，通过 13 个断言；完整 `reusable_gathering`：53/53 通过。
- Main 场景运行进入 helper live，随后停止并恢复 ready；编辑器日志 cursor 88 后无新增错误。

### 旧路径、编码与遗留风险

- 新脚本、UID、测试和三份任务文档通过 UTF-8 检查；旧 C# TimeSystem 生产路径仍只保留在 `project.godot` 与兼容源文件中，符合本批并行边界。
- TimeSystem 生产切换仍需先把 C# 静态消费者降为动态节点协议；GlobalWarehouse、Inventory/ItemStack、Crafting、StartingStats、AttributeModifierData 和战斗仍未全量完成。下一步优先处理一个不依赖 C# 静态单例的物品链消费者，或单独设计 TimeSystem 消费者降型。

## 2026-09-19 23:27：GameplayPort 并行边界

### 本批范围

- 新增 `core/application/gameplay_port.gd` 与 UID，等价保留 `GameplayPort.cs` 的六个导出路径、六个信号、玩家组件解析、库存委托、仓库请求和遭遇数组转换。
- 新增 `tests/godot/test_gameplay_port_contract.gd` 与 `tests/test_gameplay_port_contract.gd` 发现包装器；不修改 `Main.tscn`、`GameplayPort.cs`、GlobalWarehouse、Inventory/ItemStack 或任何生产 C# 消费者。
- 修复测试夹具直接调用 `_ready` 时绝对 `/root/EncounterManager` 的解析保护：仅在节点已进入 SceneTree 时查找全局管理器，生产树内语义不变。

### C# / GDScript 兼容边界与回滚点

- 新门面只使用 Node/Variant 动态协议，保留 `PlayerPath`、`PlayerInventoryPath`、`PlayerBattleDeckPath`、`PlayerHealthPath`、`PlayerCraftingPath`、`GlobalWarehousePath` 的序列化键和值，以及全部 PascalCase 方法/信号参数。
- C# `GameplayPort.cs` 继续拥有生产节点；回滚点为移除新 GDScript、UID 和测试，不需要恢复场景或资产。后续切换前必须先把 C# 强类型消费者降型，并单独验证 GlobalWarehouse/Inventory。

### GodotAI 验证结果

- `session_manage(op="list")` 确认 CUSGA 会话可运行；`filesystem_manage(op="scan")` 完成；`script_manage.find_symbols` 解析为 `extends Node`、6 个导出字段、8 个函数、6 个信号。
- GameplayPort 聚焦测试：1/1，通过 13 个断言；`reusable_gathering` 完整套件：53/53 通过。
- Main 冒烟运行最终进入 `helper_live=true`/`ready=true`，游戏日志仅有 helper/info；停止后编辑器恢复 ready。首次启动响应较慢，但不是脚本错误。
- 修正后编辑器增量日志 cursor 91 之后无新增错误；旧缓存中仍可见先前夹具的绝对路径错误和仓库未绑定错误，均未在修正后的聚焦测试再次出现。

### 旧路径、编码与遗留风险

- 新脚本、UID、测试和三份任务文档均按 UTF-8 保存；旧路径扫描应继续允许 `GameplayPort.cs`、其 UID、测试对照代码及历史文档，`Main.tscn` 仍不引用 GDScript 门面。
- 尚未完成 GameplayPort 生产切换、GlobalWarehouse/Inventory/ItemStack、StartingStats、AttributeModifierData、Crafting 全链路、战斗、存档和暂停/场景流程；不得据此宣称全量迁移完成。
- 下一批只选择 GlobalWarehouse/Inventory 的单一生产消费边界，先盘点 C# 强类型调用方并设计降型/回滚点，再考虑是否切换 GameplayPort 或相关 Autoload。

## 2026-09-20 00:08：GlobalWarehouse 生产切换

### 本批范围

- 将 `core/autoloads/global_warehouse.tscn` 的生产脚本从 `WarehouseInventoryComponent.cs` 切换到 `entities/components/warehouse_inventory_component.gd`；保留节点名 `GlobalWarehouse`、Autoload 名称、容量初始化和仓库拖拽来源标识。
- 修改 `core/application/GameplayPort.cs`：缓存通用 `Node` 仓库并保留旧 `WarehouseRequested(InventoryComponent, InventoryComponent)`；GDScript 仓库无法转为 C# InventoryComponent 时改发新增 `WarehouseNodeRequested(Node, Node)`。
- 修改 `core/ui/warehouse/warehouse_ui.gd` 同时连接/断开两条仓库请求信号；扩展 `test_inventory_component_contract.gd` 覆盖动态信号打开行为。

### C# / GDScript 兼容边界与回滚点

- 旧 C# GlobalWarehouse、InventoryComponent、GameplayPort、WarehouseUI 文件均保留；旧 C# 仓库存在时继续使用强类型信号，切换后使用 Node 信号，库存方法通过既有 PascalCase 动态协议消费。
- 回滚点为将 `global_warehouse.tscn` ext_resource 恢复到 `WarehouseInventoryComponent.cs`；如需完整回退，可移除 `WarehouseNodeRequested` 与 WarehouseUI 动态信号连接，不改存档字段、节点结构或槽位数量。

### GodotAI 验证结果

- `session_manage(op="list")`：CUSGA Godot 4.7.1 会话 ready；`filesystem_manage(op="scan")` 完成，类型表增量 0。
- `test_inventory_component_contract`：24/24 通过；`reusable_gathering`：53/53 通过。
- Main 真实运行：`/root/GlobalWarehouse` 脚本为 `res://entities/components/warehouse_inventory_component.gd`，容量 27；`GameplayPort.cs` 同时存在旧/动态仓库信号，动态仓库节点有效；调用 `RequestOpenWarehouse()` 后 WarehouseUI 可见，玩家槽位 27、仓库槽位 27，关闭成功。
- 当前运行日志只有 helper、地图生成和 info；本批没有新增 `SCRIPT ERROR`、Parse Error、Failed to load、信号或类型错误。编辑器日志中旧缓存的夹具错误、资源图片 UID 回退和 placeholder 警告未由本批触发。

### 旧路径、编码与遗留风险

- 本批修改文件及 `.uid`/场景均通过 UTF-8 检查；旧路径扫描确认生产仓库场景不再引用 `WarehouseInventoryComponent.cs`，该 C# 文件仍作为兼容垫片保留。
- 仍未迁移 Inventory/ItemStack 生产链、StartingStats、Crafting 运行时、AttributeModifierData、战斗和其余 C# Autoload；`GameplayPort.cs` 仍是生产节点，下一批只处理 Inventory/ItemStack 的一个清晰消费边界，不与 StartingStats、Crafting 或战斗混做。

## 2026-09-20 00:16：HUD 输入桥生产切换

### 本批范围

- 新增 `core/ui/hud/hud_controller.gd`、`core/ui/hud/backpack_button.gd`，并将 `scenes/Main.tscn` 的 HUD 控制节点切换为 GDScript。
- 保留 `GameplayPortPath`、`BackpackButtonPath`、`toggle_crafting`、`toggle_inventory` 和原输入阶段；旧 `HUDController.cs`、`BackpackButton.cs` 保留为兼容垫片。

### GodotAI 验证结果

- `script_manage.find_symbols` 成功；HUD 聚焦 1/1，inventory 相关聚焦通过，`item_stack` 2/2，`reusable_gathering` 53/53。
- Main 真实运行确认 Tab 打开 CraftingUI、B 打开 InventoryUI、背包按钮可关闭 InventoryUI；日志无本批新增错误。

### 遗留风险

- HUD 迁移不涉及 GameplayPort、库存规则或存档；旧 C# 脚本继续保留。后续仍需处理 Inventory/ItemStack 生产边界。

## 2026-09-20 00:17：TagComponent 生产切换

### 本批范围

- 将 `scenes/player_scenes/player.tscn` 的 TagComponent 切换到 `entities/components/tag_component.gd`。
- 将 `entities/Player.cs`、`entities/components/EquipmentComponent.cs`、`resources/talents/TagTalentEffect.cs`、`core/map/PassageGuardProbabilityProvider.cs` 的标签访问降为 Node/动态方法协议；旧 C# TagComponent 保留。

### GodotAI 验证结果

- `script_manage.find_symbols` 解析 TagComponent 的 `AddTag`、`RemoveTag`、`HasTag`、`GetTagStack`。
- TagComponent 聚焦 2/2；PassageGuard 相关 6/6；`reusable_gathering` 53/53；玩家场景和 Main 冒烟通过。
- UTF-8 和旧生产路径扫描通过；运行日志无本批新增 SCRIPT ERROR、Parse Error、信号或类型错误。

### 遗留风险

- 旧 `TagComponent.cs` 仅作为兼容垫片/旁车 UID 保留；Vital、Inventory/ItemStack、StartingStats、Crafting、AttributeModifierData 和战斗仍未完成。

## 2026-09-20 00:58：CraftingComponent 生产切换

### 本批范围

- 将 `scenes/player_scenes/player.tscn` 的 CraftingComponent 从 C# 切换到 `entities/components/crafting_component.gd`，RecipeBook、配方顺序、节点名和库存同级路径保持不变。
- 在 `GameplayPort.cs` 增加 `CraftingNodeToggleRequested(Node)`、`CraftingNodeOpenRequested(Node)` 和通用组件缓存；`crafting_ui.gd` 同时连接/解除旧强类型信号与新 Node 信号。
- 扩展 `test_inventory_component_contract.gd` 的动态合成打开断言；旧 `CraftingComponent.cs`、服务和配方资产均保留。

### C# / GDScript 兼容边界与回滚点

- C# CraftingComponent 仍可通过原 `CraftingToggleRequested`/`CraftingOpenRequested` 工作；生产 GDScript 组件通过新增 Node 信号进入同一 UI 处理函数。
- GameplayPort 的玩家、库存、卡组和其余 C# 强类型接口不变；本批没有切换 GameplayPort 生产脚本或物品链。
- 回滚只需把玩家组件脚本恢复为 `CraftingComponent.cs`；两条旧信号和全部旧文件始终保留。

### GodotAI 验证结果

- `filesystem_manage(op="scan")` 完成，类型表 160、注册增量 0；`crafting_component.gd` 与 `crafting_ui.gd` 符号解析成功。
- CraftingComponent 聚焦 1/1、15 个断言；CraftingUI 聚焦 1/1、12 个断言；`inventory_component_contract` 24/24。
- 玩家场景运行时 `/Player/Components/CraftingComponent` 实际脚本为 `res://entities/components/crafting_component.gd`；Main helper 进入 live，运行日志无新增脚本、信号、类型或资源错误。

### 旧路径、编码与遗留风险

- 修改文件严格 UTF-8；玩家生产场景不再引用 `CraftingComponent.cs`，旧 C# 文件、UID 和测试对照引用继续作为兼容垫片保留。
- 配方数据当时仍为 C# 资产；随后单独执行配方 Resource 生产切换。Inventory/ItemStack、GameplayPort、TimeSystem、Player、Vital、战斗和 AttributeModifierData 仍未全量完成。

## 2026-09-20 00:58：Crafting 配方 Resource 生产切换

### 本批范围

- 新增 `resources/recipe/res/torch_recipe_gd.tres` 与 `stone_axe_recipe_gd.tres`，保留原配方名称、两项材料、材料数量、输出物品和默认产量 1。
- 将玩家场景的内嵌配方书脚本切换到 `recipe_book_data.gd`，并改为引用两张新 GDScript 配方；原 C# 配方资产和脚本不删除。
- 扩展 reusable_gathering 契约，锁定生产配方脚本、材料数量和玩家生产引用，同时继续验证旧 C# 配方可加载。

### C# / GDScript 兼容边界与回滚点

- 新配方只迁移 CraftingIngredient/CraftingRecipe/RecipeBookData 数据层；`RequiredItem` 与 `OutputItem` 继续保存现有 ItemData Resource 身份，未切换 ItemData/ItemStack/Inventory。
- 旧 `torch_recipe.tres`、`stone_axe_recipe.tres`、`CraftingIngredient.cs`、`CraftingRecipe.cs` 与 `RecipeBookData.cs` 继续作为兼容对照；回滚只需恢复玩家配方书的四条引用。

### GodotAI 验证结果

- `resource_manage(op="load")` 成功加载两张新配方并确认名称、2 个材料、输出和默认产量；玩家场景强制重载后 CraftingComponent 仍为 GDScript。
- 生产资产聚焦 1/1、10 个断言；全部 Crafting 相关测试 6/6。
- 玩家场景运行时确认组件、配方书和两张配方均为 GDScript；Main 运行后 Tab 打开 CraftingUI，显示 2 个配方按钮并默认选中“火把合成表”，关闭正常。
- 当前运行日志只有 helper、初始化和地图生成信息；编辑器 cursor 91 后无新增错误，停止后会话恢复 ready。

### 旧路径、编码与遗留风险

- 本批及 CraftingComponent 修改文件均通过严格 UTF-8；玩家/Main/CraftingUI 生产路径扫描不再出现 CraftingComponent、RecipeBookData、CraftingRecipe 或 CraftingIngredient 的 C# 引用。
- 旧 C# 配方路径仅保留在原兼容资产和测试对照中。Crafting 生产组件、UI 与配方数据已切换，但物品链、GameplayPort、Player、TimeSystem、Vital、战斗和 AttributeModifierData 仍待后续独立迁移。
- 下一批继续选择 Inventory/ItemStack 的单一消费者边界；不得直接切换玩家库存，除非先处理 Player.cs 与 GameplayPort.cs 的强类型依赖。

## 2026-09-20 01:07：Inventory 动态兼容边界

### 本批范围

- 修改 `core/application/GameplayPort.cs`：保留旧 `InventoryToggleRequested(InventoryComponent)`，新增 `InventoryNodeToggleRequested(Node)`、`PlayerInventoryNode` 与通用玩家库存缓存；请求切换背包时按实际库存类型选择信号。
- 修改 `core/ui/inventory_ui.gd`：同时连接和解除旧强类型信号与新增 Node 信号，两条路径继续进入同一个面板切换处理函数。
- 扩展 `tests/godot/test_inventory_component_contract.gd` 的假 GameplayPort 和父面板契约，覆盖动态信号打开及退出清理；本批没有切换 `player.tscn` 的 InventoryComponent。

### C# / GDScript 兼容边界与回滚点

- 当前玩家库存仍是 `InventoryComponent.cs`，因此生产 B 键继续走旧强类型信号；未来库存切换为 GDScript 后，GameplayPort 可直接改发 Node 信号，InventoryUI 不需要再次改变处理逻辑。
- `RequestOpenWarehouse()` 仅在玩家库存与全局仓库都为旧 C# InventoryComponent 时发 `WarehouseRequested`；任一端为 GDScript 时通过 `WarehouseNodeRequested(PlayerInventoryNode, GlobalWarehouseNode)` 传递原节点身份。
- Inventory、BattleDeck、Equipment、ItemStack 的容量、槽位、移动和装备规则均未改动。回滚只需移除新增库存 Node 信号/缓存及 InventoryUI 对应连接，不涉及场景或资产恢复。

### GodotAI 验证结果

- `filesystem_manage(op="scan")` 完成，global class 160、增量 0；`inventory_ui.gd` 符号解析成功。
- `test_inventory_ui_preserves_cross_language_parent_contract`：1/1 通过，共 40 个断言；`inventory_component_contract`：24/24 通过。
- Main 真实运行确认 GameplayPort 同时暴露旧/动态库存信号，`PlayerInventoryNode` 有效且与 `PlayerInventory` 指向同一旧 C# 库存；B 键可打开 InventoryUI，普通槽位和卡组槽位均生成，关闭正常。
- 游戏日志无新增错误；编辑器 cursor 91 之后没有新增 `SCRIPT ERROR`、Parse Error、Failed to load、信号、资源或类型错误，停止后会话恢复 ready。

### 旧路径、编码与遗留风险

- `GameplayPort.cs`、`inventory_ui.gd`、`test_inventory_component_contract.gd` 均通过严格 UTF-8 解码检查，且无 BOM。
- `InventoryNodeToggleRequested` 的非文档引用仅位于 GameplayPort 兼容声明/发送、InventoryUI 双信号连接/清理及聚焦测试，未扩散到库存规则层。
- `InventoryComponent.cs` 的精确路径扫描仍命中 `player.tscn`（本批预期生产引用）、`battle_deck_capacity_tests.gd`（测试对照）和两个 Warehouse 编辑器 `.tmp` 旁车文件；这些保留项未被删除或改写。
- 玩家库存仍受 `Player.cs`、GameplayPort 的 `ItemStack` 入口以及 BattleDeck/Equipment 强类型依赖约束。下一批应只处理 Player 的库存 Node 字段与加物品入口边界，不同时切换 ItemStack、BattleDeck、Equipment、战斗或 Player 根脚本。

## 2026-09-20 01:12：Player 库存 Node 访问边界

### 本批范围

- 将 `entities/Player.cs` 的 `_inventory` 从 `InventoryComponent` 降为 `Node`，仍按 `Components/InventoryComponent` 固定路径解析。
- 保留 `TryAddItemToInventory(ItemStack)` 的旧公开签名和 ItemStack 输入，只把内部调用改为稳定 `AddItem(Item, Amount)` 协议；扩展 Inventory 契约锁定这一边界。
- 本批没有切换 `player.tscn` 的库存脚本，也没有修改 ItemStack、Equipment、BattleDeck、拖拽、容量或堆叠规则。

### C# / GDScript 兼容边界与回滚点

- 当前 C# InventoryComponent 与未来 GDScript InventoryComponent 都以未放入数量作为 `AddItem` 返回值；Player 继续只在返回 0 时报告成功，世界掉落与 GameplayPort 调用方无需改变。
- 回滚点为恢复 Player 的强类型字段、`GetNode<InventoryComponent>` 与直接方法调用；测试中的加物品冒烟在同一帧移除探针物品，没有保留运行时状态。

### GodotAI 验证结果

- `filesystem_manage(op="scan")` 完成，global class 160、增量 0；新增 Player 边界聚焦测试 1/1、5 个断言，完整 `inventory_component_contract` 25/25 通过。
- 玩家场景运行进入 helper live；真实 C# InventoryComponent 上通过 Player 动态入口加入 1 个旧 C# ItemStack，数量从 0 变为 1，随后移除恢复；玩家方法存在且运行成功。
- 当前玩家场景游戏日志只有 helper 与属性初始化信息；编辑器 cursor 91 后无新增错误，停止后会话恢复 ready。

### 旧路径、编码与遗留风险

- `Player.cs` 与聚焦测试严格 UTF-8 解码通过、无 BOM；扫描确认 Player 已无 `GetNode<InventoryComponent>` 或直接 `_inventory.AddItem`，生产玩家场景仍按本批预期引用 `InventoryComponent.cs`。
- 玩家库存生产切换仍被 `EquipmentComponent.cs` 的强类型库存参数阻塞；下一批只处理装备组件的库存 Node 参数边界并保持装备规则不变，完成验证前不切换玩家库存。

## 2026-09-20 01:49：Equipment 消费者 Node 边界

### 本批范围

- 将 `entities/Player.cs` 的 `Equipment` 属性从 `EquipmentComponent` 降为 `Node`，仍按 `Components/EquipmentComponent` 固定路径解析。
- 修改 `TerrainInteractionExecutor.cs`、`WorldInteractionCoordinator.cs`、`GatheringInteraction.cs` 与 `ReusableGatheringInteraction.cs`，通过稳定方法动态读取夜晚遭遇倍率、采集额外产量和工具耗时减免。
- `InventoryUI.cs` 作为旧兼容垫片显式转换旧 C# EquipmentComponent；生产 GDScript InventoryUI、装备槽和装备组件并行实现未改。

### C# / GDScript 兼容边界与回滚点

- 当前 C# EquipmentComponent 与并行 GDScript equipment_component.gd 都提供 `GetNightEncounterChanceMultiplier`、`GetGatheringYieldBonus`、`GetGatheringTimeReduction`，三个消费链不再依赖具体语言类型。
- 缺失组件/方法时分别使用 1、0、0 中性值，保持原有空引用回退；装备槽、ItemStack、EquipmentData、套装、属性与标签结算逻辑均未移动。
- 回滚点为恢复 Player 强类型属性、五个直接调用及旧 InventoryUI 的直接绑定；`player.tscn` 本批仍引用旧 C# 装备脚本。

### GodotAI 验证结果

- `filesystem_manage(op="scan")` 完成，global class 160、增量 0；新增聚焦测试 1/1、8 个断言，完整 `inventory_component_contract` 26/26，`reusable_gathering` 54/54。
- 玩家场景运行进入 helper live；真实 Player.Equipment 节点仍为 `EquipmentComponent.cs`，三项稳定查询均存在，空装备状态返回夜晚倍率 1、额外产量 0、时间减免 0。
- 游戏日志只有 helper 与属性初始化信息；编辑器 cursor 91 后无新增错误，停止后会话恢复 ready。

### 旧路径、编码与遗留风险

- 7 个修改代码/测试文件严格 UTF-8 解码通过、无 BOM；Player 与生产采集消费者不再含 EquipmentComponent 强类型属性/参数，玩家场景仍按本批预期引用 `EquipmentComponent.cs`。
- 下一批可单独把玩家 EquipmentComponent 切换到已验证的 `equipment_component.gd`；切换成功后仍需验证真实换装、标签/属性与采集查询，再处理玩家 InventoryComponent 生产切换。

## 2026-09-20 01:56：EquipmentComponent 生产切换

### 本批范围

- 将 `scenes/player_scenes/player.tscn` 的 EquipmentComponent 从 C# 切换到 `entities/components/equipment_component.gd`；节点结构、导出默认值、枚举、信号和装备规则不变。
- 为 GDScript 组件补齐 `GetEquippedStack(slot)`，保留旧 C# 无 `out` 查询入口；原 `EquipmentComponent.cs` 与 UID 继续作为兼容垫片。
- 聚焦测试强制加载 Main 时发现 Inventory/Crafting/Warehouse 实例仍覆盖旧 C# UI 脚本；移除这三处与已完成迁移冲突的覆盖及五个不再使用的外部资源，没有改动 Main 其它节点或数据。

### C# / GDScript 兼容边界与回滚点

- 新装备组件通过稳定属性/方法读取旧 C# 或 GDScript ItemStack、EquipmentData、ToolData；属性/标签、套装、换装、拖拽、采集产量/耗时和火把倍率职责不变。
- Player 与采集链已在上一批降为 Node 方法协议，生产 UI/DebugLoadout 也是 GDScript；旧 C# UI/Seeder/测试仍可保留并使用旧 EquipmentComponent 对照。
- 回滚点仅为恢复玩家场景 ext_resource 到 `EquipmentComponent.cs`；Main UI 修复属于已完成 UI 迁移的旧覆盖清理，不应恢复。

### GodotAI 验证结果

- `script_manage.find_symbols` 解析为 Node、2 个导出字段、36 个函数和 `EquipmentChanged`；生产脚本契约 1/1、6 个断言，装备规则 4/4、48 个断言。
- 完整 `inventory_component_contract` 27/27，`reusable_gathering` 54/54；曾出现的 Main InventoryUI 脚本身份失败在清理旧实例覆盖后单独与完整回归均通过。
- 玩家场景真实运行确认装备脚本为 GDScript；用 GDScript EquipmentData/ItemStack 装备 Ring1 后可读取堆叠并增加标签 1 层，卸下后堆叠清空、标签归零。Main 运行确认装备、InventoryUI、CraftingUI、WarehouseUI 均为预期 GDScript。
- 玩家与 Main 游戏日志只有 helper、初始化、标签变更和地图生成 info；编辑器 cursor 91 后无新增错误，验证后停止并恢复 ready。

### 旧路径、编码与遗留风险

- `equipment_component.gd`、玩家/Main 场景和聚焦测试严格 UTF-8、无 BOM；生产资产中已无 `EquipmentComponent.cs` 路径，Main 也无三个旧 UI C# 覆盖路径。
- 玩家 InventoryComponent 仍为 C#；下一批可在当前 GDScript Equipment/InventoryUI/Crafting/GlobalWarehouse 边界上单独切换玩家库存，并重点验证 GDScript ItemStack 的加物品、换装、仓库与合成链。

## 2026-09-20 02:07：玩家 Inventory / BattleDeck 物品链生产切换

### 本批范围

- 将 `player.tscn` 的 InventoryComponent 与 BattleDeckComponent 同时切换到 `inventory_component.gd` / `battle_deck_component.gd`；二者共享 GDScript ItemStack 与跨库存移动协议。
- 将 `Player.BattleDeck`、GameplayPort 卡组缓存/属性降为 Node；新增 `GetPlayerSkillCards()`，过滤动态数组并继续向战斗链输出 `Array<SkillCardData>`。
- WorldInteractionCoordinator 的通道驻守战斗改用该过滤出口；旧 C# InventoryUI 仅保留显式兼容转换。旧两个 C# 组件和 UID 均未删除。

### C# / GDScript 兼容边界与回滚点

- 玩家库存仍接受 C# ItemStack 入口：Player 读取其 Item/Amount 后调用 GDScript `AddItem`；卡组只接受旧 C# SkillCardData 或具有 Skill 字段的新 Resource，并保持自动扩容、尾部空槽和按数量展开。
- 战斗信号、WorldCombatScenePresenter 和通道驻守仍获得原 `Array<SkillCardData>`，无效动态元素被丢弃，合法 Resource 顺序与身份不变。
- InventoryUI、WarehouseUI、CraftingComponent、EquipmentComponent、DebugLoadout 与 GlobalWarehouse 已通过 Node 协议连接新库存。回滚必须同时恢复玩家普通库存和卡组脚本，避免不同 ItemStack 实现形成半切换。

### GodotAI 验证结果

- `battle_deck_component.gd` 符号解析成功；卡组生产契约 1/1、9 个断言，卡组规则 1/1、6 个断言，玩家库存生产契约 1/1、8 个断言。
- 完整 `inventory_component_contract` 29/29、`item_stack_contract` 1/1、`reusable_gathering` 54/54。
- Main 运行时确认玩家库存/卡组实际脚本均为 GDScript，DebugLoadout 后 `GetPlayerSkillCards()` 返回 4 张 C# SkillCardData；测试技能卡从玩家库存移入卡组再移回，双方数量精确恢复。
- 旧 C# ItemStack 经 GameplayPort 成功加入 GDScript 玩家库存后移除恢复；动态 InventoryUI、WarehouseUI、CraftingUI 均可打开并关闭。游戏日志仅有 helper、初始化和地图生成 info，cursor 91 后无新增错误，停止后会话 ready。

### 旧路径、编码与遗留风险

- 8 个代码/场景/测试文件严格 UTF-8、无 BOM；玩家与 Main 生产路径不再引用 `InventoryComponent.cs` 或 `BattleDeckComponent.cs`。
- 两个旧精确路径只保留在 `tests/godot/battle_deck_capacity_tests.gd` 的 C# 对照 runner 中，符合兼容测试保留规则。
- 玩家库存/卡组/装备/合成 UI 已生产切换，但 ItemData/SkillCardData/ResourceCardData 等物品 Resource、GameplayPort 根节点、战斗及其余 C# 运行时代码仍待后续独立迁移。

## 2026-09-20 11:58：GameplayPort 生产切换编译阻塞静态修复（待 GodotAI 回归）

### 本批范围

- 仅处理开发者报告的三处 `CS0246: EncounterManager`：`WorldInteractionCoordinator.cs` 一处，以及 `TerrainInteractionExecutor.cs` 两处。
- `EncounterManager` 定义于 `CUSGA.core.application`；两个消费文件现均显式导入 `using CUSGA.core.application;`，不再重复改写类型或扩大迁移范围。
- 本轮没有修改 GameplayPort 协议、场景、资源、数值或战斗逻辑。

### C# / GDScript 兼容边界与回滚点

- GameplayPort 仍按既定生产切换方案由 GDScript 提供，两个 C# 消费者仍通过 `Node`、PascalCase 方法和信号协议访问它；`EncounterManager` 继续保留为 C# 类型。
- 命名空间导入只恢复原 C# 类型解析，不改变运行时边界。GameplayPort 批次的回滚点仍是同时恢复 Main 的脚本引用与两个消费者的强类型访问，不能只回退其中一端。

### 当前证据与待验证项

- 静态扫描确认 `core/application/EncounterManager.cs` 的命名空间为 `CUSGA.core.application`，三处报错位置均可由现有 `using` 解析。
- 两个源码文件时间为 11:52:13，`.godot/mono/temp/bin/Debug/CUSGA.dll` 时间为 11:55:32；程序集产物已晚于修复源码，但本记录不把时间戳替代为完整验收。
- 仍待主线程用 GodotAI 完成 GameplayPort 聚焦、Inventory、reusable_gathering、Main/相关场景冒烟和增量日志检查；这些验证完成前，本批不得标记为通过。

### 文档与遗留风险

- `design.md` 与 `implement.md` 已完整记录 GameplayPort 生产切换边界和验证步骤，本次命名空间修复不改变设计或实施顺序，因此不重复增补。
- 若编辑器下一次 Build 仍报告 `EncounterManager`，应先核对实际构建输入和新增错误位置，不得继续盲目添加别名或改动业务类型。

## 2026-09-20 12:02：GameplayPort 生产切换与 EncounterManager 编译阻塞完成验收

### 本批范围

- 完成 GameplayPort 生产切换的最终 GodotAI 回归，并关闭开发者报告的三处 `CS0246: EncounterManager` 编译阻塞。
- `WorldInteractionCoordinator.cs` 与 `TerrainInteractionExecutor.cs` 仅通过 `using CUSGA.core.application;` 恢复保留 C# `EncounterManager` 的类型解析；没有改动遭遇倍率、战斗、场景结构或序列化数据。
- `Main.tscn` 的 GameplayPort 继续由 `core/application/gameplay_port.gd` 提供，旧 `GameplayPort.cs`、UID 和 C# 兼容输入继续保留。

### C# / GDScript 兼容边界与回滚点

- 两个 C# 消费者继续以 `Node`、PascalCase 方法、属性和 `EncounterRequested` 信号消费 GDScript GameplayPort；卡组和怪物数组在进入 C# 战斗/倍率接口前逐项过滤为强类型数组。
- 生产倍率入口仍由 `WorldInteractionCoordinator.ScaleEncounterMonsters` 委托原 C# `EncounterManager`，没有复制或改写倍率公式。
- 回滚点不变：必须同时恢复 Main 的 `GameplayPort.cs` 引用和两个 C# 消费者的强类型访问；不能只回退其中一端。

### GodotAI 验证结果

- 编辑器会话恢复 `ready` 后执行 `filesystem_manage(op="scan")`：global class 160、增量 0；11:55:32 的 `CUSGA.dll` 已晚于 11:52:13 的两份修复源码。
- GameplayPort 聚焦 3/3、43 个断言，Inventory 29/29，`reusable_gathering` 54/54，全部失败数为 0。
- `res://scenes/Main.tscn` 直接运行进入 helper live；运行时 GameplayPort 脚本为 `gameplay_port.gd`，玩家库存、合成和全局仓库引用均有效，四类请求各精确广播 1 次，卡组出口返回 4 张技能卡，协调器倍率桥可调用。
- 本次 Main 运行的游戏日志只有 helper、初始化和地图生成 info；编辑器 cursor 92 后与游戏日志基线 44 行后均无新增错误，停止后会话恢复 ready。

### 旧路径、编码与遗留风险

- 生产 Main 已不再引用 `GameplayPort.cs`；旧路径只保留在新 GDScript 的兼容说明和 Inventory 契约的旧 C# 对照读取中，符合保留兼容垫片规则。
- 两个 C# 消费者不再持有 GameplayPort 强类型字段；三处 EncounterManager 使用均由明确命名空间导入解析。本批代码、场景、测试和文档严格 UTF-8、无 BOM。
- 编辑器仍保留此前测试产生的旧错误行，但 cursor 92 后没有新增错误，不能把旧日志误判为本批回归。AttributeModifierData 未触碰；下一批继续选择独立的物品 Resource 链，不与战斗或高耦合系统混做。

## 2026-09-20 12:24：LootTable 生产切换

### 本批范围

- 将 52 个怪物资源、7 个地形 Profile、3 个直接地形卡、2 个地图场景和 1 个怪物场景，共 65 个生产资产的内嵌掉落表从 `LootTable.cs` 切换到 `loot_table.gd`；所有 Drops、概率、数量范围、顺序和物品引用只替换脚本/UID，没有改值。
- 同步 `scripts/generated/create_terrain_profiles.gd`，后续重建地形 Profile 不再写回旧 C# 路径；`MonsterData.LootTable` 放宽为 `Resource`，允许旧 C# 与生产 GDScript 掉落表共存。
- 生产 GDScript LootTable 暂时创建保留的 C# `ItemStack`，保持现有棋盘掉落和拾取链的强类型输入；旧 `LootTable.cs`、UID 和 C# 交互对照代码未删除。

### C# / GDScript 兼容边界与回滚点

- LootTable 继续只负责随机、数量和堆叠创建，不拥有库存或棋盘副作用；LootDrop 仍以通用 Resource 保存原 C# ItemData 身份。
- 棋盘、WorldInteractionCoordinator 和 Player 仍接收 C# ItemStack，因此新 LootTable 当前只接受 C# ItemData 派生资产并输出 `ItemStack.cs`。普通 ItemData 资产切换前，必须先单独迁移棋盘掉落堆叠边界。
- 回滚需恢复 65 个生产资产及生成器的旧脚本/UID，并把 MonsterData 字段恢复为强类型；既有 MonsterData 到 LootComponent 未绑定风险未在本批顺手修复。

### GodotAI 验证结果

- filesystem scan 完成，global class 160、增量 0；代表性怪物、地形 Profile 和直接地形卡均可由 `resource_manage` 加载。
- LootTable 生产契约 1/1、10 个断言；一次性采集生产资产 1/1、87 个断言；代表性怪物掉落资产 1/1、22 个断言；LootComponent 1/1、7 个断言。
- 完整 `reusable_gathering` 54/54、`item_stack_contract` 1/1、`inventory_component_contract` 29/29，失败数均为 0。
- Main 运行进入 helper live；运行时代表性怪物和地形掉落表均为 `loot_table.gd`，固定一次掉落生成 1 个 `ItemStack.cs`、数量 1，并保持原 ItemData Resource 身份。游戏日志 44 行均为 helper、初始化和地图生成 info。
- 首次工具态契约直接加载 C# `.tres` 时触发一次 InvalidCastException；按既有规范改为直接实例化 C# ItemData 后测试通过。该修复后的 editor cursor 93 起无新增错误，停止后会话恢复 ready。

### 旧路径、编码与遗留风险

- 生产 `.tres` / `.res` / `.tscn` 中 `LootTable.cs` 路径和旧 UID 均为 0；`loot_table.gd` 精确命中 65 个生产资产。旧路径只剩聚焦测试对生成器回退的否定断言。
- 65 个资产及相关代码、测试、规范和任务文档共 76 个文件严格 UTF-8 通过、无 BOM。
- LootTable 生产脚本仍通过 C# ItemData 与 C# ItemStack 兼容棋盘链，物品 Resource 尚未完成迁移。下一批先处理 BoardCardState、BoardController、TerrainInteractionExecutor、WorldInteractionCoordinator 与 Player 的通用堆叠协议，再把 LootTable 输出切换为 GDScript ItemStack；不与 ItemsControl、技能卡或战斗混做。

## 2026-09-20 12:48：棋盘掉落 ItemStack 通用协议与生产输出切换

### 本批范围

- 将 `LootBoardCardState`、`BoardCardView`、`BoardController`、`WorldInteractionCoordinator`、`TerrainInteractionExecutor`、棋盘端口与 `Player.TryAddItemToInventory` 从具体 C# ItemStack 放宽为跨语言 `RefCounted` 协议。
- 新增 `ItemStackProtocol`，以 `SetItem`/`Clear` 方法门控后读取 `Item`、`Amount`、`IsEmpty`；无效对象在地形操作转换与棋盘批量入口被过滤。
- 生产 `loot_table.gd` 改为创建 `resources/item/item_stack.gd`；概率、数量范围、额外产量、非正过滤和物品 Resource 身份均未改变。普通 ItemData 资产、ItemsControl、技能卡、战斗、StartingStats、Autoload 与 AttributeModifierData 未进入本批。

### C# / GDScript 兼容边界与回滚点

- GDScript 数组进入 C# 后统一使用非泛型 `Godot.Collections.Array`。`SpawnLootOp` 保留 `Array<ItemStack>` 构造函数兼容旧 C# GatheringInteraction/ReusableGatheringInteraction，并显式复制到非泛型数组；新构造函数直接承接 GDScript 数组。
- 旧 `ItemStack.cs`、`LootTable.cs`、调试装载工厂、UID 和旧 C# 交互继续保留；生产棋盘和 Player 同时接受两种 ItemStack，不要求复制或包装 Resource。
- 回滚必须同时恢复 LootTable 的 C# ItemStack 输出、棋盘/Player 强类型签名、SpawnLootOp 泛型字段和地形操作转换，不能只回退单侧。

### GodotAI 验证结果

- 文件系统扫描完成，global class 160、增量 0；`loot_table.gd` 与 `item_stack.gd` 符号解析正常，代表性怪物、地形 Profile 和直接地形卡 Resource 均可加载。
- LootTable 聚焦 1/1（10 断言）、棋盘堆叠边界 1/1（12 断言）、完整 `reusable_gathering` 55/55、ItemStack 1/1、Inventory 29/29、LootComponent 1/1、GameplayPort 3/3，失败数均为 0。
- Main 运行进入 helper live。运行时分别用 GDScript 与旧 C# ItemStack 生成掉落卡，均保持堆叠与 Item 身份并显示数量 3/2；混合数组过滤 1 个无效 RefCounted 后生成 4 张卡；两种堆叠加入玩家 GDScript Inventory 后按同一物品身份合并为 5。
- 本批开发中编辑器工具态直接实例化非 `@tool` C# BoardCardView 场景会降为基础 Area2D，产生 cursor 93→96 的三条预期测试错误；聚焦测试改为属性/源码协议，实际场景行为由 Main 运行时覆盖。cursor 96 后编辑器无新增错误，最终游戏日志仅有 helper 与初始化 info，停止后会话恢复 ready。

### 旧路径、编码与遗留风险

- 生产 `loot_table.gd` 对 `res://core/inventory/ItemStack.cs` 的引用为 0；棋盘掉落消费链只在 `SpawnLootOp` 的旧 C# 构造兼容入口保留 `Array<ItemStack>`。全项目其他旧路径仍属于调试工厂、库存/装备 C# 兼容代码和测试对照，未删除。
- 本批代码、测试、规范和任务文档严格 UTF-8、无 BOM；C# DLL 已由编辑器运行路径刷新，Main 与聚焦回归使用的是新程序集。
- 下一批处理普通 ItemData 资产及 `ItemsControl.gd`、仓库、地图和 UI 中的 `ItemData` 类型注解；不得与技能卡、战斗或 AttributeModifierData 混做。AttributeModifierData 仍须先确认 C# 类型表刷新后才能重试。

## 2026-09-20 13:27：EncounterManager 显式类型别名补强

### 本批范围

- 针对开发者再次报告的 `WorldInteractionCoordinator.cs(44)`、`TerrainInteractionExecutor.cs(18/244)` 三处 `CS0246: EncounterManager`，将两个 C# 消费者的隐式命名空间导入改为 `using EncounterManager = CUSGA.core.application.EncounterManager;`。
- 不改 EncounterManager 实现、GameplayPort 协议、节点路径、遭遇倍率、序列化字段或任何玩法逻辑；旧 C# 类型和兼容边界继续保留。

### C# / GDScript 兼容边界与回滚点

- `EncounterManager` 仍是 `CUSGA.core.application` 下的 C# 类型；显式别名只固定编译期解析，运行时仍由 Main 的 C# 节点提供并由 GDScript GameplayPort 通过协调器桥接。
- 回滚点为恢复两个文件的普通 `using CUSGA.core.application;`；不需要回退 GameplayPort 生产 GDScript 或遭遇系统。

### GodotAI 验证结果

- `session_manage(op="list")` 确认 CUSGA 会话 ready；`filesystem_manage(op="scan")` 完成，global class 160、增量 0。
- GameplayPort 聚焦 3/3，通过 43 个断言；显式别名后编辑器日志 cursor 124→124，无新增 C# 编译/脚本错误。
- 主场景尝试启动时没有出现新的 `CS0246`；GodotAI 返回的 retained 错误均为旧测试工具态 InvalidCast/占位实例记录，未归因于本次别名改动，已停止运行并恢复编辑器 ready。

### 旧路径、编码与遗留风险

- 两个 C# 消费者仍明确引用 `EncounterManager`，这是尚未迁移的兼容 C# 类型，符合保留规则；没有删除 `.cs`、`.csproj`、`.sln` 或 C# Autoload。
- 修改文件严格 UTF-8、无 BOM；下一批继续按进度选择剩余物品 Resource 链，不把战斗、StartingStats、Crafting 或 AttributeModifierData 混入本修复。

## 2026-09-20 13:54：普通 ItemData 生产资产迁移补记

### 本批范围

- 将 `items/` 下 103 个普通物品资产（items、element、environment、equip、items/items、tool 中的普通物品）从 `ItemData.cs` 切换到 `resources/item/item_data.gd`；保留 Ax/Pickaxe 两个 C# ToolData 资产。
- 同步 ItemsControl、商店、仓库、地图、物品槽、主菜单卡、价格生成脚本和 DebugLoadout 的 Resource 读取边界；未改字段名、数值、CardId、图标、标签或堆叠上限。

### C# / GDScript 兼容边界与回滚点

- 普通物品生产脚本为 GDScript `item_data.gd`；SkillCardData、ResourceCardData、EquipmentData、ToolData 及动态装备兼容输入仍允许 C# 类型。
- 生产消费者统一通过通用 Resource 与 `item_data_compat.gd` 读取；旧 ItemData.cs、ItemStack.cs、工具资产和测试对照继续保留。回滚点是恢复 103 个资产脚本/UID 及生产消费者的旧强类型输入。

### GodotAI 验证结果

- 普通生产资产测试 1/1、831 断言；ItemData 兼容桥 1/1、22 断言；ShopTradeBridge 1/1、20 断言；reusable_gathering 56/56；Inventory 29/29；ItemStack 1/1；DebugLoadout 4/4。
- ItemsControl 运行时加载 105 个物品（103 个 GDScript ItemData、2 个 C# ToolData）；Main、Shop、Warehouse 和掉落链均保持原字段与数量行为。

### 旧路径、编码与遗留风险

- `items/` 生产资产中 `ItemData.cs` 路径为 0；C# ItemData、ToolData、ItemStack 仅保留兼容输入、动态装备和测试对照。相关代码、资产和测试严格 UTF-8、无 BOM。
- 普通 ItemData 已完成，后续独立处理 ResourceCardData/SkillCardData，不与 AttributeModifierData、战斗或 Autoload 混做。

## 2026-09-20 13:54：ResourceCardData 生产切换

### 本批范围

- 新增 `resources/item/card/resource_card_data.gd`，等价继承 `item_data.gd`；ResourceCardData 原 C# 类无额外字段或行为，因此只迁移其继承关系，不复制战斗或库存逻辑。
- 将 5 个资源卡资产 `branch.tres`、`charcoal.tres`、`stone.tres`、`torch.tres`、`axe.tres` 的脚本切换到 GDScript，移除旧 `script_class` 和 C# 自定义脚本元数据；所有 CardId、名称、描述、图标和默认价格/堆叠值保持不变。
- 更新 ItemData/兼容桥文档注释，明确 ResourceCardData 已由 GDScript 提供；配方、地形、地图和 DebugLoadout 资源路径保持不变。

### C# / GDScript 兼容边界与回滚点

- 资源卡生产脚本为 GDScript `resource_card_data.gd`，通过 ItemData 的通用字段协议被配方、掉落、库存和 UI 消费；旧 `ResourceCardData.cs` 保留为兼容输入和回滚垫片。
- 本批没有迁移 SkillCardData、CombatSkillData 或战斗效果；回滚点是恢复 5 个 `.tres` 的旧脚本/类标头，不需要恢复任何调用方或序列化字段。

### GodotAI 验证结果

- `filesystem_manage(op="scan")` 完成，global class 160；`script_manage.find_symbols` 成功解析新脚本。
- ResourceCardData 聚焦测试 1/1、65 断言；ItemData 相关回归 4/4、935 断言；5 个代表性 Resource 均由 `resource_manage.load` 成功加载并显示 `resource_card_data.gd`、原 CardId、字段和图标。
- `Main.tscn` 当前场景冒烟进入 helper live；游戏日志 44 行均为初始化/地图生成 info，编辑器 cursor 133 后无新增错误。9 条既有图片 UID 回退 warning 未涉及本批资源卡脚本。

### 旧路径、编码与遗留风险

- 生产资源中 `res://resources/item/card/ResourceCardData.cs`、`script_class="ResourceCardData"` 和旧 C# 自定义脚本元数据均为 0；C# 源文件本身保留。修改脚本、5 个资源和测试严格 UTF-8、无 BOM。
- 下一批处理 `SkillCardData` 物品链；需要先保留 `CombatSkillData`/战斗消费的动态兼容边界，不能把战斗实现或 AttributeModifierData 同批迁移。

## 2026-09-20 14:44：SkillCardData 动态兼容边界

### 本批范围

- 新增 `resources/item/card/skill_card_data.gd` 作为 SkillCardData 的等价 GDScript 实现，保留 `Skill`、`cost`、`CardTags`、显示字段回退、元素读取、最大堆叠为 1 以及 `ApplyEffect(context)` 委托语义。
- 将 GameplayPort、局外遭遇转发、战斗场景 Presenter、BattleManager、DeckManager 与 SkillCard 展示入口统一放宽为 `Resource` / `Array[Resource]`，并通过 `ApplyEffect` 方法协议过滤技能卡。
- 本批只建立动态兼容边界，没有切换 `resources/skill_cards/`、怪物/CombatSkill 资产、`battle.tscn` 内嵌技能卡或卡表生成脚本；旧 C# SkillCardData 生产资产继续可用。

### C# / GDScript 兼容边界与回滚点

- 新旧技能卡均以 Resource 身份进入卡组与战斗链，稳定协议为序列化字段及 `ApplyEffect(context)`；C# `CombatSkillData` 和 `SkillExecutionContext` 继续承担技能实际执行，不在本批迁移。
- C# `BattleDeckComponent` 与 `InventoryUI` 的强类型入口继续作为旧生产输入保留；动态生产入口不得再要求 `Array<SkillCardData>` 或 `is SkillCardData`。
- 回滚点为删除并行 GDScript 脚本及 UID，并恢复各动态入口的 SkillCardData 强类型签名；生产资产本批未切换，因此不需要回滚 `.tres` / `.tscn`。

### GodotAI 验证结果

- SkillCardData 聚焦 1/1、19 个断言；GameplayPort 3/3、49 个断言；Inventory 29/29；`reusable_gathering` 58/58；ItemData 4/4、935 个断言，失败数均为 0。
- `battle.tscn` 直接运行进入 helper live，41 行战斗日志无新增错误；`Main.tscn` 直接运行进入 helper live，44 行初始化与地图日志无新增错误。
- 最终 editor cursor 162；Main 冒烟后增量日志为空。开发中出现的工具态 C# 静态工厂限制及占位实例错误均由真实场景运行覆盖，没有改写生产 C# API。

### 旧路径、编码与遗留风险

- 生产 `.tres` / `.tscn` 对 `skill_card_data.gd` 的引用仍为 0，这是本批刻意保留的边界；旧强类型残留只允许位于 C# 兼容实现、旧资产、测试对照与任务记录。
- 本批 19 个代码、测试、UID、任务文档和规范文件严格 UTF-8、无 BOM；没有删除 C#、`.csproj`、`.sln` 或 C# Autoload。
- 下一批单独切换 SkillCardData 生产资产与 `card_table/export_current_cards.py`，保持全部 CardId、显示字段、Skill、cost、CardTags 和 UID；仍不得把 CombatSkillData、战斗效果或 AttributeModifierData 混入。

## 2026-09-20 15:25：SkillCardData 生产资产切换

### 本批范围

- 将 `resources/skill_cards/` 下 69 个生产技能卡统一切换到 `resources/item/card/skill_card_data.gd`；同步切换 `battle.tscn` 的两个内嵌技能卡，并保持 19 张起始卡的顺序、全部 CombatSkillData / CardEffect / DamageEffect 子资源和数值不变。
- 清理 31 个怪物资源与 8 个 CombatSkill 资源中只声明未使用的旧 SkillCardData Script ext_resource；更新 `card_table/export_current_cards.py`，后续生成不再写回 C# 脚本路径、旧 UID 或旧类标记。
- 修复生产冒烟发现的显示回退差异：GDScript 技能卡现在读取保留 C# CombatSkillData 的导出字段 `CardName`、`Description`、`CardIcon`，不再动态读取不可稳定访问的非导出 `Display*` 计算属性。

### C# / GDScript 兼容边界与回滚点

- SkillCardData 包装层已由 GDScript 生产 Resource 提供；`Skill` 仍保持原 C# CombatSkillData Resource 身份，`ApplyEffect` 仍委托其 `Execute(SkillExecutionContext)`，目标、效果、伤害和状态结算均未迁移或复制。
- 旧 `SkillCardData.cs`、UID、C# BattleDeckComponent / InventoryUI 兼容输入及全部 CombatSkillData / SkillExecutionContext 继续保留；动态卡组与战斗入口仍以 Resource / `Array[Resource]` 协议工作。
- 回滚点是同时恢复 69 个技能卡、battle 内嵌包装脚本、怪物/CombatSkill 冗余声明和生成模板；不能只回退资产或生成器单侧。

### GodotAI 验证结果

- `skill_card_data.gd` 符号解析成功；动态契约 1/1、22 个断言，包含真实 C# CombatSkillData 的名称、描述、图标回退与 Resource 身份；生产资产契约 1/1、1324 个断言。
- GameplayPort 3/3、Inventory 29/29、`reusable_gathering` 58/58，失败数均为 0。测试工具给出通用 preload cache 提示，但新增断言和后续独立游戏进程均使用了当前生产脚本。
- `battle.tscn` 运行进入 helper live 和玩家回合，完成怪物行动与抽牌；Main 运行进入 helper live。editor cursor 181 后两次冒烟均无新增脚本、类型、信号、资源或运行时错误。
- Battle 调试日志仍用原始 `CardName` 输出内嵌包装卡，因此空字符串不代表 UI `DisplayName` 失败；实际 SkillCard UI 继续读取已验证的 `DisplayName`，未为日志改动资产或玩法值。

### 旧路径、编码与遗留风险

- 生产技能卡、battle 场景、怪物/CombatSkill 资产和卡表生成器对 `SkillCardData.cs`、旧 UID 与旧自定义脚本元数据的引用为 0；生产资产/场景中的 `script_class="SkillCardData"` 为 0。生成器只保留 1 条删除历史 `script_class` 的清理正则，不会生成该标记；其他旧路径只允许保留在 C# 兼容源码、测试对照、UID 旁车和任务记录。
- 本批修改脚本、测试、资产、生成器和文档严格 UTF-8、无 BOM；9 条普通物品图片 UID warning 为既有内容，不涉及技能卡生产脚本。
- CombatSkillData、SkillExecutionContext、战斗效果和 AttributeModifierData 尚未迁移；下一批继续选择单一明确边界，AttributeModifierData 仍必须先确认 C# 程序集和类型表刷新后才能重试。

## 2026-09-20 15:44：EquipmentData / ToolData 生产切换

### 本批范围

- 将 `items/tool/Ax.tres` 与 `Pickaxe.tres` 从旧 C# `ToolData.cs` 切换到 `resources/item/tool/tool_data.gd`，保持资产 UID、槽位 5/6、采集标签 wood/earth、10 点时间减免、价格、标签、CardId、名称、图标和描述不变。
- 将 `debug_generated_equipment_entry.gd` 的动态装备工厂切换为生产 `equipment_data.gd` / `tool_data.gd` 与 `item_stack.gd`；普通装备和工具的分支条件、随机属性范围、CardId、槽位与 RollRandomStats 时序保持不变。
- 扩展 DebugLoadout、Inventory 与 reusable_gathering 契约，覆盖生产工具资产、生产装备组件和旧 C# ToolData 兼容读取；旧 C# 枚举数组夹具改为在原数组上追加值，避免工具态跨语言数组赋值歧义。

### C# / GDScript 兼容边界与回滚点

- 生产工具和 Debug 动态装备输出已使用 GDScript Resource/ItemStack；`equipment_data_compat.gd` 继续通过稳定字段同时读取旧 C# 与新 GDScript EquipmentData/ToolData。
- `EquipmentData.cs`、`ToolData.cs`、`ItemStack.cs`、旧 EquipmentComponent 和测试对照均保留，不删除 C# 兼容输入；本批没有迁移 AttributeModifierData、套装规则、属性组件或普通 ItemData 工具外观资产。
- 回滚点为同时恢复 Ax/Pickaxe 的旧脚本与类标头，并恢复 Debug 工厂的三种 C# 预加载；不能只回退资产或工厂单侧。

### GodotAI 验证结果

- `debug_generated_equipment_entry.gd`、EquipmentData/ToolData、兼容桥和测试脚本均成功符号解析；四个新增聚焦测试分别 1/1 通过，断言数为 26、18、19、12。
- DebugLoadout 4/4、Inventory 29/29、reusable_gathering 58/58，失败数均为 0。GameplayPort 3/3 复核通过；此前三处 `CS0246: EncounterManager` 已由显式类型别名解析，Main 可进入 helper live。
- Ax/Pickaxe 编辑器 Resource 加载确认脚本、槽位、标签、减免、CardId、名称和图标；Main 独立游戏进程以 `CACHE_MODE_REPLACE` 重新加载后确认两张资产不含旧 `_custom_type_script` 元数据。editor cursor 190 后无新增错误，游戏日志只有 helper 注册信息。

### 旧路径、编码与遗留风险

- 生产工具资产和 Debug GDScript 对 `EquipmentData.cs`、`ToolData.cs`、`ItemStack.cs`、旧 ToolData UID 与旧类标头引用为 0；剩余旧路径只在明确的 C# 兼容测试对照中出现。
- 本批 12 个代码、资产、测试与任务设计文件严格 UTF-8、无 BOM。完整 reusable_gathering 仍产生 9 条既有普通物品图标 UID warning，未涉及本批工具资产。
- EquipmentData/ToolData 生产边界已完成；下一批继续选择单一剩余物品或 Crafting 边界。AttributeModifierData 仍不得处理，除非先确认 C# 程序集和类型表已刷新。

## 2026-09-20 16:04：CurrentMapBackgroundResolver 生产切换

### 本批范围

- 将 `WorldCombatScenePresenter` 的生产背景入口从旧 C# Resolver 静态调用切换为固定路径加载 `current_map_background_resolver.gd`，通过 Script `new` 创建 `RefCounted` 并调用稳定的 `DuplicateCurrentBackground(mapSystem)` 方法。
- 扩展 `reusable_gathering` 的背景契约，锁定生产 Presenter 的 GDScript 路径、动态工厂、方法协议以及不再静态调用旧 C# Resolver；背景查找仍优先使用 `MapInstantiator.current_scene`，复制结果仍命名为 `MapBackground`。
- 本批没有修改 Battle 场景结构、怪物/卡组数组、CombatSkill、战斗结算、地图房间命名或 AttributeModifierData。

### C# / GDScript 兼容边界与回滚点

- GDScript Resolver 只拥有当前房间 `Background` 的查找、复制与层级参数；C# Presenter 继续拥有 Battle 实例化、过场、世界视图显隐和 `battle_ended` 生命周期。
- 旧 `CurrentMapBackgroundResolver.cs` 与 UID 保留为兼容垫片。GDScript 脚本、实例或方法协议不可用时，Presenter 记录明确错误并跳过背景，不阻断原战斗流程。
- 回滚点只需恢复 Presenter 的旧静态 C# Resolver 调用；无需回退 Battle、地图、怪物或技能资产。

### GodotAI 验证结果

- CUSGA 编辑器会话 `cusga@3b687b3cf010adc9` 可运行；文件系统扫描完成，global class 160、增量 0。
- Main 真实遭遇烟测进入战斗和玩家回合；临时注入房间的 `Background` 经生产 C# Presenter → GDScript Resolver 复制为 `Battle/MapBackground`，`modulate=(0.25, 0.5, 0.75, 1)`、`z_index=-1`、`z_as_relative=false` 均保持预期。
- GDScript Resolver 符号解析得到 4 个函数；Resolver 聚焦 1/1、`reusable_gathering` 58/58、GameplayPort 聚焦 3/3，失败数均为 0。真实遭遇游戏日志 58 行均为 helper、地图、战斗初始化和抽牌 info；editor cursor 190 后没有新增错误。
- 完整 reusable 回归只新增 9 条既有普通物品图片 UID 回退 warning；没有新增 `SCRIPT ERROR`、Parse Error、Failed to load script、资源加载失败、信号错误、类型错误或运行时错误。

### 旧路径、编码与遗留风险

- 生产 `WorldCombatScenePresenter.cs` 对 `CurrentMapBackgroundResolver.DuplicateCurrentBackground` 的静态调用为 0；旧 C# 路径只保留在 `CurrentMapBackgroundResolver.cs` 兼容实现中，GDScript 路径由 Presenter 和两组背景测试引用。
- Presenter、GDScript Resolver、reusable 测试、design 与 implement 严格 UTF-8、无 BOM；追加后的进度与反馈文档也通过同一检查。
- 当前地图房间仍必须把可复制背景命名为 `Background`，这是新旧 Resolver 的既有稳定契约；不符合该节点名的旧房间会继续无背景进入战斗。本批已完成，下一批继续选择独立边界，不与 AttributeModifierData 或战斗结算混做。

## 2026-09-20 16:19：TimeSystem 生产 Autoload 切换

### 本批范围

- 将 `project.godot` 的 `TimeSystem` Autoload 从旧 C# UID 切换到 `res://core/autoloads/time_system.gd`（资源 UID `uid://dd7cqlb6a72pe`），其他 10 个 Autoload 的名称、路径和 singleton 状态保持不变。
- 将 `WorldInteractionCoordinator`、`TerrainInteractionExecutor`、`WorldInteractionContext`、`TerrainInteractionBuildContext`、`PassTimeOp`、`RecordReusableGatheringOp`、`ReusableGatheringInteraction`、`RoomBoardPresenter` 与 `EncounterManager` 改为 Node/属性/方法/信号协议，不再依赖 `TimeSystem.Instance`。
- 旧 `TimePanelUI.cs` 与 `TalentManager.cs` 也改为动态 Node 信号协议，避免兼容场景重新启用时依赖失效的静态单例；生产 TimePanel 与 TalentManager 仍保持现有 GDScript 脚本和节点结构。

### C# / GDScript 兼容边界与回滚点

- `/root/TimeSystem` 稳定协议为 `TotalTimePassed`、`CurrentDay`、`IsNight`、`PhaseProgress`、`MapMoveTimeCost`，方法 `PassTime`、`PassMapMoveTime`、`SetMapMoveTimeCost`，以及 `DayNightToggled`、`DayPassed`、`TalentSelectionTriggered`、`TimeChanged` 四个信号。
- 地形操作通过两个现有 Context 传递同一个时间 Node；EncounterManager 生产时解析 Autoload，并保留 `TimeSystemNode` 显式注入供 C# 对照测试使用。旧 `TimeSystem.cs` 与 UID 继续作为兼容垫片保留。
- 回滚必须同时恢复 `project.godot` 的旧 C# UID 与所有生产消费者的静态读取，不能只恢复 Autoload 单侧；本批没有改变 100 点阶段、10 点地图移动、信号顺序、采集冷却、天数、七日天赋、存档或战斗规则。

### GodotAI 验证结果

- 文件系统扫描完成，global class 160；`time_system.gd` 符号解析得到 10 个函数、4 个信号和 1 个导出字段。GodotAI Autoload 列表确认 TimeSystem 指向 `res://core/autoloads/time_system.gd`，其他 10 项保持原值。
- TimeSystem 生产契约 1/1、TimePanelUI 1/1、GameplayPort 3/3、完整 `reusable_gathering` 58/58，失败数均为 0。
- fresh Main 运行时脚本身份为 `time_system.gd`。初始状态为 `[0, 1, false, 0, 10]`；`PassTime(95)`、`PassMapMoveTime()`、`PassTime(1095)` 后总时间 1200、天数 7、白天、阶段进度 0，依次发出 12 次昼夜、6 次天数与 1 次七日天赋信号，三次完整快照为 95、105、1200；HUD 同步显示“第 7 天 / 白天 / 0 / 100”。
- 游戏日志 47 行均为 helper、地图生成与三次时间推进 info；editor cursor 199 后没有新增错误。完整 reusable 回归新增的 9 条普通物品图片 UID warning 为既有内容。

### 旧路径、编码与遗留风险

- 生产代码中 `TimeSystem.Instance`、`GetNode<TimeSystem>` 和 `private TimeSystem` 命中均为 0；`project.godot`、生产场景/资源/脚本对旧 UID `uid://da5qjs7g2tqfs` 和 `TimeSystem.cs` 路径命中为 0。
- 旧 `TimeSystem.cs`、`.uid`、C# 对照测试与任务记录按兼容规则保留；本批全部代码、配置、测试和任务文档严格 UTF-8、无 BOM。
- 运行时推进只发生在 fresh Main 进程，停止后未持久化任何时间状态。TimeSystem 生产切换已完成；下一批继续选择独立剩余 Autoload、Crafting 或战斗外围边界，AttributeModifierData 仍须等待类型表确认。

## 2026-09-20 17:08：BoardCardView 生产切换

### 本批范围

- 新增 `core/board/board_card_view.gd`，等价承接旧 `BoardCardView.cs` 的地形/掉落显示、标题/图标、堆叠数量、地形 3 倍缩放、散射/飞行动画、禁用灰显及五个鼠标信号。
- 将 `scenes/board_card_scene/BoardCardView.tscn` 切换到 GDScript；将 `BoardController.cs` 的视图集合、信号、生成/移除/快照入口降为 `Node2D`，通过稳定方法协议初始化卡牌；`WorldInteractionCoordinator.cs` 与 `TerrainInteractionExecutor.cs` 保持同一动态视图边界。
- 新增 `tests/godot/test_board_card_view_contract.gd` 与 `tests/test_board_card_view_contract.gd`，覆盖生产脚本路径、方法/信号、地形/掉落初始化、显示字段、数量、缩放和禁用状态。

### C# / GDScript 兼容边界与回滚点

- GDScript 视图读取 `TerrainInstance.TerrainData` 与 `ItemStack.Item/Amount/IsEmpty` 稳定协议；C# BoardController 仍拥有卡牌状态集合、网格去重、散射半径、信号转发和移除时序。旧 C# `BoardCardView.cs`、UID、`BoardCardState`、`RoomTerrainStore` 与战斗代码继续保留。
- `@tool` 只使编辑器契约测试能够实例化同一生产视图，不增加编辑器专用行为。回滚点为恢复场景旧脚本并把三个消费者的 Node2D 视图入口恢复为 C# 类型，不能只回退场景引用。

### GodotAI 验证结果

- CUSGA GodotAI 会话重启后重新扫描，global class 160、增量 0；`board_card_view.gd` 符号解析成功，包含 26 个函数、5 个信号和 `TerrainCardRestingScale` 导出字段。
- `board_card_view_contract` 1/1、33 个断言；`reusable_gathering` 58/58；`inventory_component_contract` 29/29；`item_stack_contract` 1/1，失败数均为 0。
- Main fresh run 进入 helper live 并正常停止；编辑器日志无本批新增 SCRIPT ERROR、Parse Error、Failed to load script、资源加载失败、信号错误或运行时错误。保留的警告为 9 条既有普通物品图片 UID 回退、GlobalEventBus 未使用信号和既有整数除法提示。

### 旧路径、编码与遗留风险

- 生产 `BoardCardView.tscn` 对 `BoardCardView.cs` 的引用为 0；旧 C# 文件、UID、测试对照和任务记录按兼容规则保留。`BoardController` 的视图边界仍通过动态方法连接，尚未迁移 `BoardCardState`、`RoomTerrainStore`、`RoomBoardPresenter` 或战斗链。
- 本批脚本、场景、C# 消费者、测试与任务文档均通过 UTF-8 无 BOM 检查；未运行 `dotnet` 或 `godot-mono`。
- 下一步优先处理 BoardCardState/RoomTerrainStore 或另一条独立战斗外围边界；StartingStats、Crafting、Autoload 深层清理、战斗效果和 AttributeModifierData 仍不得混入。AttributeModifierData 仍需先确认 C# 类型表刷新后再决定方案。

## 2026-09-20 17:45：BoardCardState 生产切换

### 本批范围

- 新增 `core/board/board_card_state.gd`，等价保存 terrain/loot 状态、TerrainInstance/TerrainData、ItemStack/Item/Amount/IsEmpty 原始身份、掉落数量和数量显示规则。
- `BoardController.cs` 改为通过脚本方法协议创建和消费状态，继续保留卡牌集合、网格去重、视图实例化、信号和动画时序；旧 `BoardCardState.cs`、两个派生 C# 状态继续保留。
- 扩展棋盘跨语言契约，覆盖 GDScript 状态 Resource 身份、数量、类型和生产控制器脚本路径；未触碰 RoomTerrainStore、存档、战斗或 AttributeModifierData。

### C# / GDScript 兼容边界与回滚点

- 状态对象使用 `RefCounted` 和显式 `InitializeTerrain` / `InitializeLoot` / `IsTerrain` / `IsLoot` / `Get*` 方法，不声明全局 class，避免与 C# 类型表冲突。
- BoardController 仍接收旧 C# 或 GDScript ItemStack，状态脚本只读取稳定属性并保留原对象身份；脚本缺失或输入无效时抛出原等价参数/初始化异常。
- 旧 C# BoardCardState 文件与 UID 作为兼容垫片保留；回滚点为恢复 `BoardController` 的 C# 状态构造与分支，不改变场景或资源序列化。

### GodotAI 验证结果

- `board_card_state.gd` 符号解析成功，9 个方法；文件系统扫描完成，global class 160。
- `reusable_gathering` 的 `test_board_loot_stack_cross_language_contract`：1/1，20 断言通过；Main 独立场景进入 helper live，游戏日志 44 行无错误。
- 清理后的 editor 日志没有本批新增错误；只保留项目既有的普通物品图片 UID warning、GlobalEventBus 未使用信号等警告。

### 旧路径、编码与遗留风险

- 生产 `BoardController.cs` 不再声明 `BoardCardState state` 或实例化两个 C# 派生状态；旧路径仅在兼容文件、测试对照和任务记录中保留。
- 修改代码、测试和任务文档均 UTF-8 无 BOM；未运行 `dotnet`、`godot-mono`、CodeGraph 或 GitNexus。
- 下一步继续选择单一独立边界，优先评估 `RoomTerrainStore` 是否可在不触碰存档格式的情况下迁移；StartingStats、Crafting 深层清理、Autoload 收尾、战斗效果和 AttributeModifierData 仍不混做。

## 2026-09-20 17:33：EncounterManager 编译兼容与 PassageGuardMonsterResolver 收口

### 本批范围

- 修复 `WorldInteractionCoordinator.cs`、`TerrainInteractionExecutor.cs` 对 `EncounterManager` 的编译期具体类型依赖：两处均改为持有 `Node`，通过 `ScaleEncounterMonsters` / `ResolveGatheringEncounter` 稳定方法协议调用，保留原倍率、遭遇和返回顺序。
- 完成 `core/map/passage_guard_monster_resolver.gd` 生产解析器切换，`passage_guard_controller.gd` 与驻守契约测试改用 GDScript；无向边规范化、房间缓存、Encounter `Monsters` 读取和 MonsterData 身份保持不变。
- 更新 GameplayPort 契约测试以锁定动态倍率协议；没有删除旧 C# 管理器、解析器、结果类型、`.csproj`、`.sln` 或 Autoload。

### C# / GDScript 兼容边界与回滚点

- `EncounterManager.cs` 继续作为现有 C# 节点实现；消费者只要求节点存在并实现稳定方法。C# 返回的 `GatheringEncounterResult` 通过 `RefCounted` 原始身份继续进入现有交互端口，管理器缺失时返回 `None`。
- 倍率桥先过滤动态数组为 `Array<MonsterData>`，动态调用后再次过滤返回值；缺少方法或返回非数组时回退过滤后的输入，不改变缩放算法。
- 回滚点为恢复两个消费者的强类型字段/构造参数和直接方法调用；驻守回滚点为恢复控制器旧 C# Resolver 实例化。生产场景、存档、概率与战斗请求均未改动。

### GodotAI 验证结果

- 会话 `cusga@560bb3ad50f49292` 在验证前为 `ready`；文件系统扫描完成，global class 160。
- `passage_guard_monster_resolver_contract`：1/1，8 断言通过；`reusable_gathering`：58/58；GameplayPort unnamed 套件：4/4（包含倍率源码契约）。
- `res://scenes/Main.tscn` 独立运行进入 helper live；运行时调用 `WorldInteractionCoordinator.ScaleEncounterMonsters(null, [])` 返回空数组，直接调用 EncounterManager `ResolveGatheringEncounter` 返回有效 `GatheringEncounterResult`，游戏日志无错误。
- 编辑器日志清理后仍能看到此前旧测试夹具留下的 3 条 Errors 面板历史记录（数组越界与错误释放 RefCounted），本批聚焦测试均通过；当前 Main 游戏日志仅含既有地图连接 warning/info。

### 旧路径、编码与遗留风险

- 旧 C# 路径扫描仅允许命中 `EncounterManager.cs`、`PassageGuardMonsterResolver.cs`、结果类型、兼容测试和任务文档；生产控制器已不再实例化 C# Resolver，两个 C# 消费者已无 `EncounterManager` 具体类型声明。
- 修改文件采用 UTF-8 无 BOM；未运行 `dotnet`、`godot-mono`、CodeGraph 或 GitNexus。
- 编辑器历史 Errors 面板需要后续编辑器重启后再做一次纯日志清理；不把该历史记录误判为本批运行时失败。下一步继续选择一个独立边界，优先处理 BoardCardState/RoomTerrainStore 或单独的战斗外围协议；StartingStats、Crafting 深层清理、Autoload 收尾、战斗效果和 AttributeModifierData 仍不混做。
