# 实施计划

1. 调整 `CardManager` 的目标选择导出参数：缩短可选目标呼吸半周期，降低描边宽度，并增加次级目标描边颜色。
2. 在点击模式 GUI 阻挡判断中沿父链识别怪物卡面控件，仅放行该类展示控件。
3. 通过 `CardManager` 传入 2px 运行时描边参数；保留怪物场景既有默认描边设置，避免无关场景元数据与本次视觉参数耦合。
4. 新增 `test_card_5`、`test_card_6`、`test_card_7` 的技能及卡牌资源，分别覆盖任意单位、全体单位和扩散敌人；将 `test_card_1` 至 `test_card_7` 各三份写入战斗场景初始牌组。
5. 新增初始牌组目标类型覆盖测试，同步游戏机制文档、状态管理规范和临时学习反馈。
6. 执行 `CI=true` 解决方案构建与差异检查；遵循本机规则跳过 Godot Mono。
